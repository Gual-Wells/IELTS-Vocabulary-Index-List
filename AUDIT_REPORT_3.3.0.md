# Vocabulary Index 3.3.0 实施审计报告

## 1. 审计结论

3.3.0 已将 3.2.0 的“文档滚动网页＋统一底部 Sheet＋双行一级表项”收敛为更接近 iPhone 主屏幕应用的固定导航、覆盖层、分型弹窗和紧凑一级表项。

数据层未进行结构迁移，主要风险集中在 iOS WebKit 真机行为，而不是 Schema 或业务数据。

## 2. 已处理问题

### 状态栏和顶部

- 固定导航包含 Safe Area；
- 大标题与紧凑标题分工；
- 深滚动时职能入口不离开视口；
- PIN、标注和首页警告不改变文档高度。

### 键盘和弹窗

- Visual Viewport 增加顶部与左侧偏移；
- 移除 `body position: fixed` 锁滚动；
- 输入窗口默认不聚焦；
- 搜索窗口使用键盘上方的有边距卡片。

### 跳转

- 标题与条目使用不同定位函数；
- 固定工具区与横向字母轨道分离；
- 首页应用内返回恢复位置。

### 一级表项

- 固定预留关联轨道；
- 普通词和释义共同横滑；
- 短语按普通、两行、极限三种有限状态渲染；
- 查询由两个常驻按钮合并为按需锚定菜单；
- 标注状态不再占用一级按钮槽。

### 高危操作

- Seed 重置先下载备份再确认；
- 取消程序许可式复选框。

## 3. 性能审计

保留：

- 关系索引；
- 42 条分块惰性渲染；
- SVG 模板克隆；
- 搜索合并；
- PIN 和日期局部事务；
- 滚动位置限流。

新增：

- 分块占位高度按普通行、两行短语和极限短语估算；
- 查询菜单只建立两个节点；
- 普通词横滑不需要 JavaScript 测宽；
- 极限短语功能区由 CSS Grid 根据实际节点数分配。

未发现高频关系读取、搜索或 PIN 重新引入 `backupFromState()`。

## 4. 仍需真机确认

当前执行环境无法完成真实 iPhone Safari standalone 自动化。以下必须由真机清单确认：

- 状态栏与灵动岛的实际视觉衔接；
- 键盘打开／关闭后的 Visual Viewport；
- 从 Oxford 和 ChatGPT 返回；
- `AI查询` 快捷指令是否正确绑定输入；
- 横滑文字与纵向页面滚动的手势竞争；
- 不同 iPhone 宽度下的普通词、两行短语和极限短语；
- 长时间滚动后的惰性块高度稳定性；
- iOS 系统级缩放边界。

## 5. 风险等级

- 数据迁移风险：低；
- Seed 内容变化风险：低；
- 主线程回归风险：中，已有自动性能阈值和结构性限制；
- iOS WebKit 视口风险：中，需真机验证；
- ChatGPT 快捷指令风险：未改变协议，需用户在交付后调试快捷指令本身。
