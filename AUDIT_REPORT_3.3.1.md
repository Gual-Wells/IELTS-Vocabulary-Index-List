# Vocabulary Index 3.3.1 实施审计报告

## 1. 审计结论

3.3.1 针对 3.3.0 真机截图和源码深查中确认的缺陷进行修正。修复范围同时覆盖数据引用、虚拟总表身份、导航状态、AI 标注、局部事务、覆盖层、一级表项和视觉系统，而不是只追加截图级 CSS 补丁。

Schema 4、Seed revision 3 和业务 Seed 均未改变。主要风险从“确定存在的源码缺陷”收敛为“仍需真实 iPhone WebKit 验证的布局、手势和生命周期行为”。

## 2. 关键修复审计

### 虚拟总表与位置

- 全局虚拟 Collection 的位置作用域固定为 `global`，不再按当前代表 Entry 的词域写入；
- 位置写入去重身份包含词表、模式、分区和 Entry；
- 全局词汇总表与全局短语总表均纳入字母／日期模式回归测试；
- 全局代表 Entry 变化时，PIN 和上次位置按规范英文重新映射。

### 数据完整性

- 删除孤立词、删除 Entry、删除词域和替换短语范围后清理 Entry StudyStamp；
- 只有仍存在对应聚合内容时才保留 Global StudyStamp；
- 重命名最后一个聚合实例时迁移全局日期键；
- 系统短语表 VIX 替换同步清除指向被移除短语的 Membership；
- 删除词表后的视图模式、月份和位置软引用同步规范化。

### 导航与状态切换

- Store mutation 不再无条件清除同一路由的 pending jump；
- 表单提交不再额外触发第二次完整 render；
- `hashchange` 与 `popstate` 合并到单帧路由重绘；
- 返回首页使用 `replaceState`，避免不断堆积首页历史；
- 关系展开状态改为 `collectionId + entryId`；
- 关系目的地只允许实际投影到普通表的目标。

### 标注与 AI

- iPhone 恢复“编辑”和“撤销此条”控件；
- 当前词表审阅尽量留在当前词表；
- 全局总表按规范英文聚合所有词域的标注；
- 当前词表与全局全部撤销使用局部 Annotation 事务；
- AI 批次写入校验 Entry 快照，数据危险变更前主动取消任务；
- Annotation 批次不再调用整库 `backupFromState()` 和通用全库重建。

### 布局与视觉

- 首页固定顶栏使用显式 Grid Area；
- 序号采用独立外部轨道，三位数不再覆盖英文；
- 返回顶部进入固定导航工具区，不再浮在一级表项之上；
- 固定顶栏、导航和覆盖层改为不透明表面；
- 关联附着轨道缩窄、降权，方向和二级跳转使用统一 SVG 图标；
- 横滑文本按起点／中段／末端显示渐隐提示；
- 关闭按钮保留 44pt 命中区但降低视觉重量。

## 3. 高危操作

高危操作采用两阶段流程：

1. 小型备份选择窗口：`下载备份`／`不下载`；
2. 无论选择哪项，继续进入对应操作本身的确认窗口；
3. 备份选择窗口不能通过背景或 Escape 跳过；
4. 实际确认窗口仍可取消整个操作。

增量合并不显示高危备份选择；完整替换、Seed 恢复、完整备份恢复、删除词域／词表／内容和移除来源使用该流程。

## 4. 性能审计

保留：

- 42 条分块惰性物化；
- 关系内存索引；
- SVG 模板缓存；
- 搜索合并；
- 滚动位置限流；
- PIN、学习日期和 Annotation 局部事务。

新增防回归：

- Annotation 写入不再重建整库；
- 同一表单 mutation 不再重复 render；
- 路由事件同帧合并；
- 关系目标直接使用可见投影索引；
- 文字溢出只对已物化行进行局部判断；
- 极限短语功能区使用 CSS 自动分布，不在滚动路径持续测量。

## 5. 仍需真机确认

当前执行环境无法替代真实 iPhone standalone PWA。以下不能据自动测试声明通过：

- 状态栏、灵动岛和 Home Indicator；
- 键盘打开／关闭与 Visual Viewport；
- Oxford／ChatGPT 返回；
- 长词横滑与纵向滚动手势竞争；
- PIN、Review、更新提示和首页警告同时出现时的最终视觉；
- 关联展开后的惰性块高度稳定性；
- iOS 对缩放限制的系统边界；
- `AI查询` 快捷指令的实际输入绑定。
