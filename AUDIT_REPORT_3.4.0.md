# Vocabulary Index 3.4.0 源码审计报告

## 审计范围

本报告以 3.3.1 完整源码、3.3.1 深度审计、两批 iPhone 真机截图以及 3.4.0 全部确认规则为基础，复查：

- 数据模型与 Schema 迁移；
- 系统总表投影；
- PIN、Annotation、StudyStamp；
- 搜索、关系和跳转；
- AI 与导入事务；
- 长列表分块、滚动和位置恢复；
- iPhone 顶部、一级表项、弹窗和视觉层；
- 自动化测试与交付完整性。

## 一、已关闭的 3.3.1 高优先级问题

### 搜索范围

已改为范围优先搜索。普通词表、域内总表和全局总表不再先接受全局限额再过滤。AI 联想搜索使用同一范围约束。

### 全局总表代表 Entry

已取消全局总表单代表 Entry 模型。跨域同形词和短语均保留具体 Entry 行，避免繁体释义、PIN、标注、日期和关系被错误归并。

### PIN 重映射碰撞

PIN 继续绑定具体 Entry。系统总表只显示该状态；独立域顺序变化不再把 PIN 重映射到另一个同形 Entry。

### StudyStamp 孤儿与共享语义

Schema 5 将日期统一绑定具体 Entry。删除、重命名、替换和导入路径清理具体引用；旧全局日期使用保守代表 Entry 迁移。

### AI 竞态与历史

过期 AI 结果不覆盖新 Entry，也不删除旧标注。请求支持即时 Abort。批次不再逐条挤占 Undo；任务结束记录一条 Annotation 历史。

### 导入计划过期

计划携带基准 Revision，提交前检测状态变化并重新规划。

### VIX 歧义键

无法唯一解析的裸关系键不再猜测绑定，相关 Membership 被跳过并进入预检问题列表。

## 二、系统总表模型审计

### 身份边界

当前实现维持两类身份：

1. 具体 Entry：拥有文本、域、释义、个人状态和关系；
2. 规范文本组：只用于全局排序、唯一总数和部分序号。

没有新增会拥有 PIN、Annotation 或 StudyStamp 的组级实体，符合系统总表作为投影视图的规则。

### 去重边界

- 域内：同一 Entry 多 Membership 去重；
- 跨域：同形但不同 Entry 不去重；
- 全局唯一计数：按 `kind + normalizedText` 组计数；
- 页面渲染：按具体 Entry 行。

### 独立域排序

全局组内顺序读取当前独立域 order。状态不随排序迁移。自动化测试覆盖跨域词和短语以及域重排。

## 三、序号审计

实现区分：

- 字典序连续组号；
- 字母小标题内组号；
- 日期／未标注小标题内组号。

日期模式不会错误强制不同日期中的同形 Entry 共享局部序号；只有落在同一直接小标题中的同形 Entry 才共享。

## 四、跳转审计

目标列表来自实际投影可见性，而不是仅凭底层 Membership。三态判断基于来源具体 Entry 的 domainId：

- 一个同域目标；
- 一个异域目标；
- 多个有效目标。

多目标窗口使用扁平列表，并与首页“进入词表”图标分离。

## 五、移动端状态审计

### 动态字母

字母小标题使用真实 sticky 节点。折叠前后通过坐标差补偿滚动，避免内容收起时标题跳动。字母轨道仅在当前按钮完全不可见时自动跟随。

### 横滑

横滑判定从全局时间戳改为具体 Pointer 手势。节点重建和上下文变化会建立新的文本视口，从而归零 scrollLeft。

### 短语溢出

启发式仍用于初始分类以控制成本；已物化的 two-line 短语通过真实几何检查升级 extreme。未采用全库同步测量。

### 零分区

根据最终产品规则，零数量区保留。当前页面由 Store mutation 重新构建投影，可完成 0↔1 状态更新；人工清单要求在字母和日期模式实测。

## 六、视觉审计

3.4.0 独立覆盖层完成：

- 顶栏等宽三栏／独立居中；
- 顶部图标网格居中；
- 首页左侧竖线移除；
- 全局底色渐变和系统卡片渐变、阴影移除；
- 关系附着件和弹窗减重；
- 冲突来源副字位于正文右上方。

CSS 没有进行全量重写，保留 `v3.css + v3.3.1.css + v3.4.0.css` 层叠。这降低了本轮重构风险，但仍是后续维护债务。

## 七、残余风险与待真机验证

以下项目无法仅通过 Node 自动化确认：

1. iPhone Safari 的 sticky 标题推出和折叠位置补偿；
2. 动态顶部与 PIN／标注／更新提示同时出现时的实际像素关系；
3. 真机字体字宽下普通词无横滑比例；
4. two-line 短语在 Safari 字体排版下的升级阈值；
5. VoiceOver 对锚定菜单、来源副字和折叠标题的朗读；
6. Oxford／ChatGPT 外部 App 返回和 `AI查询` 快捷指令输入绑定；
7. Service Worker 从真实 3.3.1 站点缓存升级；
8. 极长列表长时间滚动后的 iOS 内存压力。

这些均已列入人工验收清单。当前交付不声称已经完成真实 iPhone 验收。

## 八、非阻断技术债

- CSS 仍为三层历史文件；未来可在功能稳定后单独合并，不应与本轮模型更新同时重写。
- `contextCollectionId` 继续作为 PIN 创建来源和排序上下文元数据，但不改变 PIN 的具体 Entry 身份。
- 跨标签页临时 UI 状态未做全量同步；项目主场景仍是单实例主屏幕 PWA。
- 外部脏 VIX 不提供自动猜测或逐项修复器，只进行安全跳过和报告。

## 最终审计结论

3.4.0 已从“代表 Entry 的全局聚合表”转为“具体 Entry 的全局投影”，关闭了 3.3.1 中最重要的身份、搜索、日期、PIN 和 AI 一致性问题。自动化测试证明核心模型和数据事务可执行；真机 sticky、字体、弹窗和外部 App 生命周期仍必须按清单验收。
