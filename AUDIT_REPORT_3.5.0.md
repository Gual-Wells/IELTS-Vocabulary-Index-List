# Vocabulary Index 3.5.0 审计报告

## 审计依据

本版以 3.4.0 完整源码、3.4.0 真机截图和约 4 分 36 秒 iOS 屏幕录制为依据。审计重点不是静态功能存在性，而是运行时布局、滚动、路由、投影、数据状态和行为状态之间的关联。

## 3.4.0 已确认的问题

1. 未标注日期节点被隐藏后，三列 Grid 仍保留，操作区进入错误列并持续占用空列。
2. 全局 `max-content` 规则破坏普通短语换行，导致词汇、短语和释义被压缩或裁切。
3. 字母展开状态晚于位置恢复，页面先定位、后插入正文，形成明显跳动。
4. Chunk 估算高度、关系展开和短语升级同时改变真实高度，缺少统一锚定。
5. 普通表两套字母导航争夺同一 sticky 位置。
6. 字母轨道平滑追赶落后于主页面滚动，选中项可能长期不在可视区。
7. PIN 与复制式动态标题覆盖主标题，并使用半透明毛玻璃表面。
8. 跨词表跳转没有可靠应用返回栈。
9. 固定弹窗在 iOS 上仍可能把滑动传递给背景页面。
10. 右侧关系轨道、外挂序号框和大圆角共同造成正文宽度不足及边缘切口。

## 3.5.0 修复策略

- 保留 Schema 5、Entry 级个人状态、全局具体 Entry 投影、计数和序号模型。
- 删除复合普通表的同页双分区运行时，改为词汇／短语独立视图。
- 恢复固定底部工具栏，让顶部字母轨道只承担字母导航。
- 一级表项改为 Flex：正文、可选日期、紧凑操作区；日期不存在时不占宽度。
- 序号改为正文前置文本；关系展开按钮移入操作区第一位；删除右侧吸附轨道。
- 真实字母标题直接 sticky，不创建复制层，不使用毛玻璃。
- 新增应用内部返回栈与页面快照；搜索、关系跳转和同页跳转使用同一路径。
- 弹窗打开时固定 body，并对 iOS touchmove 边界进行拦截。
- 字母跟随取消平滑动画，使用即时 nearest 行为和明确的 A／# 端点。
- PIN 导航改为完全不透明、直角、紧凑的底部次级栏。
- 只重绘查询弹窗内的 Oxford 与 ChatGPT 图标；一级表项查询总入口图标保持不变。

## 功能与状态关联

### 词表优先级

普通词汇继续由同域最高优先级普通词表唯一显示。优先级调整会重建投影、计数、搜索范围、关系目的地和 PIN 可见上下文，但不改写 Entry、Membership、学习日期或 AI 标注。短语继续在所有普通 Membership 中显示。

### 词汇／短语视图

普通词表的两个视图分别保存：排序模式、展开字母、关系展开、滚动快照和上次位置。系统总表按内容类型固定，切换按钮禁用。

### 页面入口

- 首页进入：顶部、全部字母折叠。
- 内部返回：恢复原页面完整快照。
- 搜索／关系跳转：只展开目标组并定位目标。
- 同页跳转：即使 URL 主体不变，也必须执行目标展开和定位。

### AI 与数据操作

AI 核查按当前视图运行；请求可 Abort；人工标注操作不会被吸收到 AI 历史；一次任务只写一条聚合 Undo。导入提交前校验基准 Revision，增量导入等待数据库事务真正结束。

## 仍需真机验证

本包已完成源码与自动测试，但以下项目必须在真实 iPhone PWA 复验：

- sticky 字母标题推出和点击折叠；
- iOS 惯性滚动中的 Chunk 锚定；
- 固定弹窗的所有滑动边界；
- 底部工具栏、PIN 次级栏和键盘安全区；
- Oxford／ChatGPT 外部返回；
- 页面淡入淡出是否产生可见闪烁。
