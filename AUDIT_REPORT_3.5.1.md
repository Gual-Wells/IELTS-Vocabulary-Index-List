# Vocabulary Index 3.5.1 审计报告

## 审计范围

- 3.5.0 真机截图反馈；
- Dialog Shell 和 iOS 模态滚动所有权；
- 字母／日期模式切换；
- 浏览锚点与页面返回栈；
- 字母栏同步；
- 一级表项序号、释义、来源、关系槽位；
- 日历月／年导航；
- 既有数据、投影、PIN、标注和学习日期关联。

## 已修复的确定性问题

### 弹窗宽度冲突

3.5.0 同时给原生 Dialog 和内部表单设置视口宽度，导致内部卡片比外层更宽。3.5.1 改为单一宽度所有者：原生 Dialog 确定最终宽度，内部卡片只填满父级。

### 模式切换灰色逻辑

3.5.0 手动切换模式时读取目标模式的旧 Last Position，造成从日期顶部切回字母模式后跳到历史词条。3.5.1 删除该隐式恢复；历史浏览锚点只由用户明确短按按钮调用。

### 自动覆盖浏览位置

3.5.0 会在滚动、复制和返回顶部时自动改写位置。3.5.1 将其改为手动浏览锚点，避免临时查看、搜索和关系跳转覆盖长期保存位置。

### 序号与释义错位

原 DOM 将释义放在整个内容容器第二行，导致释义从序号左边缘开始。3.5.1 使用 `序号 + lexeme stack`，释义属于英文栈，始终跟随英文左边缘。

### 关系槽位造成控件漂移

3.5.0 无关系条目只有四个操作槽，有关系条目有五个。3.5.1 始终保留关系槽位，无关系时为语义空占位。

### 来源标签额外增高

3.5.0 通过负 top 和额外 padding 建立顶部来源标签通道。3.5.1 改为右下元信息，和繁体释义共用双行高度。

### 字母栏滞后

3.5.0 在选中按钮离开可视区后才移动，且手势结束后的同步可能被暂停窗口吞掉。3.5.1 使用首尾警戒区提前移动，并在交互暂停结束后执行最终同步。

## 数据与业务关联结论

本版未发现需要修改 Schema 的新问题。下列规则继续成立：

- Entry 是 PIN、Annotation、StudyStamp 的状态主体；
- 系统总表是投影视图，不拥有独立状态；
- 词表优先级只改变普通词汇投影归属，不迁移 Entry 状态；
- 浏览锚点键仍按 Collection、Mode、Section 区分；
- 页面返回快照和浏览锚点为两个不同状态系统。

## 仍需真机验证

- Native Dialog 在 iOS Visual Viewport、键盘和 Home Indicator 下的完整遮罩；
- 长按浏览锚点与系统长按手势的冲突；
- 字母栏警戒区在快速惯性滚动下的跟随；
- 来源标签和超长繁体释义在窄屏下的空间竞争；
- 日历五栏按钮在最窄目标机型上的触控和布局。
