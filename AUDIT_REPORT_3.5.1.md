# Vocabulary Index 3.5.1 审计报告（对齐重做版）

## 背景

3.5.1 第一次封包通过了静态测试，但仍存在方案与实现不一致：来源标签仍为绝对定位、模式切换仍可能在日历顶部取得下方 Entry、Dialog 只是宽度补丁、长按缺少位移取消、字母栏缺少方向与滞回。本次重做逐项消除这些差异。

## 源码级结论

### Dialog

现在由全视口 Dialog 外层统一定位，卡片使用 `width: min(..., 100%)`。标题统一使用三列物理居中；内部控件不得拥有独立视口宽度。固定 Body、Touch 边界处理和 Dialog Body 唯一滚动所有权继续保留。

### 模式切换

模式转换锚点必须满足两个条件：

1. 当前投影视图的内容 Root 已进入阅读视口；
2. 阅读探针或可见 Entry 能解析到当前 Section 的真实 Entry。

日历、Large Title 和页面顶部不满足第一个条件，因此不会再隐式跳到下方第一条内容。

### 浏览锚点

锚点只允许显式长按写入。手势使用 Pointer 捕获、位移阈值、页面滚动检测和单次提交标志。自动滚动持久化只保留页面返回快照，不再写浏览锚点。

### 字母栏

字母栏采用边缘警戒、方向判定、短暂反向锁和程序滚动抑制。它不等待选中项完全离开视图，也不会把程序滚动误当成人工横滑。

### 一级表项

来源与释义现在是真实的第二元信息行，而不是绝对定位。主行与元信息行由同一 Grid 控制；日期和操作区使用显式 Grid 列，因此不存在日期缺失导致的自动换列。

## 数据与业务关联

未发现需要升级 Schema 的问题：

- Entry 仍是 PIN、Annotation、StudyStamp 的状态主体；
- 系统总表仍只是投影视图；
- 词表优先级只改变普通词汇显示归属，不迁移 Entry 状态；
- 浏览锚点按 Collection、ViewKind、Mode 独立；
- 页面返回快照与浏览锚点是两个不同状态系统。

## 自动化不能替代的真机验证

- iOS Visual Viewport 和键盘下的原生 Dialog 遮罩；
- 长按与 Safari 系统手势；
- 快速惯性滚动中的字母栏方向与警戒位；
- 超长繁体释义和长独立域名称；
- 日历五按钮在最窄目标设备上的视觉比例。
