# Vocabulary Index 3.5.2 审计报告

## 审计对象

从 3.5.1 Clean Rebuild 基线建立的 3.5.2 运行时稳定化版本。

## 变更边界

- 未改变 Schema、IndexedDB、Seed revision、VIX、词表投影、优先级占有或具体 Entry 状态归属；
- 未实现 3.6.0 查询 Provider；
- 未实现 4.0.0 Seed、非结构性 Domain、全局非结构表或新关系模型；
- 只修改运行时 UI、当前页返回快照、视觉样式、版本元数据、缓存与测试契约。

## 已修复风险

1. 来源通过隐藏释义占位扩张表项；
2. sticky 小标题存在主动 2px 接缝；
3. 长按过程中出现 Toast 并被 iOS 选中；
4. 底栏附加安全区白带及禁用分隔线灰化；
5. dialog 根层跟随 VisualViewport 导致首帧重定位；
6. 模式/视图切换使用不稳定映射和旧状态缓存；
7. 日期标题不能折叠；
8. 字母轨道依赖固定延迟恢复自动跟随，页面顶部 A 与页面底部 # 都可能在横滑后被立即吸回；
9. Query/Relation 菜单未统一避让底部停靠栏；
10. Overlay 几何更新存在重复 RAF 调度。

## 仍需真机验证

- 固定 58px 底栏是否与 iPhone 17 Home 手势冲突；
- native dialog 在 iOS standalone 下是否完全消除白带和首帧抖动；
- 字母轨道人工锁在快速惯性滚动下的解除时机，以及页面顶部 A／页面底部 # 的边界回弹是否都不再强制归位；
- sticky 边界是否在状态栏、更新 Banner、横竖屏变化下保持零缝隙；
- 长按 20 次是否完全不再触发系统选择。

## 结论

静态与自动化验证是交付门槛，但本版的核心价值必须由真机验收决定。若上述任何一项未通过，应以 3.5.2 为单一修复基线继续收口，不提前进入后两次暂定更新。
