# Vocabulary Index 2.2.1 缺陷审计与修复报告

## 结论

本轮不沿用云备份版继续修补，而是回到本地优先架构，删除整套 GitHub 同步状态机，并重新检查本地数据库、撤销、导入、字母渲染、AI、PWA 和多实例之间的耦合。

经修复后，没有发现仍然可由静态代码和纯模块测试确认的 P0/P1 数据破坏缺陷。剩余不确定性主要位于 iPhone Safari 真机生命周期和极端 50,000 词 DOM 性能，已列入人工验收边界。

## 已删除的高复杂度功能

完整删除：

- GitHub PAT；
- 私人备份仓库配置；
- GitHub Contents API；
- `latest.json`；
- 云端 snapshots；
- revision/parentRevision；
- 自动同步、防抖同步和重试；
- 远端冲突、隔离和恢复状态机。

运行时代码中不存在云同步模块或 GitHub API 地址。

## 高优先级耦合缺陷及修复

### 1. 同版本 Service Worker 缓存冲突

**问题：** 云备份版已经使用 2.2.0。若本地回退版仍使用相同 APP_VERSION 和 CACHE_NAME，旧云模块可能继续留在同名 Cache Storage 中，设置页也无法区分两套架构。

**修复：** 最终版本提升为 **2.2.1**，缓存名改为 `gual-vocabulary-index-v2.2.1`。新 Service Worker 激活时只删除本应用旧版本缓存。

### 2. 云版敏感配置残留

**问题：** 仅删除 UI 和模块不会自动删除此前写入 localStorage 的 GitHub Token，也不会删除 IndexedDB 中的云状态。

**修复：** 启动时执行一次兼容清理：

- 删除 GitHub Token、owner、repo、branch、device 和 auto-sync localStorage 键；
- 删除 IndexedDB 中所有退休的 `cloud*` 设置；
- 不影响词库、PIN、AI 标注、撤销历史和 Groq Key。

### 3. 人工改名与来源关系失配

**问题：** 旧逻辑将 `access` 人工改为 `accessible` 时，只改变显示词和 normalizedWord，来源仍保存 `access`。之后再导入 `accessible` 来源，会形成同一词条中互相不一致的来源词形，导致完整备份校验失败。

**修复：** 人工改名会同步更新该词条的全部来源词形。启动迁移还会自动修复旧版本已经产生的此类数据。

### 4. AI 标注与撤销/重做不对称

**问题：** 撤销后的通用清理器曾根据“词条语义发生变化”主动删除标注，但删除本身不属于原历史事务，随后重做无法恢复，造成撤销/重做往返不等价。

**修复：** 语义变更对应的标注删除必须由原始业务事务显式记录。历史后清理只修复不存在的词条或错误归属，不再擅自改变备份明确保存的标注。

### 5. 回到前台时无变化也关闭编辑框

**问题：** iPhone 从牛津辞书或其他 App 返回时，`visibilitychange` 无条件重载 IndexedDB，并触发 UI 关闭所有修改对话框，即使没有任何其他实例写入。

**修复：** 前台恢复先轻量比较 `dataRevision` 和序号模式；只有确实发生变化才重载和关闭陈旧对话框。

### 6. 多实例陈旧写入

**问题：** Safari 标签页和主屏幕 PWA 共享 IndexedDB，但拥有各自内存状态。旧内存若直接提交，可能覆盖另一实例刚完成的修改。

**修复：** 每次业务事务同时验证：

- 全局 `dataRevision`；
- 每个实体的 before 快照；
- 撤销历史指针。

冲突操作会被取消并重新载入，不会静默覆盖。BroadcastChannel 负责及时通知，前台检查作为补充。

### 7. 批量词形交换的唯一索引冲突

**问题：** 在同一事务中直接 put 两个互换 normalizedWord 的词条，会被 IndexedDB 唯一索引视为瞬时重复。

**修复：** 批量 entries 事务先删除全部受影响 key，再写入最终值。

### 8. 历史清理与业务提交耦合

**问题：** 业务事务已成功后，若后续历史裁剪失败，调用方仍可能收到失败并误以为业务没有提交。

**修复：** 业务提交与历史软容量裁剪分离；裁剪失败只记录警告，不反转已经完成的数据事务。最新大型事务始终至少保留一条，以便撤销恢复或初始化。

## 备份与导入修复

- 完整备份上限从 100,000 调整为产品实际目标 50,000。
- JSON 规范化改为字段白名单，不再把未知嵌套字段写入数据库。
- PIN 缺失 createdAt 时补齐确定性时间字符串，避免排序崩溃。
- PIN、AI 标注数量受词条上限约束。
- AI 标注对象、建议长度、说明长度和词性建议均校验。
- 词表名称采用 NFKC、忽略大小写和空格合并后检测重复。
- CSV 未闭合引号会被拒绝。
- `#hashtag` 不再被当作 Markdown 标题。
- 启动失败恢复入口也执行 64 MB 文件限制。
- 恢复和 seed 初始化使用正常历史事务，可撤销和重做。

## 字母目录与渲染

保留 2.1.x 重建后的确定性 accordion：

- 不使用 `<details>` 和异步 `toggle`；
- 普通进入全部收起；
- 搜索、PIN 和明确跳转只展开目标字母；
- 每次渲染拥有 generation token；
- 词条成功插入 DOM 后才标记分组已填充；
- 旧页面任务无法修改新词表。

## AI 与异步状态

- 中文搜索输入或范围变化时取消旧请求。
- AI 新增输入变化后清除旧候选，不能提交陈旧结果。
- AI 核查按批次验证词条快照；词或词性已变化的结果不会写入。
- AI 核查只生成标注，不直接修改词汇。
- 外部实例改变数据后会关闭陈旧搜索、导入、编辑和 AI 对话框。

## 仍需真机确认的边界

当前环境无法完成可用的 Chromium 浏览器自动化，因此以下项目不能伪称已通过：

- iPhone Safari 主屏幕 PWA 的剪贴板权限和 fallback；
- iOS 下载完整 JSON 到“文件”的实际行为；
- Safari 后台冻结、进程终止及恢复；
- Safari 标签页与主屏幕 PWA 的真实 BroadcastChannel 行为；
- 飞行模式冷启动；
- 极端 50,000 词集中在同一个字母时的展开性能。

相关步骤见 `tests/MANUAL_CHECKLIST.md`。
