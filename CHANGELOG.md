# Changelog

## 2.4.0 UI & AI Workflow — 2026-07-31

### 首页与设计语言

- 重构 iPhone 与桌面首页的信息层级：词表为主体，数据维护操作降为次级区域。
- iPhone 使用紧凑双列词表卡片并兼容极窄屏单列；桌面端使用自适应栅格。
- 统一卡片密度、触控尺寸、按钮层级、留白和状态提示。
- 首页与词表页增加待审阅 AI 标注计数入口。

### Groq 模型目录

- 将已发现模型目录、最近活跃模型和刷新时间持久化到 localStorage。
- 设置页只读取本地目录；只有用户点击“刷新模型列表”时才请求 Groq。
- 手动刷新会合并而非覆盖历史目录；最近未返回的模型保留并标记状态。
- AI 运行策略不按模型品牌或模型 ID 硬编码分支；特定 Qwen 兼容问题在本版明确跳过。

### AI 核查任务

- 按词条数量与估算输入 token 动态分批，串行发送核查请求。
- 读取 token 和 request 速率限制响应头，在接近额度边界时主动等待。
- 对 429、暂时性 5xx、网络错误和超时执行有限重试，并优先采用服务端 retry-after/reset。
- 增加进度、批次、标注数量、暂停、继续和取消。
- 每批完成后立即提交稳定词条的标注；取消或后续失败不会清除已完成结果。
- 将冗长 API 错误转换为简洁中文用户提示，同时保留结构化错误供任务控制。

### 标注审阅

- 增加全局和当前词表标注审阅入口。
- 增加上一条、下一条、编辑、取消标注和退出操作。
- 审阅可跨词表、跨字母自动导航，展开目标分组、滚动并高亮词条。
- 修改或删除目标后自动剔除失效标注并继续审阅。

### 核验

- 增加模型目录持久化、动态分批、token/RPM 等待、429 结构化错误和标注审阅静态回归测试。
- 增加“不得按 Qwen/Llama/OSS 名称写策略分支”的静态约束。

## 2.3.0 Local Stable — 2026-07-31

### 架构

- 完整撤销 GitHub 私人仓库云备份、PAT、latest、snapshot、revision、自动同步及冲突状态机。
- 恢复“IndexedDB 本地工作数据库 + 手动完整 JSON 备份”的单机架构。
- 首页显式提供导出 JSON、恢复 JSON 和初始化 seed。
- 升级时自动清除云备份版遗留的 GitHub Token、仓库配置和 IndexedDB 云状态。

### 数据一致性与耦合修复

- 为所有业务事务增加全局 `dataRevision` 和实体前置快照校验。
- 增加 BroadcastChannel 与前台修订检查，防止 Safari 标签页和主屏幕 PWA 的陈旧状态静默覆盖。
- 前台恢复时仅在数据或序号设置实际变化后重载，避免无变化时关闭编辑对话框。
- 修复人工改名只改变显示词、未同步来源词形，导致后续导入和备份验证失败的问题。
- 增加旧人工改名数据的一次性来源修复迁移。
- 修复恢复事务中 AI 标注清理与撤销/重做不对称的问题。
- PIN 顺序在移动归属、删除和恢复后统一重新编号。
- 修复快速连续固定 PIN 可能生成重复记录的问题。
- 修复批量词条交换 normalizedWord 时唯一索引的瞬时冲突。
- 修复历史清理失败导致“数据库已提交但界面报告失败”的耦合问题。
- 修复恢复事务与随后独立序号设置之间的撤销冲突；序号模式按软历史状态处理。
- 完全相同的重复新增和导入不再仅因时间戳变化产生无意义历史记录。

### 导入、备份和恢复

- 完整备份上限与产品目标统一为 50,000 个全局唯一词条。
- JSON 备份增加结构、来源、归属、词性、PIN、标注和设置一致性验证；人工词形不能绕过来源规范身份校验。
- 规范化备份只输出白名单字段，丢弃未知字段并补齐安全时间字符串。
- 修复缺失 PIN 时间导致排序崩溃的问题。
- CSV 未闭合引号会被拒绝。
- `#hashtag` 等没有标题空格的词汇不再被误判为 Markdown 标题。
- 启动失败恢复入口同样执行 64 MB 文件限制。
- JSON、Markdown、CSV 和 TXT 均先解析预览，再提交事务。

### UI、PWA 和 AI

- 普通进入词表时所有字母默认收起；搜索、PIN 和明确跳转才展开目标字母。
- 字母目录使用显式 accordion，不使用原生 `<details>` 异步状态机。
- 增加 render/navigation generation token，陈旧渲染任务不能操作新页面。
- 外部数据变更会关闭具有陈旧结果的搜索和修改对话框。
- 中文搜索、AI 新增和 AI 核查均取消陈旧请求并验证返回结果；AI 布尔字段采用严格类型判断。
- Clipboard API 失败时使用兼容复制路径，并保证临时节点清理。
- 对话框快速重复触发不会重复 `showModal()`；Groq 本地设置失败会显式报告。
- 规范化新增不可见字符、双向控制字符、Unicode 连字符和首尾空格边界处理。
- 延长下载 Blob URL 生命周期，降低 iPhone PWA 导出空文件风险。
- Service Worker 采用版本化完整预缓存与当前版本 cache-first；安装时使用 `cache: reload` 避免 HTTP 缓存造成新旧模块混用。

## 2.1.1

- 普通进入词表时改为全部收起。

## 2.1.0

- 重建确定性字母 accordion，删除 2.0.x 的 `<details>`/`toggle` 状态竞争。

## 2.0.x

- 初始模块化、IndexedDB、PWA 和全局唯一词汇模型版本；已由 2.3.0 取代。
