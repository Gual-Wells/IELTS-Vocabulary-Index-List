# Vocabulary Index 2.4.0 — 变更与耦合修复报告

## 范围

本版不处理特定 Qwen 模型在 Groq JSON Object Mode 下的生成失败，也没有添加任何基于模型品牌或模型 ID 的兼容分支。整改集中于已经确认的四个方向：

1. 首页和整体交互设计；
2. Groq 模型目录持久化；
3. 大规模 AI 核查的批次、速率和失败控制；
4. AI 标注的集中审阅和定位。

## 1. 首页 UI

### 原问题

- 首页功能和维护信息平铺，产品主任务不突出。
- iPhone 卡片尺寸与信息密度不协调；桌面端同样缺少稳定的层次。
- AI 标注缺少可见入口。

### 处理

- 词表卡片前置为首页核心内容。
- 维护操作缩减为底部轻量工具区。
- iPhone 默认双列，极窄屏单列；桌面自适应栅格。
- 重建标题、统计、箭头、卡片反馈、按钮尺寸和间距层级。
- 首页增加全局 AI 标注计数，词表页增加当前词表计数。

## 2. 模型目录

### 原问题

- 可用模型每次使用前需要重新请求。
- 选择器只稳定保留默认模型和当前模型。
- 暂时未返回的模型会消失，模型目录缺乏历史连续性。

### 处理

- localStorage 新增三个独立值：历史模型目录、最近活跃模型、最近刷新时间。
- 设置页打开时只读缓存，不触发网络。
- 手动刷新将活跃列表与历史目录合并。
- 当前模型即使尚未刷新也会进入目录。
- 最近刷新未出现的历史模型保留并标记，不自动删除。
- 请求策略保持模型无关；没有 Qwen、Llama、OSS 等名称判断。

## 3. AI 核查任务

### 原问题

- 大词表一次或固定大批请求容易触发 TPM/RPM。
- 失败提示直接暴露冗长 API 原文。
- 核查缺少暂停、取消、批次进度和部分结果保护。

### 处理

- 依据词条数和紧凑请求体长度估算输入 token，动态形成批次；单批另有硬上限。
- 请求严格串行，避免并发占用额度。
- 每个成功响应读取 token/request 的 limit、remaining、reset 及 retry-after。
- 下批预计消耗超过 token 剩余额度，或 request 剩余接近耗尽时，主动等待相应 reset。
- 429、5xx、网络错误和超时采用有限次数重试；不可重试错误立即终止。
- 进度显示已完成批次、总批次、当前标注数和等待原因。
- 支持暂停、继续和取消；暂停在当前请求或等待阶段结束后阻止下一批开始。
- 每批成功后立即把该批标注写入 IndexedDB；后续失败或取消不回滚已完成结果。
- 写入前再次比较词形和词性快照，避免人工编辑后写入陈旧标注。

## 4. 标注审阅

### 原问题

核查完成后，用户只能在大词表中自行寻找黄色标注，缺少完整审核流程。

### 处理

- 新增全局和当前词表审阅入口。
- 审阅条目按词表优先级和词形排序。
- 上一条/下一条支持跨词表、跨字母。
- 利用现有确定性导航打开目标词表和字母，确保目标行已渲染后滚动并高亮。
- 审阅栏展示序号、词形、词性、拼写建议、词性建议和理由。
- 支持打开人工编辑、取消当前标注和退出。
- 编辑、删除或外部变化导致标注失效时，审阅列表重新过滤。
- 审阅期间临时隐藏 PIN 条，避免两个 sticky 工具条冲突；退出后恢复。

## 5. 未改变的边界

- IndexedDB 本地优先与手动 JSON 备份架构不变。
- 不恢复 GitHub 云备份、PAT、远端快照或账号系统。
- AI 核查仍只写标注，不直接修改词条。
- 普通进入词表仍默认全部收起。
- Oxford 图标不变。
- 特定 Qwen 兼容问题在本版明确跳过。

## 6. 已知边界

- AI 核查任务队列本身是页面会话状态；浏览器进程被系统终止后不会自动恢复未完成批次。已完成标注已经持久化，可以重新启动核查或直接审阅。
- 客户端 token 估算是保守近似，最终额度以 Groq 响应头和服务端错误为准。
- 无真实 iPhone Safari、真实 Groq Token 和可访问的自动化浏览器环境，因此仍需按真机清单验收。
