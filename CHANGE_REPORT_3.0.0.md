# Vocabulary Index 3.0.0 RC 变更报告

## 核心重构

- 新增 Domain、Collection、Entry、Membership、PhraseToken 数据模型。
- 唯一性从旧版“全局规范词形”改为“词域 + 规范词形”。
- 每个词域自动建立唯一系统短语表。
- 词性降级为 Membership 的可选 `sourceLabel`。
- 按用户确认修改设计：Membership 删除冗余 `sourceText`，英文只存于 Entry。

## 短语

- 多词表达成为一等 Entry。
- 建立确定性 PhraseToken 索引。
- 普通词 → 相关短语、短语 → 已收录组成词双向跳转。
- 精确词元匹配，`thread` 不会误匹配 `threading`。

## 释义与搜索

- 每个词域可独立启用通用繁体释义。
- 简体输入在本地规范化为繁体。
- 英文与简/繁中文搜索。

## 迁移与数据安全

- IndexedDB 升级到版本 3。
- 旧数据在单事务中转换到新仓库。
- 写入带修订号冲突检查和 BroadcastChannel 失效通知。
- 写事务避免在 IDB 读取与写入之间 `await`，降低 Safari 自动提交风险。
- 完整恢复执行严格结构和关联验证，并与撤销/重做一样执行跨实例修订号冲突检查。

## 旧功能适配

- PIN、PIN 顺序、上次位置、复制、编辑、删除、来源回落。
- 保留“不显示 / 字母分组 / 词表全局”三种序号模式。
- 撤销/重做。
- TXT/Markdown/CSV/JSON 导入和完整 JSON 恢复。
- AI 新增、动态 Groq 模型目录、分批核查、暂停/继续/取消、部分结果保留、词表内与首页全局集中审阅。
- PWA 离线壳和旧/新版本混用阻断。

## 明确不包含

- GitHub PAT、私人云备份、自动云同步、账号和后端。
- 针对某个具体 Groq 模型的硬编码策略。
- 自动词形还原、跨词域短语关联、例句、音标和完整词典。
