# Vocabulary Index 真 3.0.0 制作变更报告

日期：2026-08-01

## 1. 制作基线

- 数据、迁移、AI 和视觉基线：3.0.0 RC；
- 连续浏览行为参考：2.4.1；
- RC2：仅作为反向回归样本；
- 产品规格：`Vocabulary_Index_3.0_True_Preproduction_Report_2026-08-01.md` 的议定结论。

## 2. 核心交互重建

### 浏览主平面

- 词条主体固定为复制英文，不随模式改变；
- 主列表仅显示英文、当前词表词性和条件状态；
- 字母展开只局部创建或移除目标分组 DOM；
- 搜索、PIN、上次位置和标注统一使用单次局部定位；
- 跳转不调用整表 `renderCollection()`。

### PIN

- 从普通顶部工具区移入 Topbar 下方 sticky 上下文控制器；
- 上一个、当前重新定位、下一个始终可连续操作；
- 跳转后控制器不随内容滚出视口；
- 与 AI 标注审阅互斥，退出审阅后恢复。

### 维护层

- 移除永久五项移动底栏；
- 新增、AI、导入、导出、撤销和管理统一进入词表更多菜单；
- 词条动作菜单与只读详情分离；
- 危险操作使用独立确认对话框；
- 搜索使用独立搜索层，不复用编辑表单。

### 连续位置

- 复制成功立即记录上次位置；
- 手动滚动防抖记录稳定可见词条；
- 普通进入词表不自动抢占滚动；
- 用户主动恢复时展开目标字母并单次定位。

## 3. AI 与任务

- 模型目录持久合并，只有主动刷新才联网；
- 不按模型品牌写兼容分支；
- 核查启动前显示范围、词项、批次和模型；
- 运行任务支持暂停、继续、取消和收起；
- 每批结果立即提交，取消保留已完成标注；
- 标注审阅恢复上一条、下一条、编辑、取消和退出。

## 4. PWA 更新

- Service Worker 安装阶段不再自动 `skipWaiting()`；
- 新版本准备好后显示用户可控更新提示；
- 用户点击“立即更新”后才发送 `SKIP_WAITING`；
- `controllerchange` 后只重新载入一次。

## 5. 数据规则保留与补强

- Schema 3、Domain、Collection、Entry、Membership、PhraseToken 全部保留；
- 同一词域内 Entry 唯一；
- 同词不同词性合并到 Membership `sourceLabel`；
- 当前词表主行只显示当前 Membership 标签；
- 2.4.1 Seed、七份源词表和图标不修改。

## 6. 新增文档

- `PRODUCT_MANUAL_3.0.0.md`；
- `CHANGE_REPORT_3.0.0_TRUE.md`；
- 更新后的测试报告、README、部署说明和人工验收清单。
