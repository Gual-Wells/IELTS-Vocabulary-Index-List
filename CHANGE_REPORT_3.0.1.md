# Vocabulary Index 3.0.1 交互重构报告

## 根因

3.0.0 将底层 Entry、Membership 和 Phrase 结构直接暴露到 UI，造成短语污染普通词表、计数错误、详情与来源信息过载、位置语义冲突和管理层级冗长。

## 主要修复

- `buildProjection()` 对词汇和短语严格分流；
- 普通词表与短语表改为同构字母列表；
- 行内展开双向关系，无标题注释；
- PIN 改为行内直接操作；
- 删除独立详情弹窗；
- 词性从全部用户界面隐藏；
- 上次位置按 Collection 验证并恢复；
- 搜索改为全部 / 词域 / 词表三级；
- 管理页改为拖动排序；
- App Dialog 增加栈式返回；
- 搜索层增加背景滚动锁和 Visual Viewport 适配；
- 首页卡片压缩高度并消除“宽额头”；
- 删除“词项”等重复说明文字；
- 增加一次性旧缓存升级引导；版本和 Service Worker 缓存升级为 3.0.1 独立构建，并以同代 App Shell 原子切换，避免 HTML/JS 混装。

## 数据影响

不删除原 Entry、Membership 或词性数据。变化发生在投影和用户界面：

- 4,995 个词汇；
- 10 个短语；
- 普通词表只投影词汇；
- 短语表只投影短语。
