# Vocabulary Index 3.0.2 变更报告

## 目标

3.0.2 针对 3.0.1 真机截图暴露出的用户层问题进行结构性修正，而不是继续堆叠补丁。

## 已修正

- 空关系面板不再被 DOM 转换为可见的 `null` 文本。
- PIN 点击立即反馈，后台写入完成后只更新当前行与 PIN 控制器，不再整页重绘。
- 字母展开、字母跳转、搜索结果跳转和关联跳转取消平滑等待，改为即时定位。
- 搜索不再自动唤起键盘；键盘开合仅调整搜索层高度，不推动背景页面。
- 短语表采用平面列表，不再生成无意义的首字母区块标题。
- 词汇与短语行均提供独立关联展开按钮；子项点击会跳到对应短语表或词域总词表。
- 空关系行保留无视觉占位，以稳定 PIN 与更多按钮的位置。
- 新增全局总表：对所有词域中的普通词按规范英文全局去重。
- 每个词域新增派生总词表：显示该词域全部普通词，不受普通词表优先级分配影响。
- 全局总表和词域总词表均为派生视图，不写入 Collection、Membership 或 Seed。
- 总表拥有独立 PIN 和上次位置上下文。
- 多实例写入冲突会自动同步并重试一次，不再直接弹出大面积红色工程警告。
- 移动端触控后不再残留被裁切的焦点框；管理界面移除多层嵌套边框。
- 首页总数改为全局唯一普通词数量。
- Service Worker 与升级桥使用同一构建缓存名。

## 数据投影

- 全局总表：4,995 个全局唯一普通词。
- 通用英语总词表：4,995 个域内唯一普通词。
- A1：894。
- A2：786。
- B1：691。
- B2：1,294。
- C1：1,277。
- AWL：53。
- AVL：0。
- 短语：10。

普通词表仍按拖动顺序决定唯一词汇的首个投影位置；总词表不参与优先级竞争。
