# Vocabulary Index 3.0.3 变更报告

3.0.3 是纯性能修复版本，不新增产品功能，也不调整 3.0.2 已议定的界面结构。

## 根因

3.0.2 的关系按钮在渲染每个表项时调用 `backupFromState()`，对 5,005 个 Entry、6,407 个 Membership 等数据执行整库 `structuredClone`。展开一个含 50 个词的字母会连续复制整库约 50 次。搜索每次输入也复制整库并进行全量模糊扫描；PIN 则通过通用 `mutate()` 重建全部短语词元、投影和全库差异。

## 修正

- Store 初始化时一次性构建词汇—短语、短语—组成词、同形词索引。
- 高频关系读取直接访问索引，不创建完整备份。
- 搜索直接读取只读 Entry 数组，连续输入以 140ms 合并。
- PIN 使用单记录事务并局部更新内存状态。
- 低频编辑、导入和结构管理暂时保留完整一致性事务。

## 边界

本版解决主线程阻塞根因。真实 iPhone 仍需确认搜索键盘动画与 Safari/PWA 的系统级行为，但不应再出现由整库复制造成的“网页未响应”。
