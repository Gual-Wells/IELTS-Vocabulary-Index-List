# Vocabulary Index 3.0.4 变更报告

3.0.4 在 3.0.3 性能基线上重构导航反馈与词汇—短语关系，不改变 Schema 3 数据格式。

## 新增

- 全局短语表：跨词域去重的短语派生视图。
- 全局层固定“全局总表、全局短语表”。
- 词域层固定“总词表、短语”前两位。
- 繁体释义行内显示。
- 字母栏首位永久准星按钮。
- 统一 SVG 图标系统。
- 跳转阅读锚点、整行高亮和 PIN 持续选中状态。

## 行为修正

- 短语组成词进入优先普通词表，不再进入词域总词表。
- 关系子项文字始终复制；跳转使用独立按钮。
- 全局总表中的相关短语进入全局短语表。
- 目标已在视口内时不发生无意义滚动。
- 搜索关闭与跨表导航按视口恢复顺序执行，减少 iOS 键盘与跳转竞争。

## 性能约束

- 全局短语代表项在 Store 初始化时建立索引。
- 高频关系读取继续使用内存索引。
- 未恢复 3.0.2 的整库复制、整库投影或按键级同步重算路径。
