# Vocabulary Index 3.0.6 变更报告

## 1. 计算机术语分类

“计算机术语”保留固定的总词表和短语表，并新增四个用户可见普通词表：

| 普通词表 | 数量 |
|---|---:|
| 计算机基础与系统 | 214 |
| 软件开发与数据 | 197 |
| 网络、云与安全 | 114 |
| 人工智能 | 19 |

544 个普通词各自只拥有一个用户可见主分类 Membership。隐藏的“内置来源”仍保存来源标签，但不参与用户可见投影、管理、搜索范围或跳转目的地。

## 2. 数据交换中心

首页设置新增“数据交换”入口。面板使用四项组合选择：

- 功能：导入内容、导出内容、导出完整备份、恢复完整备份；
- 范围：全局、独立域、词表；
- 目标：现有或新建独立域 / 词表；
- 写入方式：增量合并、完整替换。

局部词表选择只列出真实 Collection；全局总表、全局短语表和词域总词表是派生视图，不作为直接写入目标。

## 3. VIX JSON

新增单一内容交换格式 `vix-json` version 1。它只描述词域、词表、词汇、短语、Membership、释义和来源目录。PIN、上次位置、标注、UI 设置、模型设置和撤销历史不进入内容文件。

完整备份仍使用 Schema 3，专门用于设备迁移与灾难恢复。

## 4. 安全与性能

- 文件上限 64 MB；
- JSON 解析和差异计算在模块 Worker 中完成；
- 预检完成前不修改 IndexedDB；
- 内容导入前自动下载当前完整恢复备份；
- 导入以一次完整事务恢复提交，不逐条刷新 UI；
- 增量合并不执行隐式删除；
- 完整替换仅作用于选定范围；
- 释义冲突可统一保留当前值或使用导入值；
- 文件目标和面板目标不一致时必须明确选择。

## 5. 升级

`builtInSeedRevision` 从 1 升至 2。既有 3.0.5 数据库会补建四个分类词表和 544 条可见分类 Membership；原有 Entry、短语、PIN、标注、浏览位置和用户自建内容不被重复插入。
