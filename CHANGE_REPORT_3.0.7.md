# Vocabulary Index 3.0.7 变更报告

日期：2026-08-01

## 1. 关联展开项显示繁体释义

当当前词域开启繁体释义时，一级表项与关联展开项采用同一显示规则：

- 词汇展开出的相关短语显示对应繁体释义；
- 短语展开出的组成词汇显示对应繁体释义；
- 全局总表与全局短语表按词域顺序选择首个已启用且非空的繁体释义；
- 未开启繁体释义的词域不显示占位文字；
- 英文子项仍负责复制，独立跳转按钮仍负责导航。

关联释义保持单行截断，不增加额外说明标题。

## 2. 数据交换中心恢复 Seed 初始化

“首页 → 设置 → 数据交换 → 功能”新增：

```text
还原到 Seed
```

执行规则：

1. 用户必须勾选“确认还原初始数据”；
2. 应用先自动下载当前完整恢复备份；
3. 以单次 IndexedDB 恢复事务载入当前版本随包发布的 `data/seed.json`；
4. 清空撤销历史并重新建立内存索引；
5. 返回首页。

该操作会重置词域、词表、词汇、短语、PIN、浏览位置、标注与显示设置。Groq API Key 继续遵循独立安全存储策略，不由 Seed 内容包管理。

## 3. 兼容性

- Schema 仍为 3；
- `builtInSeedRevision` 仍为 2；
- Seed 内容及计算机术语四大分类不变；
- VIX JSON Schema 不变；
- 3.0.6 数据可直接升级，不执行额外内容迁移。
