# Vocabulary Index 3.3.0 变更报告

## 1. 版本定位

3.3.0 是 iPhone 主屏幕 PWA 的页面外壳、浮层系统、一级表项和标注状态重构版本。

本版不改变：

- IndexedDB Schema 4；
- 内置 Seed revision 3；
- Domain、Collection、Entry、Membership、PhraseToken 和 StudyStamp 数据语义；
- VIX 内容交换格式；
- Oxford URL Scheme；
- ChatGPT `AI查询` 快捷指令的 URL 文本传输协议；
- 词汇、短语、关系和繁体释义业务内容。

`data/seed.json` 仅将 `appVersion` 从 3.2.0 更新为 3.3.0；排除该字段后，Seed 与 3.2.0 完全一致。

## 2. 页面外壳

- 顶部导航固定于 iOS 安全区以下；
- 页面背景延伸到状态栏区域，实际控件不侵入状态栏或灵动岛；
- 首页和词表使用滚动大标题＋固定紧凑标题；
- 深滚动后返回、搜索和更多仍然可用；
- 冷启动首页从顶部开始；
- 应用内返回首页恢复离开前的位置；
- 词表返回顶部按钮常驻，到达顶部时仅降低强调度。

## 3. 滚动和跳转

- 固定功能按钮与横向字母轨道拆分，消除重叠虚影；
- 字母、年份、月份、日期、未标注和分区标题统一定位到动态顶部下方；
- 搜索、PIN、关系和上次位置条目定位到顶部以下的阅读区域；
- Visual Viewport 同时读取高度、`offsetTop` 和 `offsetLeft`；
- 弹窗不再通过 `body { position: fixed }` 改写页面坐标；
- 页面外部应用返回、键盘开合、PIN 状态和查询菜单不应改变当前滚动位置。

## 4. 浮层和弹窗

建立不同几何：

- 小型居中确认窗口；
- 有明显左右留白的表单卡片；
- 有边距的底部操作窗口；
- 受 Visual Viewport 约束的搜索工作区；
- 指向查询按钮的双图标锚定菜单。

所有含输入框的窗口打开时聚焦关闭按钮或窗口容器，不自动选择输入框，也不自动唤起键盘。

## 5. 一级表项

### 5.1 固定外层几何

所有一级表项统一使用：

```text
[一级表项主体][右侧关联附着轨道]
```

右侧轨道始终预留。存在关联时显示细长附着件；无关联时不渲染附着件，但一级表项主体的文字区、日期区、控件区、间距和字体均保持一致。

关联附着件：

- 与主体无缝连接；
- 右上、右下圆角；
- 使用统一图标系统中的定制方向图标；
- 收起态向右，展开态向下；
- 不使用简单 CSS 三角形代替；
- 展开只局部生成二级关系内容。

### 5.2 普通词汇

- 英文固定一行；
- 繁体释义打开时为第二条小号副字，也固定一行；
- 英文和释义共享同一个横向滚动区域；
- 不换行、不省略、不缩小字号；
- 文字溢出不压缩日期或操作控件；
- 轻点复制，实际横向滑动后不触发复制。

### 5.3 短语

- 常规短语单行；
- 较长短语缩小字号并最多两行；
- 两行短语可提高表项高度并压缩自身控制区；
- 极限短语采用约标准表项 1.5 倍高度：上部为短语＋繁体释义共同横滑区，下部只渲染实际存在的日期和控件并平均分布；
- 未显示的日期或功能不生成空占位。

### 5.4 控件

一级表项固定高频操作：

- 学习日期刷新；
- PIN；
- 查询；
- 更多。

Oxford 和 ChatGPT 不再分别常驻。点击查询按钮后显示只含两个图标的锚定菜单。

序号显示为贴附于一级表项左边框、向外轻微突出的独立小框。

## 6. 标注

- 被 AI 标注的一级表项使用保留米白表面和边缘留白的红色渐变警告；
- 点击标注条目主体进入标注处理，不复制可能有问题的英文；
- PIN、查询、更多和关系附着件仍执行独立动作；
- 保留上一条／下一条连续标注审阅；
- 审阅条增加“撤销当前词表全部标注”；
- 首页存在未处理标注时显示黄色覆盖警告条；
- 黄色警告条主体无交互、无关闭按钮，只提供“全局全部撤销”；
- 全局撤销立即执行，不显示确认窗口；
- 标注清空后警告条自动消失。

## 7. 高危操作

“还原到 Seed”改为：

1. 自动生成并发起完整备份下载；
2. 关闭数据交换窗口；
3. 显示小型确认窗口；
4. 用户点击确认后执行 Seed 替换。

删除复选框、确认文字输入和按钮许可门槛。完整备份恢复和 VIX 完整替换同样先发起当前状态备份下载，再用独立小型确认窗口确认。界面只声明“备份下载已发起”，不声称浏览器已确认文件安全保存。

## 8. 图标系统

统一重绘主要图标：

- 返回、搜索、更多、关闭、返回顶部；
- 字母、日期、未标注、日历切换；
- 学习日期刷新、PIN、查询；
- Oxford、ChatGPT；
- 关联方向和二级跳转；
- 警告、撤销、拖动和新增。

统一 24×24 设计画布、基础线宽、端点、转角、视觉重心、按下态、选中态、禁用态和警告态。

## 9. 性能保护

- 保留 42 条一块的惰性物化；
- 根据普通词、两行短语和极限短语分别估算占位高度；
- 保留关系内存索引和惰性关系解析；
- PIN 与学习日期继续使用局部记录事务和局部行替换；
- 查询菜单只创建两个按钮；
- 普通词横滑完全由 CSS 原生滚动处理；
- 短语显示类型只在渲染时进行一次轻量文本单位估算；
- 不在滚动路径复制整库、重建整页或持续测量每个词条。
