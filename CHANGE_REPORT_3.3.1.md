# Vocabulary Index 3.3.1 变更报告

## 1. 版本定位

3.3.1 是 3.3.0 的真机缺陷修正版。数据 Schema 和产品业务模型不升级，重点修复虚拟总表、数据软引用、状态切换、AI 标注、导航、性能和 iPhone 视觉收口。

## 2. 功能修复

### 上次位置

- 全局词汇／短语总表始终使用 `global` 作用域读写位置；
- 去重缓存加入 Collection、模式、分区和 Entry；
- 同一 Entry 在不同投影视图中不再互相吞掉位置写入；
- 删除或替换后清理失效位置。

### 跳转与关系

- 程序化跳转在 Store mutation 后保留；
- 标题仍贴动态顶部，条目仍定位到上部阅读区；
- 关系跳转只展示实际可见普通表；
- 关系展开状态按词表隔离；
- 二级关系继续保持“文字复制＋独立内部跳转”。

### PIN

- 行内激活态按当前词表上下文判断；
- 在另一个投影视图设置 PIN 时，将该 Entry 的 PIN 移到当前上下文；
- 全局代表实例变化后尝试保持全局 PIN 语义。

### 标注

- iPhone 标注审阅恢复编辑和逐条撤销；
- 当前词表审阅不再无故跳到域内总表；
- 全局视图按规范英文聚合跨域标注；
- 当前词表全部撤销和首页全局全部撤销使用局部事务；
- 被标注表项主体点按进入处理，独立控件保持自身行为。

### AI 核查

- AI 批次写入使用 Entry `updatedAt` 和 `normalizedText` 快照；
- 危险数据变更、Undo／Redo 前取消正在运行的任务；
- 标注批次不再复制整库或重建所有索引；
- 任务身份失效后不再更新旧任务 UI。

## 3. 数据完整性

- 删除 Entry、孤立词、词域或短语替换范围时清理 Entry StudyStamp；
- 清理没有任何聚合内容支持的 Global StudyStamp；
- 重命名导致规范英文变化时迁移全局 StudyStamp；
- 系统短语表 VIX 替换同步删除被移除 Entry 的普通 Membership；
- 规范化 PIN、Annotation、LastPosition、ViewMode 和 CalendarMonth 的软引用。

## 4. 高危操作流程

高危操作不自动下载。先出现小型选择：

- `下载备份`：发起完整备份下载后继续；
- `不下载`：不下载，但同样继续；
- 随后出现实际操作确认；
- 实际确认仍可取消整个操作。

备份选择只决定下载，不是许可门槛。增量合并不进入该高危流程。

## 5. UI 与布局

- 修复首页紧凑顶栏标题被压入返回列；
- 序号独立占位，避免三位数覆盖英文；
- 返回顶部常驻于字母／日期工具区；
- 顶栏和导航改为不透明表面，消除透底残影；
- 关联附着件减窄并降低色块重量；
- 关联方向和二级跳转图标统一；
- 长文本加入按滚动状态变化的边缘渐隐；
- Review 控件、弹窗关闭按钮和管理计数重新收口；
- Toast 位置按当前顶部覆盖层动态避让。

## 6. 不变项

- Schema 4；
- DB version 4；
- Seed revision 3；
- Seed 业务数据不变；
- VIX format 1；
- Oxford、ChatGPT 外部协议不变；
- ChatGPT 仍由用户在 iPhone 上调试 `AI查询` 快捷指令输入绑定。
