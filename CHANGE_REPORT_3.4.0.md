# Vocabulary Index 3.4.0 变更报告

## 版本定位

3.4.0 是系统总表投影模型、跨域同形内容、关系跳转、移动端动态导航与数据一致性的综合更新。它不是内容 Seed 更新：Seed revision 保持 3，业务词汇和短语内容不变。

## 一、系统总表投影模型

- 域内词汇总表、域内短语总表、全局词汇总表和全局短语总表统一定义为具体 Entry 的投影视图，不再被视为拥有独立内容或个人状态的真实词表。
- 域内总表对同一具体 Entry 的多个 Membership 去重，只显示一次。
- 全局总表保留所有具体 Entry。不同独立域内相同 `normalizedText` 的词或短语不再选取单一代表 Entry，而是分别显示。
- 跨域同形行按规范文本分组，组内按独立域顺序和稳定 Entry ID 排列。
- 调整独立域顺序只改变同形组内显示顺序，不迁移 PIN、Annotation、StudyStamp 或其他具体 Entry 状态。
- 只有跨域同形冲突行显示右上方来源域副字。
- 全局页面显示的总数按唯一规范文本组统计；实际渲染行数独立用于分块和性能计算。

## 二、学习日期与 Schema 5

- Backup schema 从 4 升级为 5。
- StudyStamp 统一绑定具体 Entry，规范键为 `entry:<entryId>`。
- 旧 Schema 4 的 Entry 日期直接迁移。
- 旧全局词形日期只迁移到旧域顺序下的代表 Entry，不复制给全部跨域同形 Entry。
- 删除、重命名、替换和导入均清理或迁移具体 Entry 日期。
- 重命名合并日期冲突时，优先保留日历日期较晚者；日期相同则保留 `reviewedAt` 较晚者。
- IndexedDB Object Store 结构未变化，数据库版本保持 4；变化发生在备份数据规范和迁移层。

## 三、序号规则

- 设置文案由“字母内编号”改为“小标题内编号”。
- 连续编号始终继承字典序下的唯一规范文本组序号，日期模式不重排。
- 跨域同形 Entry 在连续编号下共享序号。
- 字母模式的小标题内编号按字母内唯一规范文本组编号，同形行共享序号。
- 日期模式的小标题内编号按每个具体日期和“未标注”分别计算；同形 Entry 位于不同日期组时可有不同编号，位于同一日期组时共享编号。

## 四、搜索

- 搜索顺序改为“先确定当前范围，再在范围内评分排序，最后截断”，修复普通表、域内总表和全局总表漏结果。
- AI 联想搜索采用同样的范围优先规则。
- 全局搜索直接检索具体 Entry，可通过非首个词域实例的繁体释义找到对应行。
- 跨域同形结果分别显示并携带来源域信息。

## 五、关系跳转三态

- 唯一域内目标：显示域内跳转图标并直接跳转。
- 唯一域外目标：显示域外跳转图标并直接跳转。
- 两个及以上有效目标：显示专用多目标图标，打开锚定式紧凑扁平列表供用户选择。
- 目标列表按独立域顺序、词表顺序和分区顺序排列；目标多时仅窗口内部滚动。
- 只有当前投影中真实可见、可定位的目的地才会列出。
- 首页卡片改用独立“进入词表”图标，不再复用关系跳转图标。

## 六、PIN、标注与具体 Entry 状态

- PIN 始终绑定具体 Entry；同一 PIN 在该 Entry 的普通表、域内总表和全局总表投影中保持一致。
- 在任一投影视图取消 PIN，会取消该具体 Entry 的 PIN，不创建投影级 PIN。
- 跨域同形 Entry 可分别设置 PIN、Annotation 和 StudyStamp。
- 全局冲突行的 PIN 条和标注审阅条显示来源域，避免同形内容混淆。
- 导入归一化不再把失效的全局状态重映射到另一个跨域同形代表 Entry。

## 七、AI 核查事务

- 一次完整 AI 核查无论拆成多少批次，只写入一条 Undo 历史。
- AI 批次 Annotation 写入不再逐批占用普通历史。
- 任务历史只包含 Annotation 的任务前后差异，不撤销正文、PIN 或学习日期。
- AI 请求携带 Entry 文本、更新时间和任务 Revision；过期结果被跳过，且不会误删原标注。
- `AbortController` 贯穿 Fetch 和重试等待，用户取消及高危数据操作可立即中断当前请求。
- Annotation 更新采用局部事务，减少整库复制和页面重绘。

## 八、导入与 VIX

- 导入预检计划记录 `baseRevision`；最终提交前若 Revision 已变化，则重新生成计划，避免旧 `nextBackup` 覆盖确认期间的新状态。
- 系统短语表替换同步处理 Membership，避免指向已删除 Entry。
- 外部 VIX 中无法唯一解析的裸 Entry 关系键被视为脏数据：跳过歧义 Membership，不自动猜测，并在预检中报告。
- 其他合法内容继续导入。
- 拖动排序发生 `pointercancel` 时恢复原顺序，不提交半完成排序。

## 九、移动端动态导航

- 字母轨道温和跟随当前阅读字母：按钮仍可见时不移动，完全离开可视区时才滚入视野。
- 用户手动横滑字母轨道期间暂停自动跟随。
- 真实字母小标题采用 sticky 行为，停留在主动态顶栏下方，直到下一字母推出。
- 点击吸附的小标题可折叠该组；折叠后通过坐标补偿保持标题原屏幕位置。
- Large Title 在恢复位置和程序跳转后重新校正，不再停留于半截切入固定顶栏的状态。
- 顶部覆盖层统一通过有限槽位和 CSS 几何变量参与跳转位置计算。

## 十、一级表项与短语布局

- 无序号模式释放左侧完整序号轨道，仅保留正常内边距。
- 日期不存在时不保留完整日期空槽。
- 操作图标视觉尺寸和空隙收紧，同时维持触控命中范围。
- 文本横滑状态绑定具体 DOM/Pointer 手势；节点换 Entry、重新物化或模式变化时归零。
- 仅在真实溢出时显示边缘提示。
- 短语先轻量分类；可见 two-line 短语真实溢出时升级为 extreme，避免两行 Clamp 静默裁切。
- 零数量词汇区或短语区保持 3.3.1 现行结构，并补充 0↔1 状态回归测试。

## 十一、视觉收口

- 固定顶栏采用真正独立居中布局，标题不再受左右按钮宽度影响。
- 顶部置顶图标使用统一 ViewBox 和网格居中。
- 移除首页各区域左侧装饰竖线。
- 移除全局区域的方向性渐变和光晕。
- 移除全局／域内系统表卡片的廉价渐变、浮雕和阴影，改用纯色、细边框和留白建立层级。
- 精简关系附着件、管理弹窗和关闭按钮的视觉重量。
- 统一日期、PIN、查询、更多、模式、关系、三态跳转、进入词表和关闭等高频图标。

## 十二、交付规范

- 新增正式全生命周期文件 `PROJECT_HISTORY.md`。
- 该文件记录版本时间线、模型演变、冻结规则、废弃方案、失败尝试、测试基线、当前状态、待验证事项和跨聊天窗口交接流程。
- 从 3.4.0 起，每次完整 ZIP 交付必须同步更新该文件。
