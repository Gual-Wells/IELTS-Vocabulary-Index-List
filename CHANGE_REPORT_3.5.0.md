# Vocabulary Index 3.5.0 变更报告

## 定位

3.5.0 是 3.4.0 数据模型之上的运行时与移动端 UI 重构。Backup Schema 仍为 5，DB version 仍为 4，Seed revision 仍为 3。

## 页面结构

- 普通词表拆分为词汇视图和短语视图，不再同页渲染两个分区。
- 新增固定底部五项工具栏：上次位置、回到顶部、排序切换、词／短语切换、搜索。
- 顶部保留返回与更多；词表页移除顶部搜索。
- 系统总表的词／短语切换按钮禁用。

## 一级表项

- 删除外挂序号框与永久序号轨道，序号改为英文前置文本。
- 删除右侧关系附着轨道；关系按钮成为操作区第一项。
- 日期节点仅在有日期时存在，不再保留空日期宽度。
- 无释义条目采用紧凑单行高度；有释义条目采用紧凑双行高度。
- 普通词和极限短语保留横滑；普通／两行短语允许完整换行，不再静默裁切。
- 跨域冲突来源显示在一级表项边框右上方。

## 导航和滚动

- 建立内部历史深度与页面快照，跨词表返回恢复原页面状态。
- 首页进入全部折叠；搜索／关系跳转只展开目标组。
- 同页跳转不再依赖 Hash 是否变化。
- 字母轨道只包含字母，取消平滑追赶，A／# 使用明确端点。
- 真实字母标题本身 sticky；删除复制式透明动态标题。
- Chunk 物化和关系展开加入视口锚定补偿。
- 页面切换只使用短暂透明度过渡，不动画高度和滚动。

## 弹窗与固定栏

- iOS 弹窗采用 body 固定和 touchmove 边界锁定，阻止背景穿透。
- PIN 改为完全不透明的直角底部次级栏，不覆盖标题。
- 搜索输入、范围选择和按钮统一等宽对齐。
- 查询总入口图标保持不变；只重绘 Oxford／ChatGPT 弹窗图标并缩小弹窗。

## 数据与行为关联

- 保留普通词汇的词表优先级占有机制。
- 优先级变化时 PIN 上下文优先迁移到新的最高优先级普通投影；失效上次位置被清除。
- 当前普通视图的 AI 核查、标注计数、审阅、清理和 CSV 导出按当前 kind 过滤。
- AI 人工修改保护和聚合历史只记录仍由 AI 拥有的变化。
- 增量导入改为等待 `restoreBackup` 完成。

## 视觉

- 结构性 UI 改为直角：顶栏、字母栏、底部工具栏、PIN、分组、一级表项和关系展开区。
- 移除毛玻璃、结构阴影、绿色大面积渐变、左侧装饰线和目标高亮竖线。
- 页面背景改为中性纯色。
