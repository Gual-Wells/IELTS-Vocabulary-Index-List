# Vocabulary Index 3.5.1 变更报告（对齐重做版）

## 版本状态

3.5.1 第一次封包仅部分实现已确认方案，已撤回其“完整稳定交付”认定。本文件描述的是重新对齐需求后制作的 3.5.1 重做版。

- App version：3.5.1
- Backup schema：5
- IndexedDB DB version：4
- Seed revision：3
- Service Worker cache：`v3.5.1-ios-shell-20260803-3`

本版不修改全局投影、跨域同形、普通词表优先级占有、PIN、AI 标注和学习日期的数据模型。

## 完成的修正

### 统一 Dialog Shell

- 四类原生 Dialog 统一由全视觉视口外层负责定位；
- 卡片宽度只相对于安全边距后的父容器计算，不再使用内部 `100vw`；
- 搜索、设置、管理、条目操作和确认弹窗的标题都采用 44px／自适应／44px 三列物理居中；
- 输入框、Select、文件输入、操作列表和 Footer 均限制在 Dialog Body 宽度内；
- Dialog Body 是唯一可滚动区域，固定弹窗的触摸不得传递给背景页面。

### 模式切换

- 手动切换字母／日期模式不读取目标模式保存的浏览锚点；
- 只有实际内容区已进入阅读视口并存在明确 Entry 时才保持该 Entry；
- 当前仍位于 Large Title、日历或页面顶部区域时，新模式从顶部开始；
- 搜索／关系跳转、页面返回快照和手动浏览锚点继续使用各自独立语义。

### 手动浏览锚点

- 短按靶心按钮跳转到当前 Collection／ViewKind／Mode 的手动锚点；
- 长按约 520ms 保存或覆盖当前位置；
- 手指移动超过 10px、页面开始滚动、Pointer 取消或丢失捕获时取消长按；
- `contextmenu` 仅作为同一长按事务的兼容触发，不会重复写入；
- 正常滚动、复制、搜索、PIN、关系跳转、模式切换和返回顶部均不自动改写锚点。

### 字母栏边缘跟随

- 当前字母进入首部或尾部警戒位时提前移动轨道；
- 根据字母变化方向选择前后目标位置，并使用短暂反向锁避免临界抖动；
- 程序滚动与用户横滑分离，程序滚动不会被误判为人工操作；
- 第一个和最后一个有效字母使用硬边界，不使用平滑追赶或弹性过冲；
- 用户横滑结束后执行最终同步。

### 一级表项元信息

- 一级表项改为明确的三列两行 Grid：正文、可选日期、固定操作区；
- 日期不存在时日期列不占宽度，操作区也不会自动换列；
- 序号和英文位于主行；
- 第二元信息行横跨整个一级表项：隐藏序号占位、繁体释义、右下角独立域来源；
- 繁体释义严格与英文左边缘对齐；
- 来源不再绝对定位覆盖，也不再增加来源专用高度；
- 无释义且无来源使用单行规格；有释义或来源统一使用同一双行规格；
- 关系槽位始终存在，无关系时为空占位，日期与四个固定按钮保持对齐。

### 日历导航

日历头部顺序为：上一年、上个月、年月标题、下个月、下一年。单箭头按月，双箭头按年，年跳转保持当前月份。

## 保持不变

- 普通词汇继续由同域普通词表优先级决定唯一显示归属；
- 短语继续按 Membership 在多个普通词表中显示；
- 全局词汇／短语总表显示跨域具体 Entry；
- 跨域同形 Entry 的 PIN、AI 标注、学习日期和关系独立；
- 三态关系跳转、普通表词汇／短语双视图、底部五项工具栏和查询总入口图标保持不变。
