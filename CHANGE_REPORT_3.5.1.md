# Vocabulary Index 3.5.1 变更报告

## 版本定位

3.5.1 是 3.5.0 稳定主体之上的真机 UI 与状态定义修正版。Backup Schema 仍为 5，IndexedDB DB version 仍为 4，Seed revision 仍为 3。本版不改变全局投影、跨域同形、普通词表优先级占有、PIN、AI 标注和学习日期的数据模型。

## 主要变更

### 弹窗统一布局

- 原生 `dialog` 统一负责最终视口宽度，内部卡片改为 `width: 100%`；
- 删除内部表单基于 `100vw` 的重复宽度计算，修复右侧截断；
- 搜索弹窗标题使用 44px／自适应／44px 三列物理居中；
- 输入框、下拉框、文件选择器、操作列表和底部按钮统一受 Dialog Body 宽度约束；
- 关闭按钮恢复为轻量 44px 命中区，不再使用巨型边框按钮。

### 模式切换状态

- 手动切换字母／日期模式不再读取目标模式的历史位置；
- 当前视口存在明确可见 Entry 时，在新模式中保持该 Entry；
- 当前位于日历、大标题或无明确 Entry 的区域时，新模式从顶部开始；
- 浏览锚点、页面返回快照和模式切换不再混用。

### 手动浏览锚点

- 取消滚动、复制、返回顶部等操作对“上次位置”的自动覆盖；
- 底部靶心按钮短按跳转到当前 Collection／ViewKind／Mode 的浏览锚点；
- 长按约 520ms 保存或覆盖当前位置；
- 没有锚点时短按给出“长按保存当前位置”提示；
- 页面返回栈仍自动保存即时页面状态，和浏览锚点保持独立。

### 字母栏边缘跟随

- 当前字母进入轨道首部或尾部警戒区时提前移动；
- 不再等待选中按钮完全离开可视区；
- A 和 `#` 使用硬边界归零／归末；
- 用户手动横滑结束后执行一次最终同步；
- 保持无平滑追赶、无过冲。

### 一级表项

- 序号和英文改为同一主行中的两列结构；
- 繁体释义严格与英文左边缘对齐，不再落在序号下方；
- 关系按钮槽位始终保留；无关系时使用不可交互占位，保证日期和固定控件对齐；
- 跨域来源移动到一级表项右下角；
- 繁体释义和来源共享同一第二元信息行；
- 一级表项只保留单行、双行两档，不再出现来源专用额外高度。

### 日历导航

日历头部顺序调整为：

1. 上一年；
2. 上个月；
3. 年月标题；
4. 下个月；
5. 下一年。

单箭头按月移动，双箭头按年移动。年跳转使用 ±12 个月，保持当前月份。

## 保持不变

- 普通词汇继续由同域普通词表优先级决定唯一显示归属；
- 短语继续按 Membership 在多个普通词表显示；
- 全局词汇／短语总表显示跨域具体 Entry；
- 跨域同形 Entry 的 PIN、AI 标注、学习日期和关系独立；
- 三态关系跳转、词汇／短语双视图、底部五项工具栏保持；
- 查询总入口图标保持不变。
