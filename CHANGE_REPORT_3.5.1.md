# Vocabulary Index 3.5.1 变更报告

## 版本定位

3.5.1 是从 **3.5.0 正式基线直接重建**的真机 UI 与状态定义修正版。它不继承两份已撤回 3.5.1 错版源码；错版只作为截断事故的警告性历史记录。

- App version：3.5.1
- Backup Schema：5
- IndexedDB version：4
- Seed revision：3
- VIX version：1
- 无数据迁移、无 Seed 内容变更、无主体投影模型变更。

## 弹窗系统

- 统一 `app-dialog`、操作弹窗、搜索弹窗与确认弹窗的视觉视口 Shell。
- Shell 使用 `visualViewport` 的宽、高与偏移；内部卡片只使用 Shell 的可用宽度，不再自行计算 `100vw`。
- 标题栏统一为 `44px / 自适应标题 / 44px` 三列，标题位于弹窗物理中心。
- 输入框、下拉框、文件选择器、操作列表、预检区和 Footer 都受 Dialog Body 宽度约束。
- 仅 `.dialog-body` 可滚动；固定区或不可滚动弹窗上的滑动被拦截，不传递到主页面。
- 打开弹窗时固定 Body 并记录 `scrollY`；最后一个弹窗关闭后精确恢复。

## 模式切换

- 手动切换字母／日期模式不再读取目标模式的旧浏览位置。
- 当前确实处于正文阅读区时，以当前可见具体 Entry 作为转换锚点。
- Large Title、日期日历或页面顶部仍在阅读视口时，目标模式从顶部、全收起状态开始。
- 模式切换、页面返回、搜索／关系目标和手动浏览锚点四种语义分离。

## 手动浏览锚点

- 原“上次浏览位置”改为手动浏览锚点。
- 短按：跳转到当前词表、当前词／短语视图、当前字母／日期模式保存的锚点。
- 长按约 520ms：将当前真实可见的第一条 Entry 保存或覆盖为锚点。
- 手指位移超过 10px、页面滚动、Pointer Cancel 或失去捕获会取消长按。
- 正常滚动、复制、搜索、PIN、关系跳转、返回顶部、模式切换和返回栈不再自动写入浏览锚点。

## 字母栏与字母标题

- 继续使用 3.5.0 的真实字母标题展开／收起链；`setLetterSectionOpen` 与 `toggleLetterSectionWithAnchor` 均保留并纳入运行时符号测试。
- 字母导航按钮可直接展开目标字母并定位标题。
- 当前字母抵达轨道首部／尾部警戒区时提前移动，不等待选中项完全离开视图。
- 跟随为即时滚动；A 与 `#` 使用硬边界。
- 增加方向判定、快速反向滞回和用户横向手势结束后的最终同步。

## 一级表项

- 序号仍为英文前置文本；新增真实 `entry-lexeme-stack`，繁体释义严格对齐英文左边缘。
- 来源独立域从右上外扩标签改为一级表项框内的右下元信息。
- 繁体释义与来源使用同一真实第二信息行：
  - 无释义、无来源：紧凑单行；
  - 有释义、仅来源或两者同时存在：统一双行。
- 来源不再通过负定位或顶部 Padding 扩张表项，也不会产生第三种高度。
- 操作区始终保留五个稳定槽位；没有关系时，第一个槽位为不可见、不可交互的占位，日期和其余控件保持对齐。
- 未标注日期不生成 Grid Item，不预留日期宽度；操作区仍固定在显式 `actions` 区域。

## 日历

日历标题栏改为五段：

```text
上一年（双箭头）｜上个月（单箭头）｜年月｜下个月（单箭头）｜下一年（双箭头）
```

- 双箭头位于旧单箭头外侧位置；单箭头向内移动。
- 年跳转按当前月份 ±12 个月，保持月份不变。

## 保留不变

- 普通词汇继续采用同域“词表优先级占有”；短语继续显示于全部普通 Membership。
- 系统总表仍为具体 Entry 投影；跨域同形 Entry 的 PIN、日期、释义与标注相互独立。
- 普通词表继续采用词汇／短语双视图和五项底部工具栏。
- PIN／标注继续使用 3.5.0 的不透明直角底部停靠体系。
- 一级表项查询总入口图标保持不变；Oxford／ChatGPT 弹窗选项图标保持 3.5.0 已重绘版本。
