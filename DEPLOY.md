# Vocabulary Index 3.1.1 部署与回滚

## 部署

1. 备份线上完整源码；
2. 备份现有应用数据；
3. 用本完整包替换站点内容；
4. 确认所有文件位于站点根目录；
5. 通过 HTTPS 或 localhost 访问；
6. 等待 Service Worker 更新提示并选择立即更新；
7. 完全关闭旧页面后重新打开；
8. 检查设置中的版本为 3.1.1；
9. 检查普通表词汇区、短语区、日期模式与 Seed revision 3；
10. 确认 iPhone 存在准确名称为 `AI查询` 的快捷指令；
11. 分别测试牛津和 ChatGPT 一级表项控件；
12. 导出新的 3.1.1 完整备份。

## 迁移验证

升级后应确认：

- 原有用户内容、PIN 和标注仍在；
- 计算机术语四个普通表出现短语数量；
- 通用英语 A1、A2 出现内置短语；
- 学习日期初始为空；
- 字母和日期模式可切换；
- 总表不提供直接新增入口；
- 牛津控件打开牛津英汉辞书并查询当前英文；
- ChatGPT 控件调用 `AI查询`，新建普通聊天且收到条目 JSON。

## 回滚

代码回滚不会自动降级 Schema 4。回滚前应保存 3.0.x 兼容备份。3.1.1 完整备份不保证可由旧版直接读取。

## 缓存

3.1.1 使用缓存名：

```text
gual-vocabulary-index-v3.1.1-entry-integrations-20260802-1
```

升级桥只删除旧 App Shell 缓存，不操作 IndexedDB 业务数据。
