# Vocabulary Index 3.0.6 部署与回滚

## 部署前

1. 在线上版本导出完整备份；
2. 保留旧源码 ZIP；
3. 关闭同一站点的其他 Safari 标签页和主屏幕 PWA；
4. 核对 ZIP SHA-256；
5. 解压后确认 `index.html` 位于根目录。

## 完整替换仓库

保留 `.git`，删除其余旧项目文件，再把完整 ZIP 中全部文件复制到仓库根目录。建议提交信息：

```text
Replace repository with Vocabulary Index 3.0.6 full source
```

不要与旧覆盖分支合并。

## 发布后

1. 等待 Pages 部署完成；
2. 打开站点并按提示“立即更新”；
3. 完全关闭并重新打开主屏幕 PWA；
4. 检查设置版本为 3.0.6；
5. 确认计算机术语出现四个普通词表；
6. 进入“数据交换”，完成一次词表内容导出和示例增量预检；
7. 按 `tests/MANUAL_CHECKLIST.md` 验收；
8. 导出新的 3.0.6 完整备份。

## 3.0.5 → 3.0.6

首次启动检查 `builtInSeedRevision`。低于 2 时，应用以幂等方式补充四个计算机术语普通词表及分类 Membership。该流程不重复插入 Entry，不覆盖 PIN、标注、浏览位置和用户自建内容。

## 回滚

代码回滚不会自动降级 Schema 3 数据。回滚前保留当前完整备份；部署旧版后使用与旧版兼容的备份恢复。不要把 3.0.6 内容文件当作完整应用备份。

## 缓存

正常更新通过顶部提示完成。只有确认线上文件正确且已经导出完整备份时，才考虑清理网站数据；清理网站数据会删除 IndexedDB 和本地模型设置。
