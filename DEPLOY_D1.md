# D1 GitHub 测试包

版本：4.7.3+D.1。基于 4.7.3 的 D 线测试快照，不是已验收稳定版。

## 上传方式

1. 保留原仓库文件的版本记录；测试前在旧应用中导出一份完整备份。
2. 解压后，将包内文件放进现有 GitHub Pages 发布目录，保持原路径结构。index.html 应直接位于该发布目录，不要额外套一层 project 文件夹。
3. 同时更新 js、css、sw.js、index.html 等全部文件，不要只替换一部分。保留根目录的 .nojekyll；隐藏文件也需上传。
4. 等待原有发布流程完成后访问站点。新页面及设置页应显示 4.7.3+D.1；出现更新提示时点击“立即更新”，然后关闭旧页面重新打开。
5. 若仍显示旧版，先确认发布成功以及打开的是正确站点。不要直接清除网站数据，以免丢失本地词库、学习状态和密钥。

本包是完整静态站点源码，不需要构建，也不包含 node_modules 或本地工具。没有替你更改 GitHub 仓库或 Pages 配置。

## 本轮优先测试

- Groq：原密钥/模型兼容，刷新模型目录；查词与核查切换；取消、重试、关闭后旧结果不回填。
- Collins：先在设置中填写密钥及明确的词典代码，或获取账号词典后选择并保存；再查词。
- 既有导航、词条列表、关系、PIN、学习日期与弹窗是否正常。
- iPhone 主屏幕 PWA、软键盘、长结果滚动与更新后的离线启动。

Collins 的真实鉴权、账号授权与 CORS 尚未验收；出现 401/403 或跨域错误时，请记录不含密钥的提示及复现步骤。不要把密钥、带 accesskey 的完整请求地址或私有词库上传到 GitHub。

详细实施报告：PROVIDER_RUNTIME_D1.md。真机清单：tests/MANUAL_CHECKLIST.md。

## 包内容与校验

保留应用、原有数据、文档、Node 测试及工具；仅排除本地模拟页面 tests/provider-browser.html 和 tests/provider-browser-fixture.js，避免将假响应测试入口带到公开站点。实施报告中的浏览器夹具说明针对本地工作区，不适用于本部署包。

包内 FILE_MANIFEST.txt 和 SHA256SUMS.txt 已针对本包重新生成，不含这两个校验文件自身。原工作区文件未被修改。
