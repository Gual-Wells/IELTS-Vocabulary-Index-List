# D2 GitHub 测试包

版本：4.7.3+D.2。D 线部分修正快照，不是最终稳定版。

## 重要状态

Groq 已通过有限真实联调，修正空词性误报；设置状态和请求隔离已修正。
Collins 账号词典/真实词条仍未打通：当前测试遇到验证页/CORS 阻断。本包改善错误区分，但不把这算作 Collins 接入修复。
完整证据、已知限制及下一项官方接入资料需求见 PROVIDER_RUNTIME_D2.md。

## 上传方式

1. 在旧应用导出完整数据备份，保留 GitHub 历史版本。
2. 将本包解压后的全部文件按原结构覆盖原 GitHub Pages 发布目录。index.html 应直接在发布目录，不多套 project 文件夹；不要漏掉 .nojekyll。
3. 同时更新 js、css、sw.js、index.html，不只换单个文件。无需构建或 node_modules。
4. 等待原有发布流程完成，进入站点，按更新提示“立即更新”，再关闭旧页面并重开。页面及设置应显示 4.7.3+D.2。
5. 若还是旧版先确认 GitHub 发布状态和站点地址，不直接清除站点数据，以免丢失词库、PIN、学习状态和密钥。

没有替用户修改远端仓库或 Pages 配置。

## 优先测试

- Groq：刷新目录、选择账号可用且能力兼容的模型、保存；空词性不误报，拼写错词仍提示；查词和核查保持独立。
- 两家设置状态独立；请求中改变密钥、关闭重开设置，不受迟到结果影响。
- Collins：仍需官方接入说明，不要反复更换词典代码或密钥猜测；不要将密钥/完整 accesskey URL/私有词库上传 GitHub。
- iPhone 17 标准版：真实 standalone、键盘、安全区域、主题、滚动及升级保留数据。桌面 402×874 检查不替代真机。

## 包内容

完整静态应用、原始数据、文档、Node 测试和工具；不包含 API 密钥、node_modules、本地诊断服务器、浏览器模拟页面或模拟响应脚本。
FILE_MANIFEST.txt 和 SHA256SUMS.txt 是本包实际清单，不包含两个校验文件自身。
原 D1 包保留不覆盖；需要回退时恢复完整 D1 版本，并保留当前用户数据。
