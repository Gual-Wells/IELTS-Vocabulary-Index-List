# 2.4.1 AI 标注即时刷新修复报告

## 现象

在标注审阅栏点击“取消标注”后，数据库中的标注已经删除，首页与词表标注计数也会变化，但当前已经渲染的词条行仍显示“待核查”。重新进入词表后才消失。

## 根因

这不是 IndexedDB 事务失败。`dismissAnnotation()` 已依次完成：

1. IndexedDB 无历史事务提交；
2. 内存 `annotations` Map 删除；
3. 发布 `annotations` 状态通知。

缺口位于 UI：词条徽标是在 `createWordRow()` 时根据当时状态生成的静态 DOM。标注通知此前只刷新入口计数和审阅栏，没有同步更新已存在的 `.annotation-badge`。同一词表内审阅下一项只执行滚动定位，不会重绘整张词表，因此旧徽标残留。

## 修复

- 新增 `refreshRenderedAnnotationBadges()`，在 `annotations` 状态通知后遍历当前已渲染的标注按钮，并以最新 Store 状态切换 `hidden`。
- 该机制同时覆盖新增批次标注、单条取消和批量清除后的即时显示同步。
- 修复审阅取消操作的索引计算：保存删除前索引，删除后选择同一位置的下一项；若删除末项，则选择前一项。

## 边界

未渲染的折叠字母无需更新 DOM；以后展开时会直接根据最新 Store 状态创建正确徽标。该修复不重绘词表，因此不会改变展开状态、滚动位置或产生额外大规模 DOM 开销。
