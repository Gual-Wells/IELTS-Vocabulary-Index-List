# 2.4.1 → 3.0.0 迁移说明

## 升级前

1. 在 2.4.1 设置页导出完整 JSON。
2. 将备份保存到浏览器之外。
3. 关闭该站点全部 Safari 标签页和主屏幕 PWA。
4. 不要清除 Safari 网站数据。

## 首次启动迁移

IndexedDB 版本从 1 升至 3。旧对象仓库保留为只读迁移来源，新数据在一个事务中写入 `v3*` 仓库：

- 建立默认词域“通用英语”；
- 七个旧 category 转为普通 Collection，保留优先顺序；
- 每个词域建立唯一系统短语表；
- 旧 Entry 按域内规范英文合并；
- 多词表达标记为 phrase，并重建 PhraseToken；
- 每个旧 `sources[categoryId]` 转为 Membership；
- 旧词性归档到 `sourceLabel`；存在人工词性覆盖时优先保留人工结果；
- **不迁移或生成 `sourceText`**；
- PIN、PIN 顺序、AI 标注和 IndexedDB 中独立保存的 `lastPosition:<categoryId>` 转换到新 ID；
- 旧撤销历史不迁移。

迁移完成提示只显示一次。

## 失败与阻塞

- 其他同源页面持有旧数据库连接时，升级会停止并提示关闭其他实例。
- 迁移写入失败时事务整体回滚，不提交半套 3.0 数据。
- 不要为了绕过阻塞而删除网站数据。

## 回退

3.0 数据不能直接由 2.4.1 读取。需要回退时：

1. 恢复部署 2.4.1 程序文件；
2. 使用升级前导出的 **2.x JSON** 恢复；
3. 不要把 3.0 JSON 导入旧版。


## 3.0.5 → 3.0.6 内置数据升级

3.0.6 将 `builtInSeedRevision` 升至 2。首次启动时，应用只对已有“计算机术语”词域执行幂等补充：

- 新建“计算机基础与系统”“软件开发与数据”“网络、云与安全”“人工智能”四个普通词表；
- 为现有 544 个普通词补充一个且仅一个用户可见主分类 Membership；
- 保留唯一短语表及其 577 条短语；
- 不复制 Entry，不覆盖 PIN、标注、浏览位置、繁体设置或用户自建内容；
- 重复启动不会重复创建 Collection 或 Membership。

升级还新增 VIX JSON 数据交换中心，但不会自动导入或替换任何用户内容。
