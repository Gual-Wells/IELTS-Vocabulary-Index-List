# Vocabulary Index 3.1.0 迁移说明

## 1. 数据库升级

- IndexedDB 版本：4；
- 完整备份 Schema：4；
- 新增 `v3StudyStamps`；
- 内置 Seed revision：3。

初始化流程会优先读取升级后的现有 v3 数据快照。只有没有现有数据时才使用旧 2.x 数据或 Seed，防止 3.0.x 用户内容在数据库版本升级后被初始数据覆盖。

## 2. 3.0.x → 3.1.0

保留：

- 独立域；
- 词表；
- Entry；
- Membership；
- PIN；
- 标注；
- 原有浏览位置和设置。

补充：

- 学习日期空存储；
- 字母／日期视图状态；
- 普通表短语 Membership；
- 计算机术语 577 条短语的四领域分类；
- 通用英语 10 条短语的普通表分类。

旧浏览位置作为兼容回退使用；首次在新模式浏览后写入按模式和区分离的新位置键。

## 3. 幂等要求

重复启动或重复执行 Seed revision 3 合并时：

- 不新增重复 Entry；
- 不新增重复 Membership；
- 不覆盖用户释义、PIN、标注和学习日期；
- 不删除用户自建普通表或短语；
- 只补充缺失的内置数据和分类关系。

## 4. 回滚

Schema 4 完整备份不保证可由 3.0.x 直接恢复。回滚前应保存 3.0.x 兼容备份，或在 3.1.0 中导出内容 JSON 后由旧版重新导入兼容内容。不要把内容 JSON 当作完整应用备份。
