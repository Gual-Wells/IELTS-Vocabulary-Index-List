# Vocabulary Index 3.1.1 迁移说明

3.1.0 → 3.1.1 不升级 IndexedDB Schema，也不修改 Seed 内容。首次加载只更新应用外壳和 Service Worker 缓存。现有 Entry、Membership、PIN、标注、学习日期、视图模式和浏览位置保持不变。

使用 ChatGPT 控件前，iPhone 快捷指令中必须存在准确名称为 `AI查询` 的快捷指令，并将 `Start conversation with ChatGPT` 的 Start Action 设为“输入快捷指令的信息”，Temporary chat 保持关闭。

牛津控件要求设备安装支持 `hk-com-oupc-oecd-lookup://` 的牛津英汉辞书 App。
