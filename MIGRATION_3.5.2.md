# Vocabulary Index 3.5.2 迁移说明

## 数据迁移

无。

- Backup Schema：5
- IndexedDB：4
- Seed revision：3
- VIX：1

3.5.1 数据库可直接由 3.5.2 打开。Entry、Membership、PIN、标注、学习日期、繁体释义、浏览锚点和 API 配置均不批量改写。

## 行为规则覆盖

3.5.2 正式覆盖以下 3.5.1 行为：

1. 字母/日期模式不再映射当前 Entry；
2. 词汇/短语切换不再恢复目标视图旧状态；
3. 两类普通切换均进入顶部、全部分组收起；
4. 只有真实递归返回恢复页面完整快照；
5. 日期日分组和“未标注”由默认展开改为显式折叠状态。

旧设置中的 viewModes、calendarMonths 和手动浏览锚点继续保留，但普通切换不会自动读取目标页旧位置。

## 缓存升级

新 Cache：

```text
v3.5.2-runtime-stabilization-20260804-1
```

部署后等待“新版本已准备好”提示并选择立即更新。若主屏幕 PWA 仍显示 3.5.1，完全退出后重新打开。

## 回滚

可以回滚到 3.5.1 Clean Rebuild，因为 Schema 未变；回滚前仍建议导出完整备份。3.5.2 新的页面快照字段只是浏览器历史临时状态，不写入数据库 Schema。
