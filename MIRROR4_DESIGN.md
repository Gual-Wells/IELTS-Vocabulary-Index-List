# VIX Mirror 4 设计

Mirror 是“当前材料与个人词库之间的可切换映射”，不是一次性导入任务。

## 匹配规则

`vocabulary`、`phrase` 与 `usage` 同等重要。短语包括固定搭配、固定短语和复合表达；用法包括语义项、句法框架、构式、语篇模式和可复用表达。任何类型都不能因为包含基础词而被整体排除。

`data/relation-low-level-lexemes-source.json` 是独立人工审核来源，不从 A1/A2 成员资格推导。它只抑制“把低级词当作唯一目的信息”的单词组件关系，不抑制短语和用法。

## 两层审核

- 已有层：材料中与 VIX 已有词汇、短语或用法的匹配。
- 新增层：材料需要但 VIX 尚无对应条目的候选。

每项保留类别、重要度、材料证据和分配标签。材料标题、每层、字母组与单项复选框按父子联动；取消不写入，提交只合并已选择项目，并将最终 Mirror 记录写回 Bridge 文件。

## 远程文件库

Bridge 保存多个 Mirror 文件，而不是待办队列。文件选择器以类桌面文件夹的目录、搜索、选择和删除方式呈现；当前选择、更新时间和操作都收在紧凑文件行内，不再用独立的大型状态卡占据移动端视图：

- `GET /v1/files` 只返回目录元数据。
- `GET /v1/files/{runId}` 按需返回完整 Result 与已审核记录。
- `PUT /v1/files/{runId}/record` 保存最终审核记录。
- `DELETE /v1/files/{runId}` 永久删除文件。
- `GET /v1/inbox` 只返回尚未确认的新文件元数据。

管理页负责选择、查看和删除；首页 Mirror 按钮只控制已选文件的开启或关闭。没有已选文件时按钮禁用。旧归档/恢复是本地软隐藏模型，不符合远程文件库语义，已从产品实现中删除。

目录首先显示当前浏览器的本机快照，再异步校准 Bridge。远端失败时保留快照并用顶层提醒告知，不把缓存误报成权威空目录；Bridge URL、Token 或实例标识变化时废弃不匹配快照。

## 提醒

首页最多显示一张新文件卡。多条提醒仅用叠层视觉表示；确认顶层后显示下一条。确认后不再重复，不占据首页正文布局。

Context/Result 协议为 `vix-mirror-context/4` 与 `vix-mirror-result/4`，通过 `matchPolicyHash` 冻结规则；本地 Entry ID 不离开 PWA。Bridge 1.4 仍接受已经开始的 Mirror 3 Result。

进入 Context 和词库写入前都清理 Unicode replacement character 与纯问号释义。启用释义的目标词域中，没有有效释义的候选不能进入审核导入；已有损坏释义只接受带原文证据的有效修复，不做猜测填充。
