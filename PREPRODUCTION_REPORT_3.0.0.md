# Vocabulary Index 真 3.0 预制作报告（议定稿）

- 文档版本：v0.9
- 日期：2026-08-01
- 文档状态：预制作议定稿，尚未进入代码实现
- 目标版本：Vocabulary Index 3.0.0
- 主要设备：iPhone 17 Safari 主屏幕 PWA
- 辅助设备：Windows Chrome
- 交付约束：最终只交付完整源码 ZIP；默认本地处理，不自动通过代理网络操作 GitHub

---

## 0. 本报告的性质

这不是把历史 PDF 中所有要求机械拼接成一套规格，也不是对现有 3.0 RC 或 RC2 的继续修补。

本报告按以下优先级重新建立“当前有效的真 3.0”产品规格：

1. 当前对话中用户最新明确要求；
2. 历史中后期出现、未被撤回且与当前架构一致的要求；
3. 历史中仍具有稳定价值的使用场景和工程原则；
4. 2.4.1、3.0.0 RC、RC2 的实际源码行为；
5. 助手方案仅作为候选，不自动视为已确认需求。

历史材料中的早期要求、临时故障、Pages 缓存误判、后续撤回和架构升级分别处理，不互相混淆。

---

# 一、基线判定

## 1.1 三个版本的角色

### 2.4.1：交互行为参考基线

保留价值：

- iPhone 上的成熟词表浏览路径；
- 点按词汇复制；
- 稳定字母折叠与定位；
- PIN 连续导航；
- 上次位置；
- 搜索结果定位；
- AI 标注上一条 / 下一条审阅；
- 词条只保留一个主要操作入口；
- 搜索、编辑、动作菜单使用不同类型的对话容器。

不直接继承：

- 2.x 数据模型；
- 全局唯一范围；
- 来源嵌套结构；
- 2.4.1 的青绿色视觉主题；
- 任何会妨碍 3.0 词域、短语、繁体释义能力的旧限制。

### 3.0.0 RC：数据与视觉基线

保留价值：

- Domain / Collection / Entry / Membership / PhraseToken；
- 词域内唯一；
- 系统短语表；
- 短语一等实体和双向词元关系；
- 可选繁体释义；
- 2.x 迁移；
- AI 分批核查、暂停、继续、取消和部分结果保留；
- 米色纸张背景、墨绿色强调色、Georgia 标题与数字、圆角卡片和低强度阴影；
- 当前 3.0 数据、导入、AI、PWA 和安全边界。

废弃部分：

- 词表页一次性平铺大量同级按钮；
- `PIN ← / PIN →` 位于会滚出视口的普通顶部工具区；
- 每个词条永久显示 PIN、标注和详情三个尾部按钮；
- 展开详情时重绘整个词表；
- 所有任务共用一个通用弹窗；
- 将数据模型层级直接等比例映射为用户操作层级。

### RC2：反例和回归样本

RC2 不作为实现基线。其代码只用于确认不得重复出现的设计：

- 不设置永久五项底部操作栏；
- 不把大量管理行为做成始终可见的移动端导航；
- 不把词条详情、来源、短语关系和危险操作混在同一个高频 Sheet；
- 不用“增加常驻控件”代替任务流设计；
- 不因追求形式上的触达性而持续挤压词汇浏览空间。

## 1.2 当前工程基线状态

现有 3.0.0 RC 完整源码可通过：

- `run-tests`；
- `static-tests`；
- `stress-tests`。

这只证明当前数据和静态测试基线可运行，不证明现有 UI 已达到产品要求，也不替代 iPhone 真机验收。

真 3.0 将以 3.0.0 RC 的数据内核为起点，重新实现交互层；不从 RC2 继续修补。

---

# 二、产品定位

## 2.1 产品不是纯词典，也不是后台管理器

真 3.0 是一个：

> 面向 iPhone 主屏幕 PWA 的本地优先词汇索引、连续定位和复制工具，同时提供低频的数据维护、短语关系和 AI 辅助能力。

它具有两层产品平面：

### A. 学习浏览平面（主平面）

高频任务：

1. 打开词域或词表；
2. 展开目标字母；
3. 浏览词汇和词性；
4. 点按词汇复制；
5. 使用快捷指令打开牛津词典；
6. 返回后继续原位置浏览；
7. 通过 PIN、搜索或上次位置继续定位。

设计优先级最高。

### B. 维护与增强平面（次平面）

低频或阶段性任务：

- 新增、编辑和删除；
- 导入、导出和恢复；
- 词域和词表管理；
- 短语关系与繁体释义；
- AI 新增、AI 核查和标注审阅；
- Groq 模型管理；
- 撤销与重做。

这些功能必须完整可靠，但不得占据默认浏览界面的主要视觉空间。

## 2.2 核心产品原则

1. **复制优先**：词条主体的主要点击语义始终是复制英文文本。
2. **连续浏览优先**：从外部词典返回后，词表位置、展开字母和上下文必须保持。
3. **导航持续可达**：PIN 跳转后，前后导航仍然可用；不能要求回到页面顶部。
4. **附加信息渐进披露**：繁体释义、全部来源和短语关系存在，但不强占主列表。
5. **低模式化**：不让词条点击含义随隐性模式改变。
6. **移动端真实优先**：以 iPhone 主屏幕 PWA 的单手触控、Safe Area、返回生命周期和虚拟键盘为基准。
7. **桌面辅助增强**：Windows Chrome 可以增加横向空间和管理密度，但不改变核心行为语义。
8. **功能必须真实**：未实现或未验证的能力不得显示为正式功能。

---

# 三、数据与业务规则议定

## 3.1 保留 Schema 3

真 3.0 保留以下实体：

- Domain：词域；
- Collection：普通词表或系统短语表；
- Entry：词或短语的规范实体；
- Membership：Entry 与普通词表的来源关系；
- PhraseToken：短语词元反向索引；
- PIN、Annotation、Settings、History。

## 3.2 唯一性范围

议定为：**词域内唯一，不跨词域唯一。**

规则：

- 同一词域内，规范化英文相同的词只能有一个 Entry；
- 同一 Entry 可以属于多个普通词表；
- 同词不同词性不创建重复 Entry，而是分别记录在 Membership 的 `sourceLabel`；
- 不同词域可以存在同形词，因为领域语义、来源关系和释义可能独立；
- 当需要为词条选择默认投影词表时，优先采用词域内顺序最靠前的普通词表。

这继承了早期“禁止重复、合并词性、优先上层词表”的意图，同时适配后期词域架构。

## 3.3 词与短语

- 单词和短语都属于 Entry；
- `kind` 由确定性词元规则派生；
- 每个词域拥有唯一系统短语表；
- 普通单词可查询相关短语；
- 短语可查询已收录的组成词；
- 相关短语和组成词不在普通主列表中常驻展示；
- 它们通过“详情”按需查看。

## 3.4 词性与释义

### 词性

- 词性属于 Membership 来源标签，不参与 Entry 唯一身份；
- 主列表显示当前词表对应的 `sourceLabel`；
- 当一个词在当前词表有多个来源标签时，去重后合并显示；
- 编辑词性不改变 Entry 身份。

### 繁体释义

- 释义保留为 Entry 的可选能力；
- 词域可决定是否启用释义录入；
- **主列表默认不显示释义**，避免干扰词汇和词性辨认；
- 释义在词条详情和搜索结果的辅助信息区显示；
- 复制操作始终只复制英文 `Entry.text`；
- 简转繁必须采用有词组优先规则、来源与许可明确的实现；现有简化字符表只能在完成质量和许可审计后决定是否保留。

## 3.5 上次位置

上次位置表示“用户实际停止浏览的位置”，不是只表示最后一次复制。

更新规则：

- 成功复制后立即记录该词条；
- 手动滚动时，防抖记录当前视口内第一个稳定可见词条；
- 搜索、PIN 和标注跳转不与自动恢复发生竞争；
- 进入词表时只有用户主动选择“上次位置”才跳转，普通进入不自动强制跳转；
- 跳转一次只产生一次滚动和一次目标高亮。

---

# 四、真 3.0 信息架构

## 4.1 一级结构

应用只保留五类一级状态：

1. 首页；
2. 词表浏览页；
3. 搜索层；
4. 维护/设置层；
5. AI 任务与标注审阅状态。

Domain、Collection、Entry 等数据实体不会全部转化为同级常驻导航。

## 4.2 首页

### 保留 3.0 视觉结构

- 米色背景；
- Georgia 大标题；
- 墨绿色强调；
- 词域分区；
- 词表卡片；
- 系统短语卡片采用轻微差异化；
- 数字使用 Georgia；
- 不回退到 2.4.1 的视觉主题。

### 首页布局

顶部固定栏：

- 产品标题；
- 全局搜索；
- 设置。

正文：

- 总词项和词表摘要；
- 各词域分区；
- 普通词表卡片；
- 系统短语卡片；
- 词域标题旁的低权重 `＋` 和 `•••`；
- 不使用“新建词表”作为一张与真实词表同级的大卡片。

底部状态：

- 本地保存状态；
- 当前版本；
- 上次完整备份时间（能够可靠记录后再显示）；
- 点击进入数据管理。

### 首页不承担

- 不常驻完整搜索框；
- 不平铺导入、AI、备份等管理按钮；
- 不自动展开所有结构管理功能；
- 不用单词域扁平化破坏未来多词域结构，也不让词域标题成为额外导航步骤：点击词表卡片仍直接进入词表。

## 4.3 词表浏览页

### 顶部栏

固定在 Safe Area 下方：

- 返回；
- 当前词表名称；
- 词项数量或简短说明；
- 搜索；
- 更多。

顶部栏不再常驻：

- PIN 前后；
- 新增、导入、AI 新增、AI 核查的按钮矩阵；
- 词条管理模式开关；
- 永久撤销/重做按钮组。

### 上下文导航区

顶部栏下方只允许出现一个主要上下文控制器：

1. PIN 导航；或
2. AI 标注审阅。

两者互斥，避免形成多层 sticky 控件堆叠。

其下为字母导航。

### 主列表

- 字母分组默认折叠；
- 搜索、PIN、上次位置或标注跳转会自动展开目标字母；
- 普通进入词表不继承其他词表的展开状态；
- 展开和收起只局部修改目标分组，不重建整张词表；
- 当前词表内保持滚动和展开状态，离开后按既定规则恢复。

## 4.4 词条行

默认结构：

```text
序号    English word / phrase             待核查   •••
        当前词表词性
```

规则：

- 词条主体是复制按钮；
- 主要点击语义永远不变；
- PIN 只在已经 PIN 时显示低干扰静态标记，不为所有行显示空星号；
- “待核查”只在有 Annotation 时出现；
- 右侧只保留一个 `•••` 操作入口；
- 不在行内常驻来源、相关短语、组成词和危险操作；
- 不通过点击详情箭头重绘整张列表。

### 复制反馈

复制成功后：

- 显示短时 Toast；
- 更新上次位置；
- 不弹出详情；
- 不改变折叠状态；
- 不自动打开外部应用，继续兼容用户现有快捷指令链路。

## 4.5 词条操作与详情

点击 `•••` 先打开简洁动作菜单：

- 设置 / 取消 PIN；
- 编辑词项；
- 查看详情；
- 添加相关短语（普通词）；
- 从当前词表移除；
- 彻底删除。

“查看详情”再进入独立详情容器，显示：

- 英文；
- 可选繁体释义；
- 所属词表和来源标签；
- 相关短语或组成词；
- 关联跳转。

动作菜单和详情不合并成 RC2 那种单一大 Sheet。

移动端：动作菜单使用底部 Action Sheet；详情使用较完整的独立 Modal/Sheet。  
桌面端：动作菜单可使用锚定菜单，详情使用居中 Modal。

## 4.6 词表“更多”菜单

承载低频操作：

- 新增词项；
- AI 新增；
- AI 核查；
- 审阅 AI 标注；
- 导入；
- 导出当前词表；
- 跳转到上次位置；
- 序号模式；
- 撤销 / 重做；
- 管理词表。

不设置 RC2 的永久底部五项栏。

对于刚完成的数据修改，Toast 可提供短时“撤销”操作；完整撤销/重做仍保留在更多菜单中。

---

# 五、PIN 导航完整规范

## 5.1 定位

PIN 是“连续浏览导航”，不是词条管理模式，也不是普通工具按钮。

## 5.2 显示条件

- 当前词表无 PIN：不显示；
- 当前词表有 PIN：显示 sticky PIN 导航；
- 进入 AI 标注审阅：PIN 导航暂时隐藏；
- 退出审阅：恢复；
- 打开对话框时不重复渲染或改变当前 PIN。

## 5.3 布局

```text
‹    📌 当前 PIN 文本                       2 / 12    ›
```

- 位于固定顶部栏下方；
- 随页面滚动保持可见；
- 字母导航停靠在 PIN 导航下方；
- 中间文本可点击，用于重新定位当前 PIN；
- 前后按钮为 44px 级触控目标；
- 不使用“PIN 前 / PIN 后”大文字按钮；
- 不放在会滚出视口的普通工具卡里；
- 不放在页面底部悬浮并遮挡词条。

## 5.4 状态和跳转

- PIN 有稳定顺序；
- 初始当前 PIN 优先匹配上次位置，无法匹配时采用第一个；
- 前后循环导航；
- 手动普通滚动不自动改变当前 PIN 索引，避免导航状态漂移；
- 搜索或其他显式导航命中一个已 PIN 词条时，可以同步当前 PIN；
- 跳转自动展开目标字母；
- 目标滚动到可阅读位置，不被顶部栏、PIN 条或字母导航遮挡；
- 跳转后控制器仍在原位置，可以立即继续前后跳转。

---

# 六、搜索与定位规范

## 6.1 统一搜索入口

- 首页搜索：全部词域 / 指定词域；
- 词表内搜索：默认当前词表，可切换当前词域或全部；
- 使用同一个搜索组件和结果语义；
- 顶部搜索按钮始终可达，不增加永久底栏。

## 6.2 英文搜索

- 本地即时模糊匹配；
- 不调用网络；
- 输入变化取消旧渲染任务；
- 结果显示词汇、当前来源词性和所在词表；
- 点击结果关闭搜索层，再执行唯一一次定位。

## 6.3 中文 AI 联想

- 只有用户明确点击“AI 联想”才发起请求；
- 输入变化使旧结果失效；
- 请求可取消、可超时；
- 结果只匹配本地已有 Entry，不直接添加数据；
- API 错误使用简洁用户提示，并保留结构化诊断。

## 6.4 定位状态机

所有显式定位统一走一个流程：

1. 解析目标词域和词表；
2. 切换页面；
3. 阻止上次位置自动恢复；
4. 展开目标字母；
5. 确认目标 DOM 已渲染；
6. 计算 sticky 控件占位；
7. 执行一次滚动；
8. 高亮目标；
9. 恢复滚动位置持久化。

PIN、搜索、上次位置、标注和短语关联不得各自实现不同的竞态跳转逻辑。

---

# 七、AI 与标注规范

## 7.1 模型目录

- Groq 模型目录只在用户明确刷新时访问网络；
- 历史发现模型持久保留；
- 最近一次返回状态和刷新时间持久化；
- 当前没有再次返回的历史模型标记为历史/不可确认，不直接丢失；
- 选择器不只保留默认模型和当前模型；
- 不根据 Qwen、Llama、OSS 等模型名称硬编码业务分支。

## 7.2 Qwen 兼容问题

历史只证明某次 Qwen 核查失败，而 70B 和 OSS 120B 正常；现有材料没有完整根因。

生产前必须建立真实响应测试矩阵：

- 状态码；
- 响应结构；
- JSON 输出遵循度；
- `max_tokens` / 上下文限制；
- 速率限制头；
- 工具或结构化输出支持；
- 错误体。

兼容策略必须依据能力和真实响应，不依据品牌名称。

## 7.3 AI 新增

- 明确输入意图；
- 请求锁和取消；
- 要求结构化 JSON；
- 严格校验词项和词性；
- 与当前词域已有 Entry 做去重；
- 用户预览、选择后才提交；
- 不把说明文字、编号或 Markdown 当词项。

## 7.4 AI 核查

启动前显示：

- 范围；
- 词项数量；
- 估算批次数；
- 当前模型。

运行时：

- 动态分批；
- 串行或受控并发；
- 读取限额头；
- 有限重试；
- 暂停、继续和取消；
- 已完成批次立即稳定提交；
- 取消或后续失败不清除已完成结果；
- 不直接修改词汇，只生成 Annotation。

### 任务呈现

- 启动和详细控制在 AI 核查对话框；
- 对话框关闭后，活动任务缩为低干扰状态胶囊；
- 状态胶囊只在任务存在时显示；
- 点击可重新打开详细面板；
- 不形成永久底部导航栏；
- 不遮挡 PIN 或主要词条区域。

## 7.5 标注审阅

进入显式审阅状态后：

- PIN 导航隐藏；
- 显示 sticky 审阅控制器；
- 明确上一条、下一条；
- 显示当前序号、词汇和问题摘要；
- 提供编辑、取消标注和退出；
- 自动展开并定位目标；
- 编辑或删除后重新同步列表；
- 取消首项、中间项、末项均进入正确相邻项；
- 退出后恢复普通浏览状态和 PIN 导航。

---

# 八、维护、导入和数据安全

## 8.1 结构管理

- 新建/重命名词域；
- 开关词域释义能力；
- 新建/重命名/排序普通词表；
- 系统短语表不可删除、不可重命名；
- 危险删除必须说明影响范围并二次确认；
- 结构管理不与普通浏览混在同一工具条。

## 8.2 导入

必须遵循：

```text
选择文件 → 解析 → 格式识别 → 规范化 → 校验 → 去重预览 → 用户确认 → 单事务提交
```

支持格式：

- TXT；
- Markdown；
- CSV；
- 3.0 JSON 词项；
- 完整 JSON 恢复。

要求：

- 读取失败有明确错误；
- 编码和格式异常不修改现有数据；
- 预览新增、合并、忽略和错误数量；
- 替换模式必须可撤销或有完整备份前置提醒；
- 导入后验证关系完整性。

## 8.3 备份与恢复

- 完整 JSON 包含 Schema 版本、Domains、Collections、Entries、Memberships、PhraseTokens、PIN、Annotations、Settings 和必要元数据；
- 恢复执行白名单规范化和关系校验；
- 恢复采用单事务；
- 恢复前提示导出现状；
- 失败不改变当前数据；
- 设置页和首页底部状态均可进入数据管理。

---

# 九、视觉与组件系统

## 9.1 必须保留的 3.0 美学

- `#f5f1e8` 级米色背景；
- `#2e5b4b` 级墨绿强调；
- `Georgia` 用于品牌标题、关键数字和局部索引；
- 系统无衬线用于正文、控件和词条；
- 18px 左右卡片圆角；
- 低强度自然阴影；
- 纸张式、安静、克制的整体观感；
- 浅色方案作为 3.0.0 主视觉，不强制增加深色模式。

## 9.2 交互组件分类

不得再用一个通用弹窗承担所有任务。

- Modal：编辑、设置、详情、AI 核查；
- Action Sheet / 锚定菜单：词条操作、词表更多；
- Search Sheet：搜索；
- Confirm Dialog：危险确认；
- Sticky Context Bar：PIN 或标注审阅；
- Task Capsule：活动 AI 任务；
- Toast：复制和短时反馈。

## 9.3 触控与无障碍

- 主要触控目标至少 44px；
- 不用 `title` 作为唯一说明；
- 图标按钮必须有 `aria-label`；
- 保留浏览器缩放；
- 自定义控件具有 `:focus-visible`；
- 支持 Safe Area；
- 虚拟键盘出现时使用 `dvh` 和可滚动对话内容；
- 减少动画时尊重系统设置。

---

# 十、PWA、缓存和部署规范

## 10.1 GitHub Pages 更新可见性

历史出现过 Pages 未即时刷新导致用户误判旧版本的问题。真 3.0 必须增加明确版本闭环：

- HTML、JS、CSS、Service Worker 使用同一构建版本；
- Cache 名包含版本；
- 新 Service Worker 等待时显示“新版本可用”；
- 用户确认后执行受控 `skipWaiting` / reload；
- 设置页显示运行版本和资源版本；
- 检测混合版本时阻止继续写数据并提示重新载入；
- 部署说明要求严格镜像文件树。

## 10.2 离线和生命周期

- 应用壳离线可打开；
- IndexedDB 数据离线可读写；
- AI 功能离线时明确不可用，不影响本地浏览；
- 从牛津词典返回前台时，无外部数据变化不得关闭上下文或重置滚动；
- 只有检测到真实跨实例修订变化时才重新载入和关闭陈旧编辑状态。

---

# 十一、明确不包含

3.0.0 不包含：

- GitHub PAT 或私人仓库云备份；
- 自动云同步；
- 账号、后端和多用户；
- 完整词典、例句、音标和发音；
- 自动词形还原；
- 跨词域短语关系；
- 按模型品牌写死兼容分支；
- 自动打开牛津词典；
- 永久移动端底部五项操作栏；
- RC2 的词条大详情 Sheet 交互；
- 将 Domain / Collection / Entry 全部直接暴露为同权界面元素。

---

# 十二、实施边界与代码策略

## 12.1 保留模块

优先保留并审计：

- `v3-model.js`；
- `v3-db.js`；
- `v3-store.js`；
- `v3-import.js`；
- `v3-ai.js`；
- Schema 3 数据结构和迁移测试。

## 12.2 重构模块

主要重做：

- `index.html` 的交互容器；
- `css/v3.css` 的响应式、sticky 层级和组件状态；
- `js/v3-ui.js` 的页面状态、定位、局部渲染和对话组件；
- UI 契约测试；
- PWA 更新提示。

## 12.3 必须先审计的隐藏耦合

- `renderCollection()` 是否被所有局部状态错误复用；
- PIN、上次位置、搜索和标注是否共享统一跳转器；
- Entry 编辑后 Membership、PhraseToken、PIN 和 Annotation 是否一致；
- 删除/移除来源后的默认投影词表；
- 恢复和迁移后的系统短语表唯一性；
- 简转繁实现的许可和准确性；
- 模型目录 localStorage 键和版本；
- Service Worker 与 HTML 版本握手；
- iOS 前后台恢复与跨实例通知。

---

# 十三、制作里程碑与证据

## M0：规格冻结

产物：

- 本预制作报告定稿；
- 需求决策矩阵；
- 禁止回归清单。

完成证据：用户明确确认进入制作。

## M1：基线冻结

产物：

- 3.0 RC 原始完整源码只读快照；
- 新工作副本；
- 基线测试输出；
- 文件清单和 SHA-256。

禁止：删除唯一工作副本。

## M2：浏览与定位内核

实现：

- 首页；
- 词表页；
- 局部字母折叠；
- 点按复制；
- 上次位置；
- 统一跳转器；
- PIN sticky 导航；
- 搜索定位。

证据：

- 单元和集成测试；
- UI 契约测试；
- iPhone 尺寸截图或浏览器视口验证；
- 不出现整表重绘依赖。

## M3：操作与维护

实现：

- 词条动作菜单；
- 独立详情；
- 新增、编辑、删除；
- 词域和词表管理；
- 导入导出；
- 备份恢复；
- 撤销重做。

证据：事务和失败回滚测试。

## M4：AI 与标注

实现：

- 模型目录持久化；
- AI 新增；
- AI 核查状态机；
- 任务胶囊；
- 标注审阅；
- 真实模型兼容测试记录。

证据：模拟响应、错误恢复和部分结果保留测试。

## M5：PWA 和跨设备验收

实现：

- 更新提示；
- 版本握手；
- 离线壳；
- iOS 生命周期修复。

证据：

- iPhone Safari；
- 主屏幕 PWA；
- Windows Chrome；
- 飞行模式；
- 前后台切换；
- Pages 更新流程。

## M6：完整交付

产物：

- `Vocabulary-Index-3.0.0-Full-Source.zip`；
- ZIP SHA-256；
- 变更报告；
- 测试报告；
- 人工验收清单；
- 文件清单和逐文件校验。

完成条件：

1. 从 ZIP 重新解压到全新目录；
2. 在解压副本运行全部自动测试；
3. 校验 Seed、源词表和图标；
4. 确认 ZIP 根目录可直接作为仓库根目录；
5. 不包含覆盖脚本、嵌套仓库或旧 RC 残留。

---

# 十四、验收标准

## 14.1 主任务

- 任意词条主体一次点按复制英文；
- 复制后返回应用仍位于原字母和原位置；
- 无意触发编辑、详情或 PIN；
- 词汇和词性是列表第一视觉层级。

## 14.2 PIN

- PIN 导航跳转后仍可见；
- 可连续前后跳转；
- 自动展开正确字母；
- 不被顶部栏遮挡；
- 不需要回到页面顶部；
- 审阅状态下正确隐藏和恢复。

## 14.3 搜索和上次位置

- 定位只滚动一次；
- 目标字母展开；
- 目标高亮；
- 不与上次位置竞争；
- 切换词表时不会被陈旧异步任务改写。

## 14.4 数据

- 词域内不产生重复 Entry；
- 词性合并为来源标签；
- 导入失败不修改数据；
- 保存失败不显示成功；
- 完整恢复失败可回到原状态；
- PIN、标注和短语关系无悬空引用。

## 14.5 AI

- 模型目录重启后仍存在；
- 不要求每次重新搜索模型；
- 核查可暂停、继续和取消；
- 已完成批次不因后续失败丢失；
- 核查只生成标注，不自动改词；
- Qwen 等兼容结论有真实响应证据。

## 14.6 UI

- 保留 3.0 字体、配色和纸张美学；
- 无 RC2 永久底栏；
- 无 3.0 RC 顶部按钮矩阵；
- 无空白幽灵按钮；
- 无一条词项三个常驻操作按钮；
- 无点击详情导致整表重绘；
- iPhone 与桌面布局密度分别设计。

---

# 十五、最终议定结论

真 3.0 的定义为：

> 以 3.0 Schema、词域、短语、繁体释义、迁移和 AI 内核为数据基础；以 3.0 米色纸张与墨绿视觉为美学基础；以 2.4.1 已验证的连续浏览、复制、PIN、定位和审阅行为为交互经验；重新构建一套不暴露内部复杂度、不增加永久底部工具栏、并适合 iPhone 主屏幕 PWA 的交互系统。

制作时：

- 3.0 RC 是工程和视觉基线；
- 2.4.1 是行为参考；
- RC2 是禁止回归样本；
- 历史 PDF 是需求演进证据，不是静态规格；
- 本报告定稿后才进入代码制作；
- 最终只交付完整源码 ZIP。
