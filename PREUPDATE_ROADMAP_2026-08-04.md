# Vocabulary Index 三阶段预更新路线封装


> **历史状态（2026-08-08）**：本文件已被 `REQUIREMENT_BASELINE_4.0.0.md` 取代。原 3.6.0 与 4.0.0 不再作为两个独立版本；其仍有效的需求已统一进入 Vocabulary Index 4.0.0。本文仅保留用于追溯当时决策，不再作为当前实现门禁或实现细节约束。


- 决策日期：2026-08-04
- 当前正式实施：3.5.2
- 后续状态：3.6.0 与 4.0.0 均为暂定方案，必须等待 3.5.2 真机验收后重新确认
- 直接代码基线：3.5.1 Clean Rebuild，ZIP SHA-256 `66a43ed0dc2f83cc789e6003b96b21bea1cecf8672a0f36ac7ed1d03ada9cd38`

本文件封装三次预更新的现行决策，但只授权当前完整包实施 3.5.2。后两次不得因为写入本文件而自动进入开发。

---

# 一、3.5.2：真机运行时与布局收口（本包已实施）

## 目标边界

不改变：

- Backup Schema 5；
- IndexedDB version 4；
- Seed revision 3；
- VIX version 1；
- Seed 实体、Membership、PhraseToken；
- PIN、标注、学习日期、释义和 API 配置的归属。

只处理 3.5.1 真机截图和历史交联审计中确认的运行时、几何和状态问题。

## 已实施内容

1. 一级表项改为序号、词汇信息栈、控件信息栈三套独立纵向系统；
2. 繁体释义与独立域来源采用相同次级行指标，不再互相制造隐藏占位；
3. 来源单行右对齐并省略，序号始终独立纵向居中；
4. sticky 小标题与顶部栏／字母栏有效底边零缝隙；
5. 浏览锚点达到长按阈值后先保存，抬手后才显示不可选择 Toast；
6. 底部工具栏试行固定 58px，取消额外安全区白带，禁用按钮只灰图标；
7. native dialog 根层稳定覆盖完整页面，内部卡片才跟随 VisualViewport；
8. 字母／日期、词汇／短语普通切换统一初始化到顶部且全部分组收起；
9. 只有真实递归返回恢复 view kind、mode、日期月份、滚动、展开组和关系；
10. 日期中的具体“日”和“未标注”可折叠，年／月保持结构标题；
11. 字母轨道人工横滑采用状态锁：页面顶部 A 与页面底部 # 均不因固定延迟、轻微 scrollY 变化或橡皮筋回弹强制归位；
12. Overlay RAF、折叠补偿、查询菜单和关系目标菜单的几何更新进一步收口；
13. Manifest 冷启动颜色统一为 `#fafafa`；
14. 最新样式层由 `v3.5.2.css` 替换 `v3.5.1.css`，不叠加第五个最新覆盖层。

## 真机验收决定后续

必须重点验证：

- 表项四种元信息组合；
- sticky 零缝隙；
- 顶部 A 和底部 # 的人工字母轨道锁；
- 长按文本选择；
- 58px 底栏与系统上滑手势；
- 弹窗白带、首帧抖动和键盘；
- 普通切换初始化与递归返回；
- 日期折叠。

未通过时继续以 3.5.2 为单一修复基线，不提前进入 3.6.0 或 4.0.0。

---

# 二、3.6.0：查询 Provider 与搜索语义（暂定）

## 查询入口顺序

```text
Oxford → Collins → Groq → ChatGPT
```

## Provider 契约

### Oxford

- 只传当前英文；
- 保持外部权威词典入口；
- 不附加完整 Entry JSON。

### Collins

- 同时考虑 COBUILD Advanced Learner’s Dictionary 与 English–Traditional Chinese；
- 使用用户自己的 API Key；
- 应用内显示等待／结果／错误；
- 关闭弹窗即 Abort，并拒收迟到响应；
- 结果只临时显示，不写入 Seed，不保存查询历史；
- 必须先用真实 Key 在 iPhone standalone PWA 验证 CORS；
- 若静态 Pages 前端无法直连，降级为 Collins 外部网页入口，不引入代理。

### Groq

- 使用设置中当前选定模型；
- 返回紧凑词汇核查卡；
- 第一版不流式，完成后一次性显示；
- 同一时刻只允许一个应用内 Groq／Collins 查询，新查询先取消旧查询；
- 每次查询有独立 Session ID、AbortController 和关闭令牌；
- 关闭后任何迟到响应不得更新 UI、Toast 或数据。

### ChatGPT

- 继续调用 `AI查询` iOS 快捷指令；
- 改用紧凑上下文：英文、Entry kind、当前域、当前词表、繁体释义及来源、有限直接关系、跨域冲突提示；
- 排除 PIN、学习日期、标注、全部 Membership、原始 PhraseToken 和全局完整同形实例对象；
- 解决当前最长约 30k URL 的真实传输风险。

## API Key 边界

用户接受单设备自用场景下在浏览器端保存和使用 Groq／Collins Key 的风险。实现必须继续使用应用命名空间、严格 DOM 构建、CSP 和无日志策略，但不得把浏览器存储描述为真正秘密存储。

## 搜索范围

不能阉割现有范围。所有入口提供同一套完整可选范围，只改变默认选中项：

- 全部内容；
- 全局词汇总表；
- 全局短语总表；
- 未来全局非结构表；
- 每个独立域整体；
- 结构性域词汇总表；
- 结构性域短语总表；
- 非结构性域内容总表；
- 每个具体普通词表／内容词表。

普通词表作为搜索范围时，始终同时搜索该 Collection 的词汇和短语 Membership，不因搜索从词汇页或短语页打开而改变语义。入口只决定默认范围：首页默认全部内容，具体页面默认自身完整范围。

## 进入条件

3.5.2 的 dialog、VisualViewport、取消、返回和底部停靠行为必须先在 iPhone 17 standalone PWA 通过验收。

---

# 三、4.0.0：新 Seed 内容世代与 Domain 模型（暂定）

## 版本与替换策略

- 使用 4.0.0，不使用 3.7.0；
- 新 Seed 是内容世代全量替换，不使用现行“只增不改”合并；
- 删除旧 Domain、Collection、Entry、Membership、PhraseToken、用户自建内容及所有内容绑定状态；
- 清除 PIN、学习日期、标注、浏览位置、展开状态和 Undo／Redo；
- 保留 Groq／Collins API Key、模型选择和一般显示偏好；
- 检测到新内容世代后阻断旧内容，用户先选择下载／不下载完整备份，再确认替换；
- 在一个可回滚事务中清空、写入、校验并切换 Seed revision。

## 词表内容范围

此前讨论的 A1／A2／B1／B2／C1／AWL／NAWL／CET／TEM／COCA 等只属于候选方向，不是已经批准的最终 Seed 清单。

每张表必须重新调研并冻结：

- 权威定义和具体版本；
- 原始文件和来源；
- 使用／再分发许可；
- 词条、词性和简体释义来源；
- 规范化、冲突和去重；
- 与其他表的交集；
- 普通词表优先级；
- 构建报告与 SHA-256。

COCA 数据只有在明确版本、文件、来源和许可后才能进入正式 Seed。

## 释义

- 原始内容是简体中文释义；
- 只做繁体字形转换，不进行台湾／香港本地化重写；
- Seed 构建阶段生成 `glossHant`；
- `glossSource` 记录简体释义资料来源；
- 转换器版本在 Seed 构建报告中全局记录，不为每个词条制造转换 provenance。

## 同域唯一性

- 词汇和短语都按 `domainId + normalizedText` 唯一；
- 词性差异合并到同一个 Entry；
- 同一短语进入多个词表时使用多个 Membership，不复制 Entry；
- 同一 Entry 与同一 Collection 的 Membership 唯一；
- 跨域同形词和同形短语继续保留不同具体 Entry。

## 词汇性短语

短语动词、搭配、习语和固定表达分散到对应普通词表，通过 Membership 表达，不再单开一个“结构语”集中表。

## 非结构性 Domain

新增创建时选择且创建后不可改变的 `contentMode`：

- 结构性域：词汇总表、短语总表、普通词表的词汇／短语页面；
- 非结构性域：只有一个域内内容总表，普通内容词表使用统一页面。

非结构性域用于句型、语法框架、模板表达、话语标记等非词汇型学习内容，各类各自一个内容词表。

UI 规则：

- 底部词汇／短语按钮保留原槽位并灰度禁用，不隐藏；
- 新增全局非结构表，聚合所有非结构性域的具体 Entry；
- 首页同时保留全局词汇总表、全局短语总表和全局非结构表；
- 搜索范围加入全局非结构表、非结构性域和内容词表；
- Entry 仍保留 kind／内容类型等属性，但浏览 UI 不据此分页。

## 不参与关联开关

新增可修改的 Domain 投影开关：

- 开启后该域不作为关系来源或目标；
- 从关系菜单和 ChatGPT／Groq 关系上下文中隐藏；
- 底层 PhraseToken／关系资料不删除；
- 关闭后重新进入关系计算。

## 暂定关系语义：全局直接对称关系＋同域优先导航

“必须先在同域成立再扩展域外目标”的 R-B 方案已经撤回，因为它会使“当前域无目标、域外有直接目标”这一既有三态失效，并依赖不稳定的同域代理。

候选正式方案：

1. 所有参与关联的结构性域共享：
   - `wordIndex: normalizedText → 具体词汇 Entry 集合`；
   - `phraseTokenIndex: tokenNormalizedText → 具体短语 Entry 集合`。
2. 词汇与短语只要满足精确规范词元匹配，就直接形成对称具体 Entry 关系，不要求源域存在相反类型 Entry，也不经过同域代理；
3. 导航保持既有三态：
   - 当前域有合法普通表投影时采用同域层；
   - 当前域为空时采用域外后备层；
   - 当前采用层有多个目标时使用第三多目标图标和菜单；
4. 系统总表只作为跳出来源，不作为跳入目标；
5. 多目标排序继续为独立域顺序 → Collection 顺序 → 稳定名称／标识。

示例：A 域只有词汇 `access`，B 域只有短语 `have access to`。双方直接形成跨域对称关系，均属于“本域无目标、域外有目标”，不需要任何同域中介。

## 性能边界

只以用户当前 iPhone 17 标准版为正式性能和真机验收设备，不为其他设备增加设计负担。未来换设备时再单独适配，但仍禁止明显的算法灾难和数据可靠性退化。

---

# 四、版本与交付纪律

- 后两次更新不能因本路线存在而被视为已批准实现；
- 3.5.2 真机验收可以修改、合并、延后或取消 3.6.0／4.0.0；
- 每个正式版本必须交完整源码 ZIP、当前生命周期文档、FILE_MANIFEST、SHA256SUMS 和 ZIP SHA-256；
- 最终 ZIP 必须重新解压到全新目录并重新运行全部自动测试；
- 自动测试通过不得表述为真实 iPhone 已验收。
