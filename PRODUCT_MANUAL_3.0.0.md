# Vocabulary Index 3.0.0 产品功能手册

版本：3.0.0  
主要平台：iPhone Safari / 添加到主屏幕  
辅助平台：Windows Chrome  
数据模式：本地优先，业务数据保存于浏览器 IndexedDB

---

## 1. 产品定位

Vocabulary Index 是词汇索引、连续定位和复制工具，不替代词典。日常主流程是：

1. 打开词表；
2. 展开目标字母；
3. 浏览英文和当前词表词性；
4. 点按英文复制；
5. 使用现有快捷指令打开牛津词典并粘贴查询；
6. 返回应用后继续原位置。

词域、短语、繁体释义、导入、备份和 AI 属于维护与增强能力，不改变词条主体“点按即复制”的固定语义。

---

## 2. 安装与更新

### 2.1 GitHub Pages 部署

将完整源码包解压后的全部文件放到仓库根目录。`index.html`、`manifest.webmanifest`、`sw.js` 必须直接位于根目录，不得额外套一层文件夹。

GitHub Pages 发布后，首次打开应看到 `Vocabulary Index 3.0`。建议部署后完全关闭旧 Safari 页面和旧主屏幕实例，再重新打开。

### 2.2 添加到 iPhone 主屏幕

1. 使用 Safari 打开 Pages 地址；
2. 点击分享；
3. 选择“添加到主屏幕”；
4. 从主屏幕图标启动。

### 2.3 应用更新

新 Service Worker 安装完成后，顶部出现“新版本已准备好”：

- **立即更新**：激活新版本并重新载入；
- **稍后更新**：本次隐藏提示，当前页面继续运行旧版本。

应用不会在用户操作过程中静默强制刷新。

---

## 3. 首页

首页按词域展示普通词表，并为每个词域提供短语索引入口。

### 3.1 词表卡片

卡片显示：

- 词表名称；
- 可选说明；
- 当前可见词项数量。

点击卡片直接进入词表，不需要先进入词域详情页。

### 3.2 顶部操作

- **搜索**：在当前词表、当前词域或全部词域中检索；
- **设置**：Groq、序号、词库管理、完整备份和版本信息；
- **管理**：新建词域、词表，调整已有结构；
- **待核查**：存在 AI 标注时，从首页开始跨词表审阅。

### 3.3 短语入口

每个词域自动拥有一个系统短语索引。普通词可在详情中查看相关短语；短语可查看已经收录的组成词。

---

## 4. 词表浏览

### 4.1 字母分组

- 普通词表按 A–Z 和 `#` 分组；
- 字母默认折叠；
- 点击字母标题局部展开或收起；
- 点击字母导航会展开目标字母并滚动到对应分组；
- 不会因为展开一个字母而重建整张词表。

短语索引按词域内确定性顺序直接显示，不使用字母折叠。

### 4.2 词条行

主列表只突出：

- 可选序号；
- 英文单词或短语；
- 当前词表对应的词性/来源标签；
- 已设置 PIN 时的低干扰标记；
- 有 AI 标注时的“待核查”；
- 单一 `⋯` 操作入口。

主列表默认不显示繁体释义、全部来源和短语关系。

### 4.3 点按复制

点击词条主体只复制英文文本：

- 复制成功显示短时提示；
- 同时记录该词条为上次位置；
- 不弹出详情；
- 不改变字母展开状态；
- 不自动打开外部应用。

Safari Clipboard API 不可用时，应用使用兼容复制路径。

---

## 5. PIN 连续导航

### 5.1 设置 PIN

点击词条右侧 `⋯`，选择“设置 PIN”。同一词项最多一个 PIN，PIN 保存其所在词表上下文和顺序。

### 5.2 导航方式

当前词表存在 PIN 时，顶部栏下方出现持续可达的 PIN 控制器：

- `‹`：上一个 PIN；
- 中间词汇：重新定位当前 PIN；
- `›`：下一个 PIN；
- 显示当前位置，例如 `PIN 2/12`。

跳转后 PIN 控制器仍留在视口内，可以继续前后导航，不需要返回词表顶部。目标字母自动展开，目标词条只产生一次滚动和短时高亮。

### 5.3 与标注审阅的关系

PIN 与 AI 标注审阅共用上下文区域，二者互斥。进入标注审阅后暂时隐藏 PIN；退出审阅后恢复。

---

## 6. 上次浏览位置

应用在两种情况下记录位置：

- 成功复制词条时立即记录；
- 手动滚动停止后，防抖记录视口内第一个稳定可见词条。

普通进入词表不会强制跳转。需要恢复时：

- 词表顶部出现“继续上次位置”；或
- 右上角 `•••` 中选择“跳到上次浏览位置”。

恢复会自动展开目标字母，并执行一次定位。

---

## 7. 搜索

### 7.1 检索范围

搜索层支持：

- 当前词表；
- 当前词域；
- 全部词域。

从首页打开搜索时，默认使用全部范围；从词表打开时，默认使用当前词表。

### 7.2 英文和本地中文检索

输入英文后即时匹配规范词形和近似拼写。启用繁体释义的词域也可通过已保存释义参与本地检索。

### 7.3 AI 中文联想

输入中文概念时可主动点击“AI 中文联想”，让当前 Groq 模型生成英文检索词，再在本地数据库内搜索。AI 不直接新增或修改词项。

关闭搜索或输入变化后，旧请求结果不会覆盖新状态。

### 7.4 搜索结果定位

点击搜索结果：

- 关闭搜索层；
- 进入目标投影词表；
- 自动展开目标字母；
- 单次滚动并高亮目标词条。

---

## 8. 词条操作与详情

点击 `⋯` 打开简洁动作菜单。

### 8.1 动作菜单

可用操作包括：

- 设置或取消 PIN；
- 编辑词项；
- 查看详情；
- 添加相关短语；
- 审阅当前 AI 标注；
- 从当前词表移除；
- 彻底删除词项。

“从当前词表移除”只删除当前 Membership；若普通词不再属于任何普通词表，会按数据规则清理孤立词项。“彻底删除”会同时删除来源、PIN、标注和短语索引关系。

### 8.2 详情

详情与动作菜单分离，按需显示：

- 英文；
- 可选繁体释义；
- 所属普通词表及对应词性；
- 普通词的相关短语；
- 短语的组成词。

点击相关短语或已收录组成词可直接定位。

---

## 9. 新增、编辑和去重

### 9.1 域内唯一

同一词域内规范化英文唯一。规范化包括大小写、空格、弯引号、连字符和不可见字符处理。

同一个词再次加入：

- 不创建重复 Entry；
- 在目标普通词表建立 Membership；
- 同一词表出现不同词性时，去重后合并词性标签。

不同词域可以存在同形词。

### 9.2 编辑

编辑当前词表中的词项时可修改：

- 英文；
- 当前词表词性；
- 启用时的繁体释义。

词性属于当前 Membership，不改变 Entry 身份。英文改名发生域内冲突时操作会被拒绝。

### 9.3 短语

- 短语必须包含至少两个英文词元；
- 系统短语表只接受短语；
- 从普通词添加相关短语时，短语必须精确包含该词元。

---

## 10. 导入与导出

### 10.1 支持格式

- TXT；
- Markdown；
- CSV；
- JSON 词项数组；
- 2.x 或 3.0 完整 JSON 备份。

格式定义见 `DATA_FORMATS.md`。

### 10.2 安全导入流程

普通导入采用：

1. 读取；
2. 解析；
3. 校验；
4. 预览；
5. 用户确认；
6. 单事务提交。

解析和预览完成前不会修改 IndexedDB。可选择合并或替换当前词表。

### 10.3 当前词表 CSV

词表更多菜单可导出当前词表 CSV，包含英文、当前词表词性和可用释义。

---

## 11. 完整备份与恢复

### 11.1 导出完整 JSON

设置中选择“导出完整 JSON”。完整备份包含：

- 词域；
- 词表；
- 词项；
- Membership；
- 短语索引；
- PIN；
- AI 标注；
- 显示与位置设置。

Groq API Key 和撤销历史不进入备份。

### 11.2 恢复

恢复会整体替换当前业务数据并清空撤销历史。恢复前必须先导出当前备份，并关闭同站点的其他 Safari/PWA 实例。

2.4.1 完整 JSON 可迁移到 Schema 3。迁移后应立即导出新的 3.0 完整 JSON。

---

## 12. 撤销与重做

词表更多菜单提供撤销和重做。业务修改通过统一事务历史记录，包括新增、编辑、导入、来源调整、PIN 和标注相关操作。

以下内容不作为普通历史事务：

- 浏览位置；
- Groq API Key；
- 模型目录；
- 当前滚动和临时弹窗状态。

---

## 13. Groq 设置与模型目录

设置中可以：

- 保存或清除 Groq API Key；
- 主动刷新模型目录；
- 选择模型；
- 查看最近刷新时间。

模型目录只在用户点击刷新时请求网络。新发现模型与历史模型合并保存；当前未返回的历史模型会保留并标记为“历史”。应用不按 Qwen、Llama 或 OSS 名称写死策略分支。

API Key 只保存在当前浏览器 localStorage，不进入导出备份。

---

## 14. AI 新增

在词表更多菜单选择“AI 新增”：

1. 输入生成要求；
2. 请求当前模型生成结构化候选；
3. 预览候选；
4. 用户确认后合并导入。

候选写入前仍执行域内规范词形去重和词性合并。AI 输出不会绕过 Store 数据规则。

---

## 15. AI 核查

### 15.1 启动前确认

启动页显示：

- 核查范围；
- 词项数量；
- 预计批次数；
- 当前模型。

核查只生成 Annotation，不直接修改词汇。

### 15.2 任务控制

任务运行时支持：

- 查看批次进度；
- 暂停；
- 继续；
- 取消；
- 收起为临时任务胶囊。

每批完成后立即保存该批稳定词项的标注。取消、网络失败或后续批次失败，不会清除已完成结果。

### 15.3 限流与错误

应用读取可用的速率限制/重试信息，对 429、临时 5xx、超时和网络错误进行有限重试。真实额度仍取决于 Groq 账户和所选模型。

---

## 16. AI 标注审阅

存在标注时可从首页、词表或词条动作菜单进入审阅。

上下文控制器提供：

- 上一条；
- 下一条；
- 编辑；
- 取消标注；
- 退出审阅。

审阅可跨词表导航。目标字母自动展开；取消当前标注后进入相邻有效项。退出后恢复 PIN 控制器。

---

## 17. 设置与显示

序号模式：

- 不显示；
- 每个字母重新编号；
- 词表内连续编号。

设置页还提供：

- 词域和词表管理；
- 短语索引入口；
- 完整备份与恢复；
- 当前版本和 Schema 信息。

---

## 18. 数据安全与多实例

- 所有业务数据保存在 IndexedDB；
- 每次业务写入检查数据修订号和实体前态；
- Safari 标签页和主屏幕 PWA 通过 BroadcastChannel 通知变化；
- 检测到外部实例更新后，当前实例重新加载数据；
- 不建议两个实例同时执行导入、恢复或大规模编辑；
- 清理 Safari 网站数据会删除本地业务数据和 Groq 设置，必须提前导出完整 JSON。

---

## 19. 离线能力

首次完整在线加载后，应用壳、Seed、图标和运行模块由 Service Worker 缓存。离线时可继续：

- 浏览；
- 复制；
- PIN；
- 本地搜索；
- 编辑和数据维护。

Groq 模型刷新、AI 新增、AI 中文联想和 AI 核查需要网络。

---

## 20. 常见问题

### 页面仍显示旧版本

1. 等待顶部更新提示并点击“立即更新”；
2. 完全关闭 Safari 标签页和主屏幕应用；
3. 重新打开；
4. 仍异常时检查 GitHub Pages 是否完成发布。

不要在未导出备份的情况下直接清理网站数据。

### 点词没有复制

确认页面通过 HTTPS/GitHub Pages 打开。Safari 权限或系统限制导致 Clipboard API 不可用时，应用会尝试兼容复制；仍失败会显示错误。

### PIN 跳转后找不到控制器

正常版本中 PIN 控制器应持续位于顶部栏下方。进入标注审阅时它会暂时隐藏，退出审阅后恢复。

### AI 模型为空

先填写有效 Groq API Key，再点击“刷新模型目录”，然后保存所选模型。

### AI 核查中断

已完成批次的标注应保留。重新进入词表后可审阅已有标注，再按需要重新启动核查。

---

## 21. 发布前人工验收

自动测试不能替代 iPhone 真机。部署后必须按 `tests/MANUAL_CHECKLIST.md` 验证：

- 主屏幕安装和 Safe Area；
- 5,005 词 Seed 首次初始化；
- 字母局部展开；
- 复制及返回后位置保持；
- PIN 连续前后导航；
- 上次位置；
- 搜索定位；
- 动作与详情分离；
- 导入预览和恢复；
- 真实 Groq 请求；
- AI 暂停、取消和部分结果；
- PWA 更新提示；
- 离线冷启动；
- Safari 与主屏幕双实例。
