# Vocabulary Index 3.0.1 产品功能手册

主要平台：iPhone Safari / 添加到主屏幕  
辅助平台：Windows Chrome  
数据模式：本地优先，业务数据保存于 IndexedDB

## 1. 首页

首页以同一种卡片展示普通词表和短语表。卡片只保留名称、必要副标题和数量；数量不附加重复说明。

- 点击卡片进入对应词表。
- 搜索按钮打开三级范围搜索。
- “管理”进入拖动式词库管理。
- 右上角更多按钮进入设置、备份与 AI 配置。

## 2. 普通词表与短语表

两者完全同构：

- 都按 A–Z / `#` 分组；
- 都使用相同字母导航、折叠方式、列表行和位置恢复；
- 普通词表的一级行仅为词汇；
- 短语表的一级行仅为短语。

底层 Entry 均为一级实体，但 UI 不把短语混入普通词表，也不把词汇混入短语表。

## 3. 行操作

每行包含：

- 可选序号；
- 英文文本；
- 关系展开按钮（存在关系时）；
- 直接 PIN 按钮；
- `⋯` 低频操作。

点击英文主体只复制英文，不打开详情、不改变列表状态。

### 3.1 关系展开

- 词汇向下展开相关短语；
- 短语向下展开组成词汇；
- 不显示关系标题或“未收录”等额外注释；
- 子项按英文字典序排列；
- 点击子项复制其英文文本。

### 3.2 PIN

点击行内 `PIN` 立即设置或取消，不打开弹窗。

当前词表存在 PIN 时，顶部下方出现 sticky PIN 导航：

- `‹` 上一个；
- 中间文本重新定位当前 PIN；
- `›` 下一个。

跳转后导航仍可继续操作。

### 3.3 更多菜单

`⋯` 只收纳低频操作：

- 编辑；
- 为词汇添加短语；
- 核查当前标注；
- 从当前普通词表移除；
- 删除。

不再提供独立“详情”页面。词性和来源关系保留在数据层，不进入日常界面。

## 4. 上次位置

每个词表独立保存上次位置。

- 成功复制后立即保存；
- 手动滚动停止后保存稳定可见行；
- “继续上次位置”直接位于当前词表主界面；
- A1 的继续入口只恢复 A1，不会进入短语表或其他词表；
- 更多菜单中不重复提供同名入口。

## 5. 搜索

搜索范围分为三级：

1. 全部；
2. 某一词域；
3. 某一具体词表。

从词表中打开时默认限定当前词表；从首页打开时默认全部。

- 英文即时本地匹配；
- 已保存繁体释义可参与本地中文匹配；
- 中文输入可主动使用 AI 联想；
- 点击结果进入正确投影词表并定位一次。

在 iPhone 上，打开搜索会锁定背景滚动，并根据 `visualViewport` 调整弹层，避免键盘将背景页面整体向上推移。关闭后恢复原滚动位置。

## 6. 管理词库

管理页直接展示词域和全部词表结构。

- 所有词表（含短语）都可拖动改变首页顺序；
- 普通词表之间的相对顺序同时决定词汇投影优先级；
- 多词域时可拖动词域；
- 每行 `⋯` 处理重命名、删除等低频操作；
- `＋` 新建词表或词域；
- 不再通过“提高/降低优先级”按钮逐次移动。

短语表由词域自动维护，可参与显示排序，但不参与普通词汇的投影优先级计算。

## 7. 弹窗导航

弹窗具有层级状态：

```text
操作菜单 → 编辑 / AI / 设置子层 → 关闭子层 → 回到原操作菜单
```

同一个表单弹窗继续深入时，应用保存父层标题、内容、输入和滚动状态。关闭当前层只返回上一层，而不是丢失整个操作上下文。

## 8. 添加、编辑与导入

- 普通词表新增入口只接受单个词汇；
- 短语表新增入口只接受多词短语；
- 为词汇“添加短语”时，短语必须精确包含该词元；
- 编辑界面不显示词性；
- 旧词性仍保留在 Membership 数据中；
- 普通词表导入中的多词表达会进入词域短语表，不计入普通词表数量。

导入支持 TXT、Markdown、CSV 和 JSON，修改数据库前先解析和预览。

## 9. 数据计数

- 首页总数只计算词汇；
- 普通词表数量只计算可见词汇；
- 短语表数量只计算短语；
- 短语不再增加 A1/A2 等普通词表数量。

内置迁移结果见 `DATA_REPORT.md`。

## 10. AI

### 模型目录

- 仅主动刷新时联网；
- 当前目录与历史发现模型合并；
- 当前选择持久保存；
- 不按模型品牌写死兼容分支。

### AI 新增

- 根据当前词表生成候选；
- 候选先预览再导入；
- 导入仍执行词域内去重和词汇/短语类型隔离。

### AI 核查

- 启动前显示范围、数量、批次和模型；
- 只生成待审阅标注，不直接修改英文；
- 支持暂停、继续、取消和收起；
- 每批完成后立即保存；
- 取消或后续失败保留已完成批次。

## 11. 备份与迁移

完整 JSON 包含：

- Domains；
- Collections；
- Entries；
- Memberships；
- PhraseTokens；
- PIN；
- Annotations；
- Settings。

Groq API Key 不进入备份。

2.4.1 升级时，多词表达转换为短语实体；原普通词表 Membership 可保留用于迁移审计，但 3.0.1 投影不会再把短语显示在普通词表中。

## 12. PWA 更新

从旧版首次进入 3.0.1 时，应用可能自动刷新一次以清除旧应用壳缓存；词库、PIN、标注和设置所在的 IndexedDB 不会被删除。

新 Service Worker 安装后显示更新提示：

- “立即更新”激活新版本并重载一次；
- “稍后更新”继续当前会话；
- 安装阶段不静默强制刷新。

本版本使用独立缓存名，避免旧 HTML 与新 JS 混合启动。
