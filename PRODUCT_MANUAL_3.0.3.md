# Vocabulary Index 3.0.3 产品功能手册

## 首页

首页包含三个层级：

1. **全局总表**：所有词域中的普通词按英文规范形式全局去重。
2. **词域总词表**：对应词域内全部普通词，域内唯一。
3. **普通词表与短语表**：普通词表按优先级分配普通词；短语表只显示短语。

卡片中的数字直接表示数量，不附加“词项”等说明文字。

## 普通词表

- 一级行只显示普通词。
- 点击英文主体复制英文。
- 点击关联按钮展开相关短语。
- 点击展开后的短语，跳转到该短语所在词域的短语表。
- PIN 位于行内，可直接设置或取消。
- `⋯` 用于编辑、添加短语、标注和删除等低频操作。
- 词性保留在数据层，不在日常 UI 中显示。

## 短语表

- 一级行只显示短语。
- 不显示首字母区块标题。
- 点击英文主体复制短语。
- 点击关联按钮展开组成词。
- 已收录的组成词可跳转到对应词域总词表。
- 未收录的组成词点击后复制，不制造虚假跳转。
- PIN 与 `⋯` 的位置和行为与普通词一致。

## 总词表

### 词域总词表

显示词域中的全部普通词，不受 A1、A2、AWL 等普通词表排序影响。适合作为短语组成词的稳定跳转目标。

### 全局总表

将所有词域中的普通词按规范英文全局去重。多个词域存在同形词时，按词域排序选取展示代表；关联短语会合并所有同形词在各词域中的关系。

总表是派生视图，不会复制数据，也不改变原词表来源。

## 搜索

搜索范围包括：

- 全部；
- 全局总表；
- 单个词域；
- 词域总词表；
- 具体普通词表；
- 短语表。

在某个词表内打开搜索时，默认限定当前词表。搜索不自动弹出键盘；用户点击输入框后才输入。关闭搜索后恢复原页面位置。

## PIN

- 行内 PIN 点击后立即显示状态变化。
- 每个词表、短语表、词域总词表和全局总表拥有独立 PIN 上下文。
- PIN 前后导航在存在 PIN 时持续可达。
- PIN 跳转不会跨入其他词表。

## 上次位置

每个列表分别保存自己的上次位置。A1、短语、词域总词表和全局总表互不混用。

## 管理

- 词域可拖动排序。
- 普通词表与短语表可在词域内拖动排序。
- 普通词表之间的顺序决定重复普通词的首个投影位置。
- 全局总表和词域总词表是固定派生视图，不参与拖动。
- 多窗口同时修改时，应用自动同步并重试一次；仍无法完成时才提示重新操作。

## 数据安全

- 主要数据保存在 IndexedDB。
- 应用壳缓存更新不删除 IndexedDB。
- 支持完整 JSON 导出与恢复。
- 部署新版本前应保留完整 JSON 备份。

## 性能行为

3.0.3 将词汇—短语关系在启动时建立为内存索引。展开字母、显示关系按钮和展开关系时不再复制完整数据库。

搜索连续输入会合并为一次本地查询；PIN 只提交一条 PIN 记录，不触发全库投影和短语索引重建。编辑、导入、恢复和结构管理属于低频一致性操作，仍可能比普通浏览操作耗时。
