# Vocabulary Index 3.0.4 产品功能手册

## 1. 产品定位

Vocabulary Index 是本地优先的英语词汇与短语索引。主要使用路径是：进入词表、浏览或定位英文、点按复制，再到外部词典查询。应用把高频浏览与低频管理分离，数据默认保存在当前浏览器的 IndexedDB 中。

## 2. 首页结构

首页按以下层级展示：

1. **全局总表**：所有词域中的普通词汇全局去重后形成的派生列表。
2. **全局短语表**：所有词域中的短语全局去重后形成的派生列表。
3. 每个词域固定先显示：
   - **总词表**：该词域内全部普通词汇；
   - **短语**：该词域内全部短语；
   - 用户建立的普通词表，按管理页中的拖动顺序排列。

全局表和词域总表均为运行时派生视图，不复制词汇数据，也不改变普通词表的优先归属。

## 3. 词表浏览

### 3.1 普通词汇表

普通词汇表、词域总词表和全局总表按字母分组。字母栏最前方固定显示一个准星按钮：

- 尚无浏览位置时，按钮保留但不可用；
- 复制或稳定浏览过词汇后，按钮原位启用；
- 点按后只恢复当前词表自己的上次位置。

### 3.2 短语表

词域短语表和全局短语表使用连续列表，不生成多余的首字母标题。顶部仍保留准星按钮，用于恢复该短语表自己的位置。

### 3.3 表项结构

一级表项包含：

- 英文主体：点按只复制英文；
- 繁体释义：仅在对应词域开启且存在释义时显示；
- 关系按钮：存在关联时显示；
- PIN：直接设置或取消；
- 更多：编辑、标注和删除等低频操作。

词性保留在数据层，但不在浏览界面显示。

## 4. 词汇与短语关系

### 4.1 词汇展开短语

点按词汇行的关系按钮，在当前行下展开相关短语，按英文字典序排列：

- 点按短语文字：复制短语；
- 点按短语右侧跳转按钮：
  - 普通词表或词域总词表进入当前词域短语表；
  - 全局总表进入全局短语表。

### 4.2 短语展开词汇

点按短语行的关系按钮，在当前行下展开组成词汇，按英文字典序排列：

- 点按词汇文字：复制词汇；
- 点按右侧跳转按钮：进入该词在对应词域中优先级最高的普通词表；
- 不再跳入词域总词表；
- 组成词没有普通词表来源时，不显示虚假跳转按钮。

全局短语涉及多个词域的同形词时，会要求选择目标词域和普通词表。

## 5. 跳转反馈

搜索、关系、上次位置和 PIN 使用同一定位系统：

- 目标已经完整位于可视区域时，不移动页面，只高亮；
- 长距离或跨表跳转时，将目标放到有效内容视口上部约 38% 的阅读锚点；
- sticky 标题栏、PIN 条和字母栏不会遮挡目标；
- 目标行使用浅墨绿高亮和左侧定位线；
- PIN 跳转保留较弱选中状态，直到用户主动滚动、复制或跳往其他位置。

## 6. PIN

PIN 直接位于一级表项：

- 点按图钉即可设置或取消；
- 有 PIN 时显示持续可达的前后导航条；
- 前后跳转后仍可继续操作；
- PIN 导航使用与其他图标一致的 SVG 图标系统。

## 7. 搜索

搜索范围支持：

- 全部；
- 全局总表；
- 全局短语表；
- 独立词域；
- 词域总词表、短语表或具体普通词表。

在具体词表中打开搜索时，默认限定当前词表。英文即时搜索；中文可以匹配本地繁体释义，也可以按需使用 AI 联想。搜索结果跳转使用统一阅读锚点与目标高亮。

## 8. 繁体释义

在词域管理中启用繁体释义后：

- 释义显示在英文后方的行内空白区域；
- 保持单行，过长时省略；
- 不增加词表行高度；
- 点按释义不会替代英文复制操作；
- 全局表优先显示词域顺序靠前且已启用的非空释义。

关闭显示不会删除已保存释义。

## 9. 词库管理

管理页中：

- 词域可以拖动排序；
- 每个词域的总词表固定第一位；
- 词域短语表固定第二位；
- 普通词表只能在固定项之后拖动；
- 普通词表顺序决定重复词汇优先显示在哪个普通词表；
- 重命名、删除等低频操作放在各项的更多菜单中。

## 10. AI 与数据

应用支持 AI 新增、AI 核查、暂停、继续、取消和分批保存。模型目录持久化，只有主动刷新时才重新联网获取。

支持完整 JSON 备份恢复、CSV 导出，以及 TXT、Markdown、CSV、JSON 导入。清理浏览器网站数据前必须先导出完整备份。

## 11. PWA 更新

新版本安装完成后由用户确认激活。Service Worker 使用版本化应用壳，避免 HTML、JavaScript 和 CSS 混用不同构建。更新应用壳不会删除 IndexedDB 中的词库、PIN、标注或上次位置。
