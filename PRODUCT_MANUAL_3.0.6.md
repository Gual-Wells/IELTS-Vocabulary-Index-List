# Vocabulary Index 3.0.6 产品功能手册

## 1. 首页

首页分成三个视觉层级：页面标题、全局索引区、各独立词域区。全局和每个词域拥有独立的低对比度容器、标题和细色标；词表卡片保持同一纸张式组件。

全局固定显示：

1. 全局总表；
2. 全局短语表。

每个独立词域固定显示：

1. 总词表；
2. 短语；
3. 用户可见普通词表。

总表均为派生视图，不复制 Entry。

## 2. 计算机术语

“计算机术语”默认开启繁体释义，结构为：

```text
总词表（544）
短语（577）
计算机基础与系统（214）
软件开发与数据（197）
网络、云与安全（114）
人工智能（19）
```

四个普通词表可在固定总表和短语之后拖动排序。每个普通词只进入一个主分类；短语不拆分。

## 3. 浏览、复制与关系

- 英文主体点按复制；
- 关系按钮展开或收起；
- PIN 直接位于表项；
- 更多菜单承担编辑、删除和标注等低频操作；
- 繁体释义位于英文后方的单行空白区域；
- 关系子项文字仍然复制，右侧独立按钮跳转；
- 短语组成词进入对应普通词表，不进入词域总词表。

搜索、关联、上次位置和 PIN 共用目标高亮。长距离跳转落在有效视口上部约 38%，目标已在屏幕内时只高亮而不滚动。

## 4. 数据交换中心

入口：

```text
首页 → 设置 → 数据交换
```

### 4.1 功能

- 导入内容；
- 导出内容；
- 导出完整备份；
- 恢复完整备份。

### 4.2 内容范围

- 全局；
- 独立域；
- 词表。

选择“词表”时，先选独立域，再选普通词表或该域唯一短语表。总词表和全局派生表不会出现在目标列表中。

### 4.3 写入方式

**增量合并**：新增或更新文件中明确出现的内容，保留未出现的旧内容，不隐式删除。

**完整替换**：只替换当前选择的范围。全局替换更新全部内容结构；独立域替换不影响其他域；普通词表替换主要更新该表的 Membership；短语表替换更新该域短语。

### 4.4 导入步骤

```text
选择功能、范围、目标和方式
→ 选择 JSON
→ Worker 解析与预检
→ 查看差异和冲突
→ 自动下载恢复备份
→ 确认导入
→ 单次事务写入
```

差异预览显示新增、更新、删除、Membership、重复和冲突数量。释义冲突可统一选择保留当前值或使用导入值。

当文件声明目标与面板选择不一致时，可以使用当前选择、使用文件目标或取消。

### 4.5 内容导出与完整备份

内容导出使用 VIX JSON，只包含内容结构和来源信息，适合发送给外部工具或模型进行研究、补充和重构。

完整备份包含 PIN、位置、标注和应用设置，用于迁移与恢复。Groq API Key 不进入备份。

## 5. VIX JSON 使用方式

常见协作流程：

```text
从目标独立域或词表导出 VIX JSON
→ 连同需求交给模型或人工整理
→ 获得 merge / replace JSON
→ 数据交换中心读取并预检
→ 一次确认导入
```

格式说明见 `DATA_FORMATS.md`，Schema 见 `data/vix-json.schema.json`，示例见 `data/examples/`。

## 6. 管理与更新

管理页支持词域和普通词表拖动排序。总词表、短语表及全局派生表保持固定位置。

PWA 更新由用户确认激活。Service Worker 保证同一缓存代中的 HTML、CSS 和 JavaScript 一致。更新应用壳不会删除 IndexedDB 内容。
