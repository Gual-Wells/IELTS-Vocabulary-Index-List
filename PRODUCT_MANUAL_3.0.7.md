# Vocabulary Index 3.0.7 产品功能手册

## 1. 首页与词域

首页分为全局索引区和各独立词域容器。每个词域固定显示总词表、唯一短语表及其普通词表。

“计算机术语”包含：

- 总词表；
- 短语；
- 计算机基础与系统；
- 软件开发与数据；
- 网络、云与安全；
- 人工智能。

## 2. 繁体释义

词域开启繁体释义后：

- 一级词汇或短语表项在英文后方显示繁体释义；
- 展开后的相关短语或组成词汇同样显示繁体释义；
- 释义单行省略，不增加列表行高；
- 点击英文只复制英文；跳转使用独立图标按钮。

全局总表和全局短语表存在同形条目时，按词域显示顺序选取首个已启用且非空的繁体释义。

## 3. 数据交换中心

路径：

```text
首页 → 设置 → 数据交换
```

功能包括：

- 导入内容；
- 导出内容；
- 导出完整备份；
- 恢复完整备份；
- 还原到 Seed。

内容导入支持全局、独立域和词表三级范围，以及增量合并和完整替换。导入前在 Worker 中预检差异，确认后以单次事务提交。

## 4. 还原到 Seed

“还原到 Seed”用于恢复当前版本随应用发布的初始数据格局。

执行前：

- 必须勾选确认；
- 应用自动下载当前完整恢复备份。

执行后会恢复：

- 内置词域和词表；
- 初始词汇与短语；
- 初始繁体释义设置；
- 初始排序和应用显示设置。

同时会清除当前数据库中的：

- 用户自建内容；
- PIN；
- 上次位置；
- 标注；
- 撤销历史。

需要回退时，使用自动下载的完整恢复备份。

## 5. 完整备份与内容 JSON

- **内容 JSON**：用于词库研究、生成、增量和结构替换，不包含个人状态。
- **完整备份**：用于设备迁移和灾难恢复，包含 PIN、位置、标注和应用设置。

总词表、全局总表与全局短语表均为派生视图，不作为直接导入目标。
