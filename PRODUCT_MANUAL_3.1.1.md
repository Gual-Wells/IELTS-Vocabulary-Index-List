# Vocabulary Index 3.1.1 产品功能手册

## 1. 产品结构

Vocabulary Index 是本地优先的英语词汇与短语索引。所有可编辑内容都属于某个独立域；全局词汇总表和全局短语总表只是各独立域内容的系统聚合视图，不拥有独立新增入口或 Membership。

首页层级：

```text
全局索引
├─ 全局词汇总表
└─ 全局短语总表

独立域
├─ 词汇总表
├─ 短语总表
└─ 普通表
   ├─ 词汇区
   └─ 短语区
```

四类总表始终保持单一类型：词汇总表只显示词汇，短语总表只显示短语。普通表可同时包含词汇与短语，词汇区固定在前，短语区固定在后。

## 2. 普通表复合视图

普通表卡片显示 `N 词 · M 短语`。进入普通表后：

- 词汇区和短语区分别拥有字母导航；
- 词汇区可跳到短语区，短语区可跳回词汇区；
- 两个区共享当前排序模式；
- 两个区分别保存字母模式和日期模式的上次位置；
- 页面右下角提供返回顶部控件。

短语总表也重新采用字母导航和字母小标题，只生成实际存在内容的字母组。

## 3. 学习日期

每个一级词汇或短语行都有学习日期区域和日期刷新控件。日期格式为 `YYYY·M·D`。

只有点击日期刷新控件才会记录或更新学习日期。以下操作不会更新日期：

- 点击英文复制；
- 展开关联；
- 关联跳转；
- PIN；
- 搜索；
- 滚动或恢复上次位置；
- 返回顶部。

学习日期绑定具体 Entry；全局聚合项使用独立的全局学习记录，不伪造任一独立域中的学习动作。

## 4. 字母模式与日期模式

每个列表支持字母模式和日期模式。

### 字母模式

普通词汇、短语均按字母分组。顶部导航包含：

- 当前模式的上次位置；
- 模式切换；
- 普通表中的跨区跳转；
- 实际存在的字母。

### 日期模式

日期模式默认按最新日期在前排列，同一天内按英文字典序排列。未刷新过的内容统一放在最后的“未标注”区。

日期标题采用三级结构：

```text
2026
8 月
2 日
```

只生成实际存在记录的年、月、日标题。简易日历只允许点击当前区实际存在记录的日期；日期模式另有“跳到未标注”控件。

字母模式和日期模式拥有相互独立的上次位置。普通表的词汇区和短语区又分别保存位置，因此一个普通表最多保存四组浏览位置。

## 5. 关联展开与跳转

一级词汇可展开相关短语，一级短语可展开组成词汇。关联子项英文负责复制，独立图标负责跳转。

合法关联目标只有普通表的词汇区或短语区。四类总表只允许作为关联跳出的来源，不能成为关联跳入目标。

### 独立域内页面

- 当前独立域存在合法目标：显示域内跳转图标；
- 当前独立域没有合法目标、其他独立域存在目标：显示域外后备图标；
- 两种图标互斥；
- 没有合法普通表目标：不显示跳转按钮。

### 全局总表

全局总表不是独立域，不使用“跨域”概念。关联项使用全局下钻图标，进入某个独立域的具体普通表。多个合法目标时先选择目的地。

关联跳转服从目标普通表当前保存的字母或日期模式，但不会刷新学习日期，也不会把总表设为跳转目标。

## 6. PIN、繁体释义与更多操作

- PIN 位于一级表项中；
- 开启词域繁体释义后，一级表项和关联展开项都显示繁体释义；
- 英文主体点击后复制；
- 更多菜单提供编辑、删除、AI 核查等低频操作；
- 全局聚合项没有直接内容所有权，编辑或删除时必须先选择具体独立域实例。

## 7. 搜索

搜索支持：

- 全部内容；
- 全局词汇总表和全局短语总表；
- 整个独立域；
- 独立域词汇总表、短语总表和普通表。

全局搜索结果由独立域 Entry 聚合产生，不存在只属于全局的数据。

## 8. 数据交换中心

首页设置中的“数据交换”统一处理：

- 导入内容；
- 导出内容；
- 导出完整备份；
- 恢复完整备份；
- 还原到 Seed。

内容导入支持全局、独立域和普通表范围，以及增量合并和完整替换。普通表内容 JSON 可同时包含词汇和短语。

VIX 内容 JSON 不包含学习日期、PIN、标注和浏览位置。完整备份包含这些个人状态。

“还原到 Seed”会恢复当前版本的内置词域、词表、词汇、短语、分类和默认设置，并清除用户内容、PIN、标注、学习日期和浏览状态。执行前会生成恢复备份。

## 9. Seed 内容

3.1.1 内置两个独立域：

- 通用英语：4,995 个词汇、10 条短语；
- 计算机术语：544 个词汇、577 条短语。

通用英语 10 条短语分别归入 A1 和 A2 普通表。计算机术语 577 条短语全部且仅归入一个主领域普通表：

| 普通表 | 词汇 | 短语 |
|---|---:|---:|
| 计算机基础与系统 | 214 | 139 |
| 软件开发与数据 | 197 | 222 |
| 网络、云与安全 | 114 | 165 |
| 人工智能 | 19 | 51 |

各独立域短语总表仍包含域内全部短语；普通表 Membership 只负责分类投影，不复制 Entry。

## 10. 升级与兼容

3.0.x 数据库升级到 Schema 4 时会原位保留原有词域、词表、Entry、Membership、PIN、标注和设置，再补充 3.1.0 起的新状态存储和内置短语分类。迁移可重复执行，不应生成重复 Entry 或 Membership。

旧 VIX JSON 仍可导入。3.1.1 导出的普通表内容包可能同时含词汇和短语，旧版本不应恢复该内容包。

## 11. 运行环境

主要目标：

- iPhone Safari 及主屏幕 PWA；
- Windows Chrome。

应用不依赖远程数据库。Groq API Key 使用独立本地安全存储，不进入内容 JSON，也不由 Seed 恢复管理。
## 12. 一级表项外部查询控件

3.1.1 在每个一级词汇与短语表项末尾增加两个独立控件。关联展开子项不提供这些控件。

### 牛津英汉辞书

牛津控件只提交当前一级条目的英文纯文本，并直接调用：

```text
hk-com-oupc-oecd-lookup://x-callback-url/s?q={URL 编码后的英文}
```

该功能不经过剪贴板，也不依赖牛津快捷指令。设备必须安装支持此 URL Scheme 的牛津英汉辞书 App。

### ChatGPT

ChatGPT 控件会同步生成当前一级条目的 `vix-entry-context` JSON，并调用用户安装的快捷指令：

```text
名称：AI查询
接收：文本（同时允许 App 不影响调用）
Start conversation with ChatGPT
Temporary chat：关闭
Start Action：输入快捷指令的信息
```

发送内容包括：当前 Entry 的完整记录、所在独立域、普通表及 Membership、直接关联、学习日期、PIN、标注和相关来源。全局总表条目会发送所有独立域实例，不会虚构全局 Entry。内容不包含 Groq API Key、其他无关 Entry、全局设置、撤销历史或无关浏览位置。

查询文本要求 ChatGPT 使用网页搜索和权威原始资料核查含义、繁体中文译法、用法或技术场景，并提供重要来源。快捷指令名称必须保持为 `AI查询`。

两个控件都不会触发英文复制、学习日期刷新、PIN 变更、关联收起或浏览模式切换。

