# Vocabulary Index 3.2.0 产品功能手册

## 1. 运行定位

本版本只针对 iPhone Safari 添加到主屏幕后运行的独立 PWA。应用是单用户、单设备、本地优先工具，不提供账户、云同步或多人协作。

数据保存在当前设备的浏览器 IndexedDB。其他人打开同一公开仓库或部署地址时，只会使用他们自己的本地存储，不会访问或修改本机数据。

## 2. 首页

首页直接显示：

```text
全局
├─ 全局词汇总表
└─ 全局短语总表

独立域
├─ 词汇总表
├─ 短语总表
└─ 普通表
```

首页不再重复显示第二套产品标题。普通表卡片显示 `N 词 · M 短语`；系统总表卡片使用淡侧向渐变光晕，表示它们是系统派生视图。

## 3. 一级表项

一级词汇和短语采用两行结构：

```text
英文｜繁体释义｜学习日期
日期刷新｜标注｜关联｜PIN｜更多｜Oxford｜ChatGPT
```

- 点击英文复制；
- 英文不再任意断字；
- 超长英文使用省略，完整内容可由系统辅助信息读取；
- 开启繁体释义后，释义在第一行独立显示；
- 只有日期刷新控件更新学习日期；
- 第二行所有操作均使用固定触控区域。

## 4. 学习日期与排序

每个一级 Entry 可手动标注最近学习日期。只有点击刷新控件才更新日期。

列表支持：

- 字母模式；
- 日期模式；
- 独立的模式上次位置；
- 日期日历；
- 年、月、日三级标题；
- 未标注区；
- 返回顶部。

普通表的词汇区和短语区共享当前模式，但分别保存位置。

## 5. 普通表与总表

四类总表保持单一类型：

- 全局词汇总表：仅词汇；
- 全局短语总表：仅短语；
- 独立域词汇总表：仅词汇；
- 独立域短语总表：仅短语。

普通表同时包含词汇区和短语区，词汇在前，短语在后，两区可互相跳转。

总表是派生视图，没有独立新增入口。全局总表只是所有独立域的系统聚合，不属于任何独立域。

## 6. 关联

- 词汇展开相关短语；
- 短语展开组成词汇；
- 关联子项英文负责复制；
- 独立跳转图标负责导航；
- 总表只允许关联跳出，不允许成为关联跳入目标；
- 合法落点只能是普通表词汇区或短语区；
- 当前域有目标时只显示域内跳转；
- 当前域没有目标且其他域存在目标时才显示域外后备跳转；
- 全局总表使用全局下钻，不使用跨域语义。

## 7. PIN、标注和浏览位置

PIN、标注、学习日期和浏览位置属于个人状态，保存在完整备份中，不进入 VIX 内容 JSON。

字母模式与日期模式各自保存上次位置。普通表词汇区、短语区又分别保存，互不覆盖。返回顶部会先保留当前位置，不把顶部写成新的上次位置。

## 8. Oxford 与 ChatGPT

### Oxford

只发送当前一级条目的英文纯文本：

```text
hk-com-oupc-oecd-lookup://x-callback-url/s?q={英文}
```

### ChatGPT

调用名为 `AI查询` 的 iOS 快捷指令。快捷指令需要：

```text
接收：文本（同时允许 App 不影响）
Start conversation with ChatGPT
Temporary chat：关闭
Start Action：输入快捷指令的信息
```

发送内容为当前条目的 `vix-entry-context` JSON，包括 Entry、独立域、普通表、Membership、直接关联、学习日期、PIN、标注和来源。不会发送 Groq API Key、无关 Entry、全局设置或撤销历史。

两种外部查询均不会修改学习日期、PIN、关联展开或浏览模式。

## 9. 数据交换

设置中的“数据交换”支持：

- 内容导入；
- 内容导出；
- 完整备份；
- 完整恢复；
- 还原到 Seed。

内容 JSON 可作用于全局、独立域或普通表，并支持增量合并或完整替换。个人状态只进入完整备份。

## 10. 性能行为

长列表不会一次生成全部一级表项。应用按小块接近视口时渲染；跳转到尚未生成的条目时会立即加载目标块。

SVG、关联解析和滚动位置追踪均采用缓存或惰性处理。sticky 控件使用不透明背景，避免长列表滚动时持续计算背景模糊。

## 11. Seed 数据

- 独立域：2；
- 全局去重词汇：5,322；
- 全局短语：587；
- 计算机术语：544 词、577 短语；
- 计算机术语繁体释义覆盖：100%。

3.2.0 不改变 3.1.1 的业务数据，只更新应用版本标记。

## 12. 安装与更新

通过 HTTPS 打开部署地址，在 Safari 分享菜单中选择“添加到主屏幕”。启动主屏幕图标后使用。

更新时等待应用出现新版本提示并选择更新；若旧外壳仍在运行，完全关闭主屏幕应用后重新打开。更新不会清除 IndexedDB。
