# Vocabulary Index 3.3.0 产品功能手册

## 1. 启动与首页

从 iPhone 主屏幕图标启动。冷启动进入首页顶部；从词表返回首页时恢复离开首页前的位置。

若存在未处理 AI 标注，固定导航下方会出现黄色警告条。警告条不能关闭，只能通过“全部撤销”清除全局标注，或在词表中逐项处理至零后自动消失。

## 2. 词表顶部

- 固定紧凑导航：返回、标题、搜索、更多；
- 滚动大标题：显示词表名称和数量；
- 固定功能区与横向字母轨道分离；
- 字母模式、日期模式和未标注入口根据当前词表显示；
- 返回顶部按钮在词表中常驻。

## 3. 一级表项

### 普通词汇

轻点英文复制。英文和繁体释义都保持一行；超长时在文字区内横向滑动查看，不压缩日期和按钮。

### 短语

短短语单行；较长短语缩小字号并显示为两行。极限短语使用较高表项，上部短语和繁体释义可以共同横滑，下部排列日期和功能。

### 日期

点击日历回转图标，将当前条目的学习日期刷新为今天。当前年份显示 `月·日`，其他年份显示两位年份＋月＋日。

### PIN

点击图钉设置或取消 PIN。PIN 导航作为顶部覆盖层出现，不推动当前列表。

### 查询

点击统一查询按钮，在其附近弹出两个图标：

- 词典图标：直接打开牛津英汉辞书；
- AI 对话图标：运行 `AI查询` 快捷指令并打开 ChatGPT。

### 更多

包含编辑、添加相关短语、标注处理、从当前词表移除和删除等低频动作。

### 关联

存在关联时，表项右侧显示细长附着件。方向图标向右表示未展开，向下表示已展开。二级表项仍支持复制文字和独立跳转。

## 4. AI 标注

被标注条目显示红色边缘渐变。此时点按条目主体进入标注处理，不复制英文。

审阅顶部提供：

- 上一条／下一条；
- 编辑；
- 撤销此条；
- 撤销当前词表全部标注；
- 退出审阅。

当前词表和全局批量撤销均立即执行，不显示确认窗口。

## 5. 搜索

打开搜索后不会自动弹出键盘。点击输入框才开始编辑。搜索窗口保留明显边距，键盘出现后移动到可视区域内，背景词表不改变位置。

## 6. 日期模式

日期模式按年、月、日组织已标注条目，并在末尾显示“未标注”。点击日历日期或未标注入口后，相应小标题停在固定顶部下方。

## 7. 数据交换与 Seed

还原到 Seed、恢复完整备份或执行 VIX 完整替换时，程序先发起当前状态的完整备份下载，再显示小型确认窗口。确认后才执行替换。

程序只能确认下载已发起，不能确认用户是否永久保存了下载文件。

## 8. 缩放

应用尽量阻止双击、输入聚焦和手势造成的意外缩放。iOS 系统辅助功能仍可能允许缩放，这是平台行为。
