# Vocabulary Index 3.3.1 产品功能手册

## 1. 首页与词表

从 iPhone 主屏幕启动时进入首页顶部。应用内从词表返回首页时恢复离开前位置。词表顶部保留返回、紧凑标题、搜索和更多；大标题随内容滚动离开。

## 2. 浏览和上次位置

系统分别记录每张词表的字母／日期模式和普通表的词汇／短语区位置。固定工具区中的靶心按钮返回当前模式、当前分区的上次位置。全局词汇和全局短语总表同样独立记录。

## 3. 一级表项

- 轻点普通英文复制；
- 长英文和繁体释义可在文字区横向滑动；
- 边缘渐隐表示仍有隐藏内容；
- 日历按钮把学习日期刷新为今天；
- PIN 在当前词表上下文中设置；
- 查询按钮打开 Oxford／ChatGPT 双图标菜单；
- 更多提供编辑、来源与危险操作；
- 有关系时，右侧窄附着件展开二级内容。

被 AI 标注的条目显示红色警告渐变，轻点主体进入标注处理，不复制可能错误的英文。

## 4. 标注

词表工具区显示当前词表待处理数量。审阅条支持：

- 上一条／下一条；
- 编辑；
- 撤销此条；
- 撤销当前词表全部标注；
- 退出。

首页存在任何未处理标注时显示黄色警告条。其唯一操作是全局全部撤销；无确认窗口。

## 5. 搜索与弹窗

打开搜索或表单不会自动弹出键盘。点击输入框才开始编辑。搜索、设置、操作和确认窗口均保留 iPhone 边缘留白。

## 6. 高危操作与备份

Seed 还原、完整恢复、完整替换、删除词域／词表／内容等操作先询问：

- 下载备份；
- 不下载。

选择任何一项都会继续显示实际操作确认。备份选择本身不批准、不阻止操作；在后续确认窗口仍可取消整个操作。

## 7. 外部查询

Oxford 接收当前英文。ChatGPT 继续运行名为 `AI查询` 的快捷指令并通过 URL 传入 `vix-entry-context` JSON。快捷指令动作是否正确绑定输入，需要在 iPhone 上单独确认。

## 8. 缩放

应用尽量阻止双击、输入聚焦和手势误触缩放。iOS 辅助功能仍可能保留系统级缩放能力。
