# Vocabulary Index 3.4.0 产品手册

## 1. 安装与启动

将完整源码部署到 HTTPS 静态站点，通过 iPhone Safari 打开后选择“添加到主屏幕”。3.4.0 使用 IndexedDB 保存本地数据，Service Worker 提供离线外壳。

首次从 3.3.1 升级时，应用会自动将备份数据规范升级为 Schema 5。建议升级前后各导出一份完整备份。

## 2. 首页入口

首页分为全局入口和各独立域。每个域包含：

- 词汇总表；
- 短语总表；
- 普通词表卡片。

系统总表只是浏览入口，不拥有独立内容。编辑、PIN、标注和日期始终作用于具体 Entry。

## 3. 全局总表与跨域同形内容

全局词汇总表和全局短语总表汇总所有独立域的具体 Entry。

若相同英文或短语存在于多个独立域，系统分别显示多行。冲突行右上角显示来源域。每行可有不同释义、PIN、标注、日期和关系。

标题中的全局数量按唯一规范文本统计，因此可能小于实际可见行数。

## 4. 排序和编号

### 排序模式

- 字母排序；
- 日期排序。

### 编号模式

- 无序号；
- 小标题内编号；
- 连续编号。

连续编号来自字典序，日期模式下不变化。小标题内编号按当前直接容纳条目的字母、日期或“未标注”组重新计算。

## 5. 浏览和折叠字母

滚动进入某字母后，该字母小标题会吸附在固定顶栏下方。点击吸附标题可折叠整组；标题保持原位置。顶部字母轨道会温和跟随当前字母。

点击顶部字母按钮可快速跳转。返回上次位置按钮可恢复当前词表、排序模式和分区对应的位置。

## 6. 一级表项操作

轻点英文区域复制文本。超长文本可水平滑动；滑动后不会误触复制。

常用按钮：

- 日期：设置或刷新学习日期；
- PIN：添加或取消具体 Entry 的 PIN；
- 查询：选择 Oxford 或 ChatGPT 快捷指令；
- 更多：编辑、来源、移除或其他上下文操作。

跨域冲突来源副字仅用于说明来源，不是按钮。

## 7. 关系和跳转

展开关系后可复制二级内容或跳到其所在普通词表。

- 一个域内目标：直接跳转；
- 一个域外目标：直接跳转；
- 多个目标：点击多目标图标，在小窗口中选择。

选择窗口只显示有效目的地，不会列出无法定位的 Membership。

## 8. PIN

PIN 绑定具体 Entry。只要该 Entry 在当前投影中可见，PIN 状态和顶部 PIN 导航都会一致。跨域同形 Entry 可分别设置 PIN。

## 9. 学习日期和日历

学习日期绑定具体 Entry。日期模式按学习日期分组，未设置日期的内容进入“未标注”。

跨域同形 Entry 日期独立。重命名合并发生日期冲突时，系统保留较晚日期。

## 10. AI 标注核查

AI 核查可检查当前范围的词汇或短语。任务按批次请求，但整个任务只占一条 Undo 历史。

取消任务会立即中止当前请求。任务期间若 Entry 被编辑，旧结果不会覆盖新内容，也不会删除原标注。

标注内容可逐条处理、清除当前词表或从首页清除全部。

## 11. 搜索

搜索默认限制在所选范围内再排序，因此不会被其他词表的全局高分结果挤掉。

全局总表搜索具体 Entry，包括其繁体释义。跨域同形结果分别显示来源域。

## 12. 导入、导出与 VIX

完整备份用于恢复全部数据和个人状态。VIX 用于交换词域、词表、Entry 和 Membership。

导入前显示预检。若确认期间数据已变化，系统重新预检。外部 VIX 中无法唯一识别词域的裸关系键会被跳过并报告，不会自动猜测。

## 13. 高危操作

Seed 重置、完整恢复、完整替换和删除等高危操作首先询问是否下载完整备份：

- “下载备份”：发起下载后继续；
- “不下载”：直接继续。

随后才显示实际操作确认。备份选择本身不会批准或取消高危操作。

## 14. 数据安全建议

- 重大导入、迁移和删除前导出完整备份；
- 定期保存 Schema 5 备份到 Files 或其他可靠位置；
- 不要手工修改备份 schemaVersion；
- 不要依赖浏览器缓存作为唯一副本；
- VIX 预检出现歧义项时先修复来源文件。

## 15. 真机验证

完整自动化测试不等于 iPhone Safari 验收。部署后应执行 `tests/MANUAL_CHECKLIST.md`，重点检查：

- 状态栏和 Home Indicator；
- sticky 字母与折叠位置；
- 横滑手势；
- 弹窗和键盘；
- Oxford／ChatGPT 外部返回；
- Service Worker 更新；
- 大型总表滚动性能。
