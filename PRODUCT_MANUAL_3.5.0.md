# Vocabulary Index 3.5.0 使用手册

## 打开词表

首页包含全局系统总表、各独立域系统总表和普通词表。系统总表按固定内容类型显示；普通词表默认打开词汇视图。

## 普通词表的两个视图

使用底部第四个按钮切换词汇和短语。两个视图分别记忆排序模式、展开字母和上次位置。即使某一视图为空，也可以切换并查看空表。

## 底部工具栏

从左到右：

1. 继续当前视图上次浏览位置；
2. 回到页面顶部；
3. 字母／日期排序切换；
4. 词汇／短语视图切换；
5. 搜索。

## 字母和条目

点击字母小标题展开或收起。滚动时当前小标题吸附在字母栏下方。条目序号直接显示在英文前方。

条目右侧依次为：关系、学习日期、PIN、查询和更多。没有关系时不显示关系按钮；没有学习日期时日期文本不占空间。

## PIN

设置 PIN 后，页面底部工具栏上方出现 PIN 次级栏。左右按钮切换 PIN，中间按钮重新定位当前 PIN。PIN 属于具体 Entry，在不同投影视图中保持同一状态。

## 查询

点击一级表项查询总入口，选择 Oxford 或 ChatGPT。总入口图标与此前版本一致，弹窗内两个选项采用 3.5.0 新图标。

## 返回

从关系或搜索跳到其他词表后，左上角返回会回到来源页面并恢复其滚动和展开状态。外部 Oxford／ChatGPT 返回不改变当前应用页面。

## 优先级

同一普通词汇属于同域多个普通词表时，只显示在最高优先级词表。管理页调整普通词表顺序会宏观重新分配这些重叠词汇。短语不使用唯一占有规则。
