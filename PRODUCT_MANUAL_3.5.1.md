# Vocabulary Index 3.5.1 使用手册

## 浏览锚点

底部左侧靶心按钮改为手动浏览锚点：

- 短按：回到当前词表、当前词汇／短语视图、当前排序模式的锚点；
- 长按：保存当前屏幕中稳定可见的第一条词条；
- 没有锚点时短按会提示长按保存。

页面正常滚动不会再自动覆盖锚点。

## 字母／日期模式

切换模式时，应用尽量保持当前可见词条；如果当前屏幕没有明确词条，则从新模式顶部开始。切换模式不会自动跳到旧历史位置。

## 字母导航

字母栏会在当前字母接近轨道首部或尾部时提前移动，保证选中按钮持续可见。手动横滑字母栏后，应用会在手势结束后重新同步。

## 一级表项

- 序号与英文位于同一主行；
- 繁体释义位于英文下方；
- 跨域同形词的来源位于表项右下角；
- 有释义或来源时采用同一双行高度；
- 无关系的条目仍保留一个不可见关系槽，以保证其他按钮对齐。

## 日历

日历头部外侧双箭头按年跳转，内侧单箭头按月跳转：

- `«`：上一年；
- `‹`：上个月；
- `›`：下个月；
- `»`：下一年。

实际界面使用统一线性图标，不使用文本箭头。
