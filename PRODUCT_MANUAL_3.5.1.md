# Vocabulary Index 3.5.1 产品手册补充

本文件只说明 3.5.1 新交互；其余功能见 `PRODUCT_MANUAL_3.5.0.md`。

## 浏览锚点

底部工具栏第一项现在是手动浏览锚点：

- **短按**：跳转到已保存位置；
- **长按约半秒**：保存当前屏幕中第一条真实可见词条；
- 手指滑动或页面开始滚动会取消长按；
- 没有保存位置时短按会显示操作提示。

词汇／短语视图及字母／日期模式分别保存，普通滚动不会覆盖。

## 切换排序模式

底部排序按钮在字母和日期模式之间切换：

- 正在阅读具体词条时，尽量保持该词条；
- 位于页面顶部或日历顶部时，新模式从顶部开始；
- 不会再跳到目标模式中某个旧历史位置。

## 字母导航

- 点击字母按钮会展开对应分组并定位标题；
- 点击字母标题会展开或收起；
- 当前字母接近轨道左右边缘时，字母栏会提前跟随；
- A 与 `#` 分别对应轨道首尾。

## 日期日历

日历顶部五个区域依次为：上一年、上个月、年月、下个月、下一年。双箭头按年，单箭头按月。

## 弹窗

所有主要弹窗使用统一卡片：

- 标题位于物理中心；
- 输入和按钮不会超出右侧；
- 只有内容区可滚动；
- 在固定区域滑动不会带动后方词表。
