# Vocabulary Index 3.5.2 产品手册

## 浏览词表

进入词表后使用底部五个按钮：

1. 浏览锚点：短按跳转，长按约 520ms 覆盖保存当前位置；
2. 回到顶部；
3. 切换字母/日期排序；
4. 普通词表切换词汇/短语；系统总表不可切换；
5. 搜索。

## 切换规则

切换字母/日期或词汇/短语时，目标页面总是从顶部、全部分组收起开始。这是 3.5.2 的明确规则，不属于状态丢失。

搜索、关系、PIN 和浏览锚点则属于目标跳转，会自动展开目标组。

## 返回

从搜索或关系进入其他词表后，使用左上角返回、浏览器返回或 iOS 边缘返回，可恢复离开时页面的模式、月份、展开组、关系和滚动位置。

## 字母轨道

在页面顶部 A 或页面底部 # 横向拖动字母轨道后，轨道应保持用户放置的位置。只有纵向阅读真实进入新的 sticky 字母，或用户点击字母导航时，自动跟随才重新接管。

## 日期排序

点击具体日期或“未标注”标题展开内容，再次点击收起。点击日历中有记录的日期会展开并定位相应日期。

## 表项信息

- 英文下方可显示繁体字形释义；
- 全局同形冲突时，右侧控件下方显示独立域来源；
- 两类信息各自控制所在区域高度，不再产生隐藏占位；
- 来源过长时单行省略，可从条目上下文和目标菜单确认具体域。

## 弹窗与底栏

3.5.2 取消了底部工具栏额外白带，并重构弹窗全屏遮罩。此部分属于真机修订，需要在 iPhone 17 主屏幕 PWA 上验收；若系统上滑手势与按钮发生实际冲突，应反馈具体按钮、手势和录屏时间点。
