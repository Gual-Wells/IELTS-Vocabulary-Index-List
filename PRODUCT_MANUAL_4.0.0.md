# Vocabulary Index 4.0.0 使用手册

## 首页

首页全局区默认显示“全局词汇”和“全局短语”；使用区域内切换可查看“全局非结构内容”。下方 Domain 仍是主要索引入口。

## 浏览

从首页进入任何词表都从字母模式顶部开始，结构化词表优先显示词汇。底部工具栏提供浏览锚点、回顶、字母/日期、word/phrase 切换、搜索。nonStructured 页面保留同一工具栏，但无意义的 word/phrase 切换为灰色禁用。

长按浏览锚点按钮约 520ms 保存当前位置；短按跳回已保存位置。正常页面文字不可用 iOS 长按选择，英文复制与查询使用产品自己的点击/Provider 控件。

## Entry

点击英文复制。PIN、学习日期、Annotation 都属于当前具体 Entry。跨域同形词不会共享状态。

## 关系

展开按钮显示当前 Effective Relations。设置默认关闭低信息功能词关联；如需查看完整底层关系，可在设置关闭“关闭低级词汇关联”。Domain 的“不参与关联”也只暂时隐藏该域关系，不删除关系数据。

跳转图标：当前结构域唯一、其他结构域唯一、非结构域唯一、多目标。多目标菜单会显示所有有效目标。

## 搜索

搜索支持模糊召回；搜索范围可选择全部、全局类型、Domain 或普通 Collection。Collection 范围代表其完整有效可见内容，而不是当前只看 word 还是 phrase。

## 查询

- Oxford：直接打开牛津查询。
- Collins：配置 API Key 后尝试内嵌结果；失败时可打开 Collins 网站。
- Groq：使用当前模型生成临时核查卡。
- ChatGPT：调用 iOS 快捷指令 `AI查询`，发送紧凑条目上下文。

## 设置/数据

可配置 Groq/Collins Key、Groq 模型、低级关系过滤和 Domain 关联开关。VIX v2 用于内容交换；Full Backup Schema6 用于完整 4.0 状态恢复。旧版本文件不兼容。

## 4.0 升级

首次从 3.5.x 升级时可先下载旧备份，然后确认替换整个内容世代。旧 PIN/日期/标注/自建内容不会迁移；这是有意的断代设计。
