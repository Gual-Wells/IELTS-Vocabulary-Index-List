# Vocabulary Index 4.0.1 使用手册

4.0.1 的数据和学习语义与 4.0.0 相同，主要变化是 iPhone UI 收口。

- 设置/管理词库可以层层打开；关闭顶层只返回上一层，上一层未保存输入和滚动位置继续保留。
- 长管理窗口只滚动中间内容，不再把弹窗撑到屏幕边缘。
- 字母浏览使用新的独立 sticky 当前字母；列表中的字母标题仍可点击展开/收起。
- 非结构内容的长句型/模板现在支持两行或极长横向滚动显示。
- 查询菜单仍按 Oxford、Collins、Groq、ChatGPT 排列，图标下有名称。
- 设置中的低级词汇关联开关仍与 4.0.0 完全相同，只更换视觉。
- 长按浏览锚点、复制、PIN、日期、搜索、关系、Provider、VIX v2、Backup Schema 6 均无语义变化。

真实 iPhone 17 standalone 的最终验收项目见 `tests/MANUAL_CHECKLIST.md`。
