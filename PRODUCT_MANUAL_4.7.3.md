# Vocabulary Index 4.7.3 产品手册

## 词表内切换

Word/短语与字母/日期切换仍按4.7.2规则回到目标TOP并收起分组，但4.7.3不再先把整个页面淡到透明。切换应表现为直接、稳定的状态提交。

## 首页切换

结构化/非结构化全局表直接替换卡片，不再闪灭；返回Home也不再让整个App短暂消失。

## 关联展开

一级表项主体保持不动，关联内容从该表项下方展开/收起。展开过程中Entry文字、日期和操作按钮不应整行闪烁。

## 大型全局表

VirtualEntryList现在会把远离视口的已渲染chunk退休为等高placeholder。字母展开状态仍保留；再次跳回旧字母时对应chunk会自动恢复。此机制用于避免A→Z后所有历史Entry row永久常驻。

## 导航

现行导航继续使用`single-slot-vix-v1`和VIX-owned navigation stack；4.7.3没有待用户裁决的导航架构变更。
