# Vocabulary Index 项目全生命周期历史与交接文档

> 本文件是正式项目留痕，不等同于 Changelog。目标是让新的聊天窗口或维护者只凭当前完整源码 ZIP 与本文件，即可理解项目背景、冻结规则、历史失败、当前实现、待验证事项和下一步迭代边界。

## 状态标签

- **[已发布]**：曾作为完整版本交付的事实。
- **[当前实现]**：当前完整源码包中的实际行为。
- **[已确认未实现]**：产品规则已确认，但当前源码尚未完成。
- **[已废弃]**：曾采用或讨论，后来明确取消。
- **[待验证]**：源码已有实现，但仍需真实设备或外部环境验证。
- **[历史问题]**：曾造成故障或返工，必须避免复发。

---

# 一、项目使命与主场景

## [当前实现] 产品目标

Vocabulary Index 是面向个人长期词汇管理的 iPhone 主屏幕 PWA。核心不是传统背词卡，而是：

- 以独立域和普通词表组织词汇、短语；
- 通过域内／全局系统总表重新投影和排序；
- 支持 PIN、学习日期、AI 标注审阅、来源关系和外部查询；
- 以本地优先方式保存数据；
- 使用完整备份与 VIX 进行恢复和交换；
- 在 iPhone 上维持低摩擦、高密度、可恢复位置的浏览体验。

## [当前实现] 运行边界

- 主平台：iPhone Safari 添加到主屏幕；
- 数据：IndexedDB 本地持久化；
- 离线外壳：Service Worker；
- 主要外部出口：Oxford 查询、ChatGPT `AI查询` 快捷指令；
- 默认单用户、单设备、单主实例；
- 不依赖云同步作为核心功能。

---

# 二、版本时间线

## 1.x：单文件原型阶段

### [历史问题] 初始结构

最初版本是单 HTML 原型，使用 localStorage、内联事件、临时 ID 和混合 UI。它证明了词汇索引概念，但存在：

- 数据身份不稳定；
- 导入缺乏规范；
- 视图和数据耦合；
- 危险操作缺乏恢复能力；
- iPhone 生命周期不可靠；
- 后续修改容易整体回归。

这一阶段不再作为行为参考。

## 2.2.0–2.2.1：云同步试验与撤销

### [已发布] 2.2.0

曾加入 GitHub PAT／云同步，导致本地核心逻辑与远端状态耦合，复杂度和失败面显著增加。

### [已发布] 2.2.1

移除云同步，恢复 local-first。此后项目冻结规则：

> 云能力不能成为打开、浏览、编辑和恢复本地词库的必要条件。

## 2.3.x：本地稳定性阶段

### [已发布]

完成：

- IndexedDB；
- 稳定 Entry/Collection 身份；
- 字母导航；
- PIN；
- 搜索；
- 上次位置；
- iPhone 前后台生命周期修复。

## 2.4.1：行为参考版本

### [已发布]

2.4.1 成为后来 3.x 重构的主要行为参考：

- 点按复制；
- 字母定位；
- PIN 导航；
- 每词表上次位置；
- 搜索；
- AI 标注审阅；
- 模型列表和批量核查。

### [当前规则]

后续重构不得为了视觉或 Schema 更新而无意丢失这些核心行为。

## 3.0 首次失败尝试

### [历史问题]

第一次所谓 3.0 更新出现执行与报告失真：代码实际仍接近 2.4.1，但被描述为已完成；工作目录又被删除／重建，缺少可复验交付证据。

由此形成永久工程规则：

1. 不依据口头报告判断完成；
2. 每阶段保存源码快照；
3. 最终 ZIP 必须在全新目录重新解压；
4. 在解压副本上重新跑测试；
5. 交付报告必须区分已实现、未验证和失败事项。

## 3.0.0：真实 3.x 数据模型

### [已发布]

引入：

- 独立域；
- 短语一等公民；
- Membership；
- PhraseToken；
- 繁体释义；
- 迁移器；
- AI 与 PWA 外壳。

### [已废弃]

RC2 曾尝试“永久底部工具栏＋独立详情页”的组合，因工具栏内容与浏览任务不匹配、详情页破坏直接操作和浏览密度而被否决。

### [当前规则]

3.5.0 恢复的是服务于列表浏览的五项底部工具栏，不恢复独立详情页，也不恢复 RC2 的旧工具集合。其职责限定为：上次位置、回到顶部、排序切换、词／短语视图切换、搜索。

## 3.0.1–3.0.3：一级表项和性能修复

### [已发布] 3.0.1

普通表主列表一度规定“只显示词，短语只在短语表”。关系、PIN 和更多保持行内，不建立独立详情页。

### [已发布] 3.0.2

处理真机总数、字母和生命周期，但引入频繁完整复制／整表重建，造成明显性能回归。

### [已发布] 3.0.3

通过局部写入、索引和分块恢复性能。

### [历史问题]

3.0.2 证明：高频操作不得走完整 Backup 深复制、全库 canonicalize 和整页重绘。

## 3.0.4–3.0.7：关系、计算机域和交换格式

### [已发布] 3.0.4

加入全局短语表、统一导航、行内释义、关系复制／跳转分离。

### [已发布] 3.0.5

加入计算机术语独立域。

### [已发布] 3.0.6/3.0.7

完成内容分类、VIX 交换、Seed 重置和相关报告。

## 3.1.0：复合普通词表与学习日期

### [已发布]

普通词表改为复合结构：词汇区和短语区可以同时存在。旧的“普通表只显示词”规则被废弃。

引入：

- 字母／日期模式；
- 学习日期和日历；
- 两分区独立位置；
- 普通表词／短语统计。

## 3.1.1：外部查询出口

### [已发布]

加入 Oxford 和 ChatGPT 外部查询。

### [当前实现]

ChatGPT 使用 URL 传递并调用名为 `AI查询` 的快捷指令。快捷指令输入绑定仍需用户在真实设备独立确认。

## 3.2.0：iPhone 收敛

### [已发布]

重点完成：

- 两行一级表项；
- 44px 触控区；
- 长列表分块；
- 视口恢复；
- iPhone 专用布局。

## 3.3.0：动态外壳与一级表项重设计

### [已发布]

引入：

- 滚动 Large Title＋固定紧凑顶栏；
- safe area；
- 首页内部位置恢复；
- 无自动聚焦弹窗；
- 常驻返回顶部；
- 覆盖式 PIN／标注条；
- 红色边缘标注；
- 查询锚定菜单；
- 固定关系轨道；
- 极限短语布局。

### [历史问题]

3.3.0 测试通过但真机仍暴露：

- 全局上次位置键读写不一致；
- 首页紧凑顶栏 Grid 错位；
- 三位序号遮挡；
- 返回顶部遮挡操作；
- 半透明顶栏产生残影；
- 旧 CSS 隐藏新标注按钮；
- 图标系统和弹窗比例未完成。

## 3.3.1：真机修复与深度审计

### [已发布]

修复了大量 3.3.0 真机问题，并将高危操作改为：

1. 先询问是否下载完整备份；
2. “下载备份”和“不下载”都继续；
3. 再进入实际操作确认。

### [历史问题]

深度源码审计确认仍存在：

- 搜索先全局截断再过滤，词表内严重漏结果；
- 全局总表只选代表 Entry，跨域繁体释义和状态丢失；
- 域重排可能使 PIN 代表重映射碰撞；
- 共享全局日期与跨域不同语义冲突；
- AI 过期结果可能误删旧标注；
- AI 批次挤占 Undo；
- 取消不能中断当前 Fetch；
- 导入计划在确认期间过期；
- two-line 短语可被静默裁切；
- 字母轨道不跟随阅读位置；
- 横滑位置可能残留；
- 首页渐变、阴影和装饰线视觉廉价。

## 3.4.0：系统总表投影模型

### [已发布]

3.4.0 完成项目语义上的重要修正：

- 系统总表被正式定义为具体 Entry 的特殊投影视图；
- 全局词汇和短语跨域同形内容分别显示；
- 规范文本组只承担全局排序、计数和序号，不拥有个人状态；
- PIN、Annotation、StudyStamp 全部属于具体 Entry；
- Backup schema 升级为 5，学习日期绑定具体 Entry；
- 搜索改为范围优先；
- 关系跳转改为域内／域外／多目标三态；
- AI 增加 Abort、过期保护和聚合 Undo；
- 导入计划加入 Revision 防护；
- 新增正式生命周期文档 `PROJECT_HISTORY.md`。

### [历史问题]

3.4.0 的业务模型基本正确，但其真机运行时不可用。iOS 录屏确认：

- 未标注日期隐藏后 Grid 自动换列，正文被控件区挤成竖线或空白；
- `max-content` 破坏词和短语布局；
- 展开状态晚于位置恢复，页面先定位后被正文推走；
- Chunk 物化、关系展开和短语升级没有统一滚动锚定；
- 普通表词汇区／短语区的两套 sticky 导航互相覆盖；
- 字母轨道平滑追赶严重落后；
- 复制式动态标题和 PIN 覆盖栏使用半透明毛玻璃，遮挡标题并透出背景；
- 固定弹窗滑动穿透主页面；
- 跨词表跳转返回直接回首页；
- 大圆角、外挂序号框和右侧关系轨道造成切口与正文空间崩溃。

这次事故形成新规则：不能把静态测试中“机制存在”当作真机运行时成立；滚动、sticky、惰性渲染和高度变化必须作为同一状态系统验证。

## 3.5.0：运行时、导航与一级表项重构

### [当前实现]

3.5.0 保留 3.4.0 的数据模型，重做页面运行时：

- 普通词表拆为词汇视图和短语视图，避免同页双导航冲突；
- 恢复任务型底部五项工具栏，顶部字母栏只承载字母；
- 建立内部页面快照和返回栈；
- 首页进入为顶部全收起，内部返回恢复完整状态，搜索／关系跳转只展开目标组；
- 同页面跳转显式执行，不依赖 Hash 变化；
- 字母跟随取消 smooth，使用即时 nearest；
- 真实小标题自身 sticky，不复制玻璃层；
- 一级表项删除外挂序号框和右侧关系轨道，采用正文＋可选日期＋紧凑操作区；
- 序号回归正文前置文本，跨域来源放在表项边框右上角；
- PIN 和标注栏改为底部工具栏上方的完全不透明直角停靠栏；
- 固定弹窗加入 iOS body 固定和 touchmove 边界锁；
- 查询总入口图标保持不变，只重绘 Oxford／ChatGPT 选项图标；
- 结构性 UI 改为直角或极小圆角，并删除残留渐变、结构阴影、毛玻璃和目标首部竖线；
- AI 人工标注保护、聚合 Undo、Abort 和导入等待事务进一步收口；
- Backup schema、DB version、Seed revision 和 VIX version 均不变化。

---

# 三、当前数据模型

## [当前实现] 核心实体

- `Domain`：独立语义域；
- `Collection`：普通词表或虚拟系统入口；
- `Entry`：具体词或短语，拥有 domainId；
- `Membership`：Entry 属于普通词表的来源关系；
- `PhraseToken`：短语拆分关系；
- `Pin`：绑定具体 Entry；
- `Annotation`：绑定具体 Entry；
- `StudyStamp`：Schema 5 中绑定具体 Entry；
- `Settings`：模式、位置、日历、域显示等；
- `History`：Undo／Redo 差异历史。

## [当前实现] 系统总表

系统总表不是内容容器：

- 域内总表：当前域普通词表 Entry 的去重投影；
- 全局总表：所有域具体 Entry 的投影；
- 跨域同形 Entry 分别显示；
- 全局唯一数量按 `kind + normalizedText` 组计数；
- 组内按独立域顺序排列；
- 组不拥有 PIN、Annotation 或 StudyStamp。

## [当前实现] 版本

- App：3.5.0；
- Backup schema：5；
- IndexedDB DB version：4；
- Seed revision：3；
- VIX version：1。

---

# 四、当前冻结的产品规则

## [当前实现] 个人状态

- PIN、AI 标注、学习日期始终属于具体 Entry；
- 系统总表只投影显示；
- 独立域顺序变化不得迁移状态；
- 跨域同形 Entry 独立处理。

## [当前实现] 全局冲突行

- 词和短语采用同一逻辑；
- 分别显示具体 Entry；
- 只在冲突行右上角显示来源域副字；
- 全局总数不重复统计；
- 组内多行可共享某些编号，但不是同一个数据对象。

## [当前实现] 序号

- 连续编号：始终继承字典序规范文本组号；
- 字母小标题内编号：同字母内同形 Entry 共享；
- 日期小标题内编号：按具体日期／未标注分别计算；同形 Entry 不同日期可不同，同日期共享。

## [当前实现] 三态关系跳转

- 唯一域内：域内图标＋直接跳转；
- 唯一域外：域外图标＋直接跳转；
- 多目标：第三种图标＋锚定扁平选择菜单；
- 只列真实可见目标；
- 首页固定卡片使用独立进入图标。

## [当前实现] 高危操作

- 先选择“下载备份／不下载”；
- 两者均继续；
- 再正式确认操作；
- 备份选择不等于批准或取消高危操作。

## [当前实现] 普通表双视图与空表

- 普通表的词汇和短语是同一 Collection 下的两份独立投影视图；
- 两个视图分别保存模式、位置、展开字母和关系展开状态；
- 即使某个 kind 数量为 0，仍可切换并显示规范空表；
- 必须支持 0↔1 动态更新。

## [当前实现] 动态字母

- 顶部横向轨道只包含字母；
- 当前字母使用无动画 nearest 跟随，A/# 有明确端点；
- 真实小标题自身 sticky，完全不透明；
- 点击 sticky 标题折叠，折叠后尽量保持标题屏幕位置；
- 不允许复制式悬浮标题、毛玻璃或平滑追赶。

## [当前实现] 普通词表优先级占有

- 同域普通词汇拥有多个普通 Membership 时，只显示在最高优先级词表；
- 调整优先级是有意保留的宏观词汇沉淀／调配能力，不得改为导入时间占有；
- 优先级变化重算投影、计数、搜索范围、关系目标和可见位置，但不迁移具体 Entry 的 PIN、标注、学习日期或释义；
- 短语继续在全部普通 Membership 中显示。

## [当前实现] 底部工具栏与停靠栏

- 底部五项固定为上次位置、顶部、排序、词／短语切换、搜索；
- PIN／标注栏停靠在工具栏上方，完全不透明、紧凑、直角；
- 系统总表的词／短语切换禁用；
- 查询总入口图标保持 3.4.0 既定图形，只更新查询菜单中的 Oxford／ChatGPT 图标。

---

# 五、工程与性能原则

## [当前规则]

1. 高频操作不得完整复制 Backup；
2. 不在滚动每帧重建投影；
3. 不对全库同步 DOM 测量；
4. 短语真实溢出只检查已物化内容；
5. AI Annotation 使用局部事务；
6. 一次 AI 任务只占一条历史；
7. 所有导入计划提交前检查 Revision；
8. 最终交付在全新目录重新解压、校验和测试；
9. 不以测试通过代替真机验收；
10. 不在功能大改同时重写全部 CSS 或 Router。

## [历史问题]

需要避免复发：

- 3.0 首次假完成／不可复验；
- 3.0.2 整库复制性能回归；
- 3.3.0 静态测试通过但真机布局失败；
- 旧 CSS 规则覆盖并隐藏新控件；
- 代表 Entry 让聚合视图错误拥有个人状态语义。

---

# 六、测试与当前基线

## [当前实现]

标准测试命令：

```bash
npm run test:all
```

3.5.0 最终自动测试结果和性能数字以 `TEST_REPORT_3.5.0.md` 为准。自动测试覆盖模型、交换格式、静态契约、压力、集成和 Node 环境性能，但不能证明 iOS sticky、惯性滚动、触控边界和 PWA 生命周期。

# 七、待验证事项

## [待验证]

1. 真实 iPhone 中真实 sticky 小标题的推出、折叠和位置稳定；
2. 字母轨道无动画 nearest 跟随和 A/# 端点；
3. Chunk 物化、关系展开和短语升级期间的滚动锚定；
4. 一级表项在 Safari 字体下的正文宽度、横滑和紧凑高度；
5. PIN／标注底部停靠栏与 Home Indicator、键盘和最后一行的关系；
6. 固定弹窗与可滚动弹窗在 iOS 上的背景滑动锁；
7. 应用内部返回栈、iOS 边缘返回和同页跳转；
8. Oxford／ChatGPT 外部返回与 `AI查询` 快捷指令输入；
9. 词表优先级调整后的全套投影和状态失效；
10. Service Worker 从 3.4.0 到 3.5.0 的真实缓存升级；
11. VoiceOver 和键盘焦点；
12. 3.4.0 灾难录屏所示场景的逐项对照复验。

完整矩阵见 `tests/MANUAL_CHECKLIST.md`。

# 八、已明确延期或不做的工程

## [已废弃／延期]

- GitHub PAT 云同步作为核心；
- 独立详情页，以及 RC2 那套与列表任务不匹配的旧底部工具栏；
- 全量跨标签页临时 UI 同步；
- 自动猜测歧义 VIX；
- 本轮全量 CSS 重写；
- 本轮完整 Router 重写；
- 通用浮层布局引擎；
- 一次性重画所有低频图标。

这些项目不得在没有新需求和收益论证时擅自回引。

---

# 九、交付规范

## [当前规则]

每次正式版本必须交付完整源码 ZIP，至少包括：

- 可部署源码、Seed、工具和测试；
- README、CHANGELOG；
- 当前版本 Audit、Change、Test、Migration、UX、Product Manual；
- DATA_FORMATS、DATA_REPORT、DEPLOY、SECURITY、LOCAL_ARCHITECTURE；
- `PROJECT_HISTORY.md`；
- `FILE_MANIFEST.txt`；
- `SHA256SUMS.txt`。

`PROJECT_HISTORY.md` 每版必须同步更新，不能只追加版本号。

---

# 十、跨聊天窗口交接流程

新窗口继续项目时：

1. 上传最新完整源码 ZIP；
2. 要求先阅读 `PROJECT_HISTORY.md`；
3. 再阅读当前版本的 Audit、Change、Migration、UX 和 Test 报告；
4. 解压源码并运行五组测试；
5. 对照 `tests/MANUAL_CHECKLIST.md` 查看尚未完成的真机验证；
6. 讨论新需求时明确其状态：已发布、当前实现、确认未实现、废弃或待验证；
7. 修改前建立版本快照；
8. 最终重新打完整 ZIP 并在全新目录复验。

仅有聊天摘要而无完整 ZIP，不足以进行可靠源码修改。仅有 ZIP 而不读取本文件，容易恢复已废弃方案或遗漏冻结规则。

---

# 十一、下一阶段建议

## [待验证优先]

3.5.0 交付后不应立即扩展功能。首先用真实 iPhone 录制一条完整路径：

1. 首页进入普通词表；
2. 展开大字母组并长距离滚动；
3. 观察字母跟随和 sticky；
4. 展开关系并同页／跨页跳转；
5. 返回来源页；
6. 切换词汇／短语视图；
7. 设置 PIN 并使用底部停靠栏；
8. 打开搜索和固定弹窗，测试背景滑动；
9. 进入日期模式并恢复上次位置；
10. 从 Oxford／ChatGPT 返回。

发现问题时，应同时记录录屏时间点、入口路径、当前 collection/view/mode、是否存在 PIN／标注栏及数据变更。下一次修订只处理有证据的运行时问题，避免重新叠加未经真机验证的动效。
