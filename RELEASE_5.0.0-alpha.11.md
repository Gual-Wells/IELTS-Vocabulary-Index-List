# Vocabulary Index 5.0.0-alpha.11

本版完成 Mirror 4 的结构化实现。

- 词汇、短语、用法同等匹配，不以单词为默认中心。
- 审核页分为“VIX 已有匹配”和“VIX 库外候选”两层，支持材料、结果层、分类组、单条四级复选联动。
- 每个匹配保留材料证据、类别与重要度；提交后形成可启用、关闭、归档、恢复的材料记录。
- 低级组件过滤改为独立人工审核词库，不再绑定 A1/A2；只过滤单词组件噪声，不过滤短语和用法。
- Bridge 确认后的结果保留为可恢复历史。
- Windows PowerShell 5.1 提交改用 UTF-8 字节，避免简繁中文变为 `?`。
- 恢复嵌套弹窗覆盖可见性，并统一管理类弹窗和查询入口样式。

配套 Bridge 版本为 1.1.0。Mirror 3 的已有运行仍兼容；重新同步后创建的运行使用 Mirror 4。
