# Vocabulary Index 5.0.0-alpha.12

本版完成 Mirror 自动化文件库改造。首页 Mirror 按钮只负责开启或关闭当前已选文件；没有已选文件时不可操作。文件选择、详情和永久删除位于设置中的 Mirror 文件页。

Bridge 收件轮询只传元数据，打开文件时才读取完整结果。管理弹窗先显示再异步读取远程目录；Mirror 切换不再读取 IndexedDB 或重建全库结构，而是复用结构投影，并只为启用文件构建稀疏关系视图。

新文件提醒只在首页以单张叠层卡显示，确认后显示下一张且不再重复。被覆盖的父弹窗保持原尺寸、清晰度和主题，不再缩小或虚化。

配套 Bridge 版本为 `1.2.0`，必须先部署 Bridge，再部署 Pages。

