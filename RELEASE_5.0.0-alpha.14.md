# Vocabulary Index 5.0.0-alpha.14

本版完成 iOS Bridge 认证根因收口：iOS/WebKit 可能把形如 `G:...` 的 Token 当作 URI scheme 并把首字母变为小写。PWA 在原值收到 401 后只尝试一次首字母恢复，成功才更新输入框与本机配置；Bridge 的认证规则没有放宽。

Mirror 文件选择页已按远程文件夹重构。大型“未选择/已关闭”状态卡和空白框被删除，路径、搜索、文件元数据、当前状态、选择和永久删除收敛成紧凑目录。目录本机快照即时呈现，远端失败不再把已有快照误报为空；应用启动时预热完整目录。

弹窗滚动守卫恢复为最顶层独占：父设置页保持原尺寸与清晰度，但不能被触摸或滚轮带动。通知继续位于全部弹窗之上，多条 Mirror 新文件以单卡叠层顺序确认。

Bridge 与 Groq 的过程/正常状态不再常驻页面。Bridge 打开不再自动发请求；保存先提交配置并关闭，再后台同步 Mirror Context，减少小操作被整份词库序列化拖住的等待。

Mirror 数据链补上坏释义入口：损坏释义不会进入 Context；启用释义的词域拒绝无有效释义的候选；已有坏释义只接受有效证据修复。词汇、短语和用法仍同等匹配，低级组件仍只由独立审核词库判断。

Seed revision 8 修复内置释义缺项：构建时合并同一词条的全部不同来源释义，23,917 个词条总量不变，通用词条释义项增至 48,142。升级时只替换仍等于 Seed 7 内置基线的字段，用户手工释义不被覆盖。

Groq 结果为查询词和每条例句增加发音。播放采用 Google Chirp 3 HD，经 Bridge 按点击请求生成；未点击时没有 TTS 请求，同一结果弹窗内临时复用音频，关闭弹窗即释放。Google TTS Key 与 Groq Key 一样只加密保存在 Bridge。

发布顺序：先升级 Bridge 1.4.0；随后发布 Pages 5.0.0-alpha.14。
