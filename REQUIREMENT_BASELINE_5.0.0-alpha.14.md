# 5.0.0-alpha.14 统一需求基线

## 产品边界

- Pages PWA 是唯一前台；本地 IndexedDB 保存词库、关系和学习状态。
- 查询只保留 Oxford 与 Groq。Oxford 承担完整学习，Groq 承担简洁的便携查询与回忆提示。
- Bridge 是版本无关的个人后端与远程 Mirror 文件库，保存 Groq Key、Mirror Context、自动化投递结果和已审核记录。
- Groq 查询词与英文例句提供发音入口；语音由 Bridge 调用 Google Chirp 3 HD，API Key 只加密保存在 Bridge。
- Collins 与旧 ChatGPT 查询不回到产品正文；历史证据只保留在历史包和生命周期文档中。

## Bridge 与跨设备认证

- Device Token 同时通过 Bearer 与 `X-VIX-Device-Token` 发送，Bridge 按完全相等规则认证。
- iOS/WebKit 若把形如 `G:...` 的粘贴值首字母变为小写，客户端仅在原值收到 401 后尝试一次首字母恢复；恢复成功才保存纠正值，不进行宽松匹配。
- 凭据字段关闭自动纠正、自动大写和拼写检查；真实内容遮罩，空字段与 placeholder 正常显示。
- 页面不打印指纹、debug、连接详情或“Groq 正常”等常驻状态。成功与失败统一走最高层提醒。
- Bridge 页面打开时不自动探测；保存不等待整份 Mirror Context 上传，后续同步在配置提交后后台执行。
- 保存、测试、打开结果和渲染例句都不得合成语音。只有用户点击对应播放按钮后才发起一次合成请求。
- 同一结果弹窗内可复用已经生成的音频；弹窗关闭即停止播放、取消请求并释放音频缓存，不形成持久语音库。

## Mirror 自动化与文件库

- 不保留手动请求、手动 Result 导入、队列、归档或恢复流程。
- 外部任务通过 VIX Function 自动取得冻结 Context、完成主任务、生成 Result 并提交 Bridge。
- Bridge 支持多个 Mirror 文件；PWA 从远程目录选择、查看和永久删除。
- 首页 Mirror 按钮只开关已选文件；未选择文件时禁用，不打开管理页。
- 文件选择页采用紧凑文件夹结构：路径、搜索、文件行、状态、选择与删除。移动端不得出现大型状态卡和无意义空白区。
- 目录先显示本机快照，再异步校准 Bridge；远端失败保留快照，只有成功的远端空结果才显示为空目录。
- 文件目录在应用启动和重新可见时后台预热，避免每次打开管理页再从零等待。

## 匹配、审核与数据质量

- `vocabulary`、`phrase`、`usage` 同等优先，短语和用法不因组件是低级词而被抑制。
- 低级组件判断只使用独立审核的 `relation-low-level-lexemes-source.json`，不绑定 A1/A2 成员资格。
- 审核保持两层：已有 VIX 匹配、VIX 外新候选。只有勾选项写入。
- Unicode replacement character、纯半角/全角问号释义不得进入 Context 或写入词库。
- 启用释义的目标词域中，缺少有效释义的候选无效；已有坏释义只用有证据的有效结果修复。
- Seed 8 合并同一词条在 CET/TEM 等来源中的全部不同释义项，不再只取第一项；单字段上限为 240 字符。
- Seed 7 到 Seed 8 使用逐字段基线：只补全仍等于旧内置值的释义，保留用户已修改的任何释义字段。

## UI 与交互

- 嵌套弹窗保留父层原尺寸、清晰度和主题，不虚化、不缩小、不隐藏。
- 只有最顶层弹窗可以滚动；触摸与滚轮到达边界时不得把滚动传给父弹窗或页面。
- 通知层始终高于全部弹窗。多条新文件通知只显示最上层并用叠层效果表示数量，确认后显示下一条。
- 设置顺序保持 Groq、关联、显示、词库、数据、Bridge、Mirror；下载资源与普通操作按钮分组。
- Groq 结果中的播放按钮沿用 VIX 图标、边框和状态色；iOS Safari/PWA 以用户点击直接恢复音频上下文，不自动播放。

## 交付门槛

- 自动测试覆盖 iOS Token 变形恢复、合法小写 Token 不变、远程目录缓存、Mirror 两层数据、坏释义阻断、顶层弹窗滚动和 402×874 移动端布局。
- Pages 发布包只含 `dist/` 运行时；Bridge 使用 1.4.0 包，Seed revision 为 8。版本更新不迁移或覆盖用户词库与学习状态。
