# Vocabulary Index 3.1.0 测试报告

测试日期：2026-08-02

## 自动测试

| 测试 | 结果 |
|---|---|
| 模型、迁移、投影、Seed、VIX 单元测试 | 通过 |
| 静态契约测试 | 通过 |
| 800 步随机状态压力测试 | 通过 |
| 搜索与 VIX 预检性能测试 | 通过 |
| TypeScript `checkJs` | 0 错误 |
| JavaScript / MJS 语法检查 | 通过 |
| HTML 解析 | 通过 |
| CSS 解析 | 通过 |
| JSON 解析 | 13 / 13 |
| VIX JSON Schema 示例 | 6 / 6 |
| PWA 预缓存 HTTP 冒烟 | 18 / 18 |

最近一次性能结果：

- 25 次 Seed 本地搜索：约 30 ms；
- 人工智能普通表 VIX 增量预检：约 2.7 s；
- 性能阈值：搜索低于 2.5 s，预检低于 8 s。

## 关键数据断言

- Schema 4，IndexedDB 4，Seed revision 3；
- 3.0.x 完整备份原位迁移，不进入旧 2.x 重构分支；
- 现有 v3 IndexedDB 数据在升级后优先读取，避免被 Seed 覆盖；
- 全局词汇总表只含 word；
- 全局短语总表只含 phrase；
- 独立域词汇总表只含 word；
- 独立域短语总表只含 phrase；
- 普通表可同时投影 word 与 phrase；
- 全局总表没有持久化 Collection；
- 学习日期进入完整备份，不进入 VIX 内容 JSON；
- 544 个计算机词汇分别进入一个可见普通表；
- 577 条计算机短语分别进入一个可见普通表；
- 短语分类数量为 139 / 222 / 165 / 51；
- 通用英语 10 条短语各有一个普通表 Membership；
- Seed revision 合并保留既有 StudyStamp；
- 普通表 VIX 导出和导入保留短语 Membership。

## 静态交互契约

- 一级表项包含日期和显式刷新控件；
- 复制路径不调用日期刷新；
- 字母／日期模式、日历、年月日标题和未标注区存在；
- 普通表生成词汇区和短语区；
- 两区具备互跳控件；
- 位置键包含 collection、mode 和 section；
- 关联目标解析只接受普通 Collection；
- 域内、域外和全局下钻导航语义分离；
- 返回顶部不会作为学习行为；
- Seed 恢复和数据交换仍可用。

## 压力测试结果

固定随机种子执行 800 步新增、删除、Membership 调整、PIN、学习日期和视图模式变更。最终状态：

- 126 个 Entry；
- 156 个 Membership；
- 45 条 StudyStamp；
- 所有规范化、唯一性、可见性和引用完整性校验通过。

## 尚未完成的环境测试

本构建环境没有真实 iPhone Safari、主屏幕 PWA 或 Windows Chrome 图形会话。因此以下项目需要部署后人工验收：

- 日期与繁体释义在窄屏一级表项中的最终宽度；
- Safari 动态地址栏和软键盘下的定位；
- 两区 sticky 导航和返回顶部遮挡情况；
- 主屏幕 PWA 的文件下载、备份和恢复；
- 数千条内容下的真机滚动与模式切换体感。

人工清单见 `tests/MANUAL_CHECKLIST.md`。
