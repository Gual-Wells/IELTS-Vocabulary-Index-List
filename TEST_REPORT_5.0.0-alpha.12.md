# 5.0.0-alpha.12 测试报告

覆盖范围与结果：

- Bridge 1.2：11/11 通过；覆盖鉴权、轻量收件箱、远程文件列表、按需读取、审核记录写回、确认和永久删除。
- Mirror：4/4 通过；覆盖 Context/Result 校验、三类同权匹配、选择、开启、关闭和本地记录永久删除。
- 主数据、运行时、行为、集成、Provider、Seed 迁移与压力回归通过。
- 静态资源契约通过，Service Worker 预缓存 48 项且无重复。
- 402×874 Chromium 布局契约通过：父弹窗透明度 1、视觉比例 1:1、保持可见且不接收交互。
- 23,917 条模拟 Mirror 投影耗时 57.4ms；产品开关路径不调用 `reloadStore`。
- 通用 VIX v2 预检从约 12.4s 降至 7.05s，回到既有 8s 门限内。

已构建 `dist/` 并在构建产物上再次通过静态校验。
