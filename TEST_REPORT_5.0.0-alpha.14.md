# 5.0.0-alpha.14 测试报告

本地环境未提供 npm 命令，因此按 `test:all` 定义用同一 Node 运行时逐项执行，并补跑 Mirror 大词库、冷启动、构建后静态检查与真机尺寸视觉验收。

## 自动化结果

- 核心数据与导入：PASS，23,917 个 Seed 条目、20,793 个关系组件。
- 静态与发布资源：PASS，48 个预缓存资源；`dist/` 只含运行时白名单文件。
- 运行时符号、运行时行为、压力与集成：PASS。
- Groq/Bridge 前端：11/11 PASS；包含 iOS Token 首字母恢复、合法小写 Token 保持、凭据变化使目录快照失效、候选 Key 不持久化验证，以及 TTS 只在调用播放动作时请求。
- Mirror：5/5 PASS；完整 Seed Context、两层结果、永久删除、坏释义阻断与本地 Entry ID 隔离通过。
- Bridge：13/13 PASS；认证作用域、Mirror 分片、远程目录恢复、Groq 密钥、Google TTS 密钥加密与严格按请求合成通过。
- Seed 迁移：4/4 PASS；Seed 4/6/7 升级、用户新增与删除、学习状态、用户修改释义保护通过。
- Seed 运行时：2/2 PASS；revision 8 分片重组与 hash 失败关闭通过。
- 性能：25 次搜索 221.2 ms，关系 110.0 ms，VIX 预检 4,593.2 ms；Mirror 23,917 词构建 22.1 ms。
- 402×874 布局合同：PASS；Mirror 文件页紧凑尺寸、弹窗层级、父层无缩放/虚化、滚动边界阻断及 44×44 发音按钮点击面积通过。
- 冷启动中断恢复：PASS；1,358 ms 时在 9,027 条记录处中断，恢复并完成导入用时 19,076 ms。

## 定向验收

- iOS URI-scheme 式 Token 首字母变形只在原值 401 后恢复，合法小写 Token 不变。
- Device Token 变化时废弃不匹配的本机文件目录快照。
- 保存 Bridge、测试 TTS Key、打开或渲染 Groq 结果均不生成音频；只有点击查询词或例句的播放按钮才调用合成。
- 同一结果弹窗复用已经解码的音频；重新查询或关闭弹窗会停止播放、取消请求并释放缓存。
- Seed 8 合并遗漏释义项，仍等于 Seed 7 内置基线的字段得到补全，手工改过的释义保持原值。
- 实际 402×874 浏览器视图中，Bridge 凭据与资源区完整可达；Mirror 空目录收敛为路径、搜索与一行空状态，不再出现大型状态卡和空白框。
- 嵌套父设置页保持原尺寸、清晰度和主题，带 `inert` 且不可交互；通知层级高于弹窗栈。

## 安全检查

- 用户审计中出现过的 Token/指纹片段未进入源码、测试、文档或发布目录。
- Groq 与 Google TTS Key 只经 Bridge 加密保存；Pages 包不包含真实凭据。
