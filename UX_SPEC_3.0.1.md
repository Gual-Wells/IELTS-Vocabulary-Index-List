# Vocabulary Index 3.0.1 UX 硬约束

## 内容投影

1. 普通词表一级行只能是 `kind=word`。
2. 短语表一级行只能是 `kind=phrase`。
3. 两类页面完全同构。
4. 词汇展开短语；短语展开词汇。
5. 展开区不得显示“相关短语”“组成词汇”“未收录”等解释文字。
6. 展开子项按规范英文升序排列。

## 浏览界面

1. 不显示“词项”标签。
2. 不显示词性和词表来源。
3. 不提供独立详情弹窗。
4. 卡片标题位于上部，数量位于下部；禁止绝对定位造成大面积上部空白。
5. PIN 必须在行内直接操作。
6. 更多菜单不得重复“继续上次位置”。
7. 每个词表的位置状态严格隔离。

## 搜索

1. 范围必须包含全部、词域、具体词表。
2. 当前词表打开搜索时默认当前词表。
3. 背景滚动必须锁定。
4. iOS 键盘只改变搜索层可用高度，不推动背景页面。
5. 关闭后恢复原滚动位置。

## 管理

1. 词表顺序通过拖动改变。
2. 禁止用“提高/降低优先级”作为主要排序方式。
3. 管理页直接呈现结构，不使用“管理词表 · A1”长按钮列表。

## 弹窗

1. 子层打开时保留父层状态。
2. 关闭当前层返回父层。
3. 父层输入、选择和滚动不得丢失。
4. 危险操作使用独立确认层。

## 视觉

1. 保留米色、墨绿、Georgia 数字与标题的 3.0 视觉语言。
2. 列表英文使用较轻字重和紧凑行高。
3. 必要信息通过位置、缩进和字重表达，避免重复说明文字。
4. 触控按钮保持足够目标区域，但不得用永久空槽破坏列表密度。
