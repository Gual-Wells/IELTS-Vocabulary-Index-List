# Vocabulary Index 3.0.4 UX 规范

1. 搜索、关系、上次位置与 PIN 共用统一定位函数。
2. 完整可见目标不滚动；不可见目标定位到有效视口上部约 38%。
3. 跳转目标必须完整可见，且不被 sticky 元素遮挡。
4. 一级表项文字只负责复制；关系展开、PIN、跳转和更多均为独立控件。
5. 词汇展开短语；短语展开词汇；子项按英文字典序排列。
6. 全局总表跳短语进入全局短语表。
7. 短语组成词进入对应词域优先普通词表。
8. 每个词域固定总词表第一、短语第二；全局固定总表第一、短语表第二。
9. 上次位置准星始终占据导航栏第一格，无位置时禁用但不移除。
10. 繁体释义与英文同一行，不增加行高。
11. 图标必须使用统一 SVG，不使用 Emoji 或平台相关字符箭头。
12. 任何新增交互不得重新引入整库复制或主线程长任务。
