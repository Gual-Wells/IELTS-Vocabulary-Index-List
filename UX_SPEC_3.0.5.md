# Vocabulary Index 3.0.5 UX 规范

1. 全局层、独立词域层、词表卡片必须形成清楚的三级视觉层级。
2. 层级主要依靠背景、间距、标题与细色标，不使用多重粗边框。
3. 全局层只显示全局总表和全局短语表。
4. 每个词域固定先显示总词表和短语表。
5. 隐藏来源 Collection 不得出现在首页、管理器、搜索范围或导航目的地中。
6. 没有用户可见普通词表的词域，短语组成词跳转回该词域总词表。
7. “计算机术语”默认显示繁体释义，仍保持单行表项和既有触控密度。
8. 新内置词域的数据库合并必须幂等，不能覆盖或重排现有词域内容。
