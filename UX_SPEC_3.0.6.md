# Vocabulary Index 3.0.6 UX 规范

1. 首页必须清楚区分全局层、各独立词域和词表卡片；主要依靠背景、间距、标题和细色标，不叠加粗重边框。
2. 全局固定前两项为全局总表、全局短语表；词域固定前两项为总词表、短语。
3. 计算机术语四个普通词表位于固定项之后，可拖动排序。
4. 隐藏来源 Collection 不得出现在首页、管理器、搜索范围、导出内容结构或跳转目的地中。
5. 数据交换只在首页设置中提供一个统一入口，不把六类导入导出操作散落到不同菜单。
6. 数据交换面板只要求用户理解：做什么、在哪一层、目标是谁、合并还是替换。
7. iPhone 使用全屏或大尺寸 Sheet；Windows 使用居中宽面板。差异预览为下一层，返回时保留原选择和文件。
8. 目标下拉只能列出真实可写 Collection；派生总表不能成为导入目标。
9. 增量合并不得隐式删除；完整替换必须先生成恢复备份。
10. 文件目标与当前选择冲突时不得静默写入。
11. 预检只展开真正冲突，不能要求用户逐条确认全部数据。
12. 大型 JSON 的解析和差异计算必须在 Worker 中执行；完成后只提交一次数据事务和一次界面重建。
13. 词汇、短语、PIN、搜索和导航的既有视觉密度与性能约束不得因数据交换功能回退。
