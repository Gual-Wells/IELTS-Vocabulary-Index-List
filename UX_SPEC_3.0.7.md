# Vocabulary Index 3.0.7 UX 规范

1. 一级表项与关联展开项在繁体释义显示上必须同构。
2. 关联子项英文负责复制，跳转必须使用独立控件，不得覆盖复制行为。
3. 关联释义不得增加“相关短语”“组成词汇”等说明标题。
4. 关联释义单行截断，并与一级释义使用相同的弱化层级。
5. 数据交换中心必须保留独立“还原到 Seed”功能。
6. Seed 还原必须先自动导出完整恢复备份，并要求用户显式确认。
7. Seed 还原使用单次事务，不逐条写入、不逐项重绘。
8. Seed 还原完成后只重建一次索引和首页。
9. 内容 JSON、完整备份和 Seed 初始化在用户层必须明确区分。
10. 不因本轮小改动改变 3.0.6 的首页视觉分层、四大计算机术语分类或数据交换范围模型。
