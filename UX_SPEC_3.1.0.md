# Vocabulary Index 3.1.0 UX 规格

## 1. 本版本边界

本版本只实现已确认的学习日期、复合普通表、关联导航和 Seed 重构。前端规范调研中提出的视觉系统、触控尺寸、键盘焦点和无障碍重构不纳入 3.1.0，留待后续专门版本。

## 2. 一级表项

顺序固定：

```text
英文主体｜繁体释义｜学习日期｜刷新｜关联｜PIN｜更多
```

- 繁体释义关闭时隐藏释义内容；
- 无日期时日期区域保持空白；
- 只有刷新控件写入日期；
- 刷新后给予轻量状态反馈；
- 其他既有控件语义不变。

## 3. 普通表

- 词汇区固定在前；
- 短语区固定在后；
- 两区分别拥有导航栏；
- 模式切换作用于整个普通表；
- 两区的位置和日历月份独立保存；
- 词汇导航提供跳到短语区，短语导航提供跳到词汇区。

## 4. 排序模式

### 字母模式

- 词汇和短语都按字母分组；
- 只显示存在内容的字母；
- 上次位置按区、按模式保存。

### 日期模式

- 日期降序；
- 同日字典序；
- 年、月、日三级标题；
- 未标注区最后；
- 日历只开放存在记录的日期；
- 刷新后条目移动到今天，并重新定位与高亮。

## 5. 关联导航

- 总表只许关联跳出，不许关联跳入；
- 合法落点只包括普通表词汇区和短语区；
- 域内和域外后备使用不同图标并互斥；
- 全局总表使用单独的全局下钻图标；
- 多目标先显示目的地选择；
- 无合法目标时只保留复制。

## 6. 返回顶部

- 页面滚动超过约一个视口后显示；
- 点击后不改写学习日期或上次位置；
- 返回顶部期间暂停位置追踪；
- 普通表只提供一个页面级控件。

## 7. 异步一致性

- 模式、词表或页面变化后，旧异步跳转不得继续滚动新页面；
- 日期刷新只提交一条状态记录；
- 日期模式刷新后必须等待新 DOM 生成再定位；
- 搜索、关联、PIN、跨区和返回顶部跳转应相互取消过期导航任务；
- 不因单条日期更新重建数据库关系或 PhraseToken。
