# Vocabulary Index 3.2.0 iPhone PWA UX 规格

## 1. 平台边界

- 唯一设计目标：iPhone 竖屏 standalone PWA；
- 不为桌面或其他移动平台建立适配承诺；
- 页面只使用一个纵向滚动容器；
- 所有固定或 sticky 元素必须避开安全区；
- 主要交互基于单手触控。

## 2. 首页层级

- 顶栏标题：`词汇索引`；
- 副标题：全局词汇数量；
- 不显示额外 Hero；
- “管理”位于全局标题行；
- 系统总表以淡侧光表示系统投影；
- 普通表卡片计数不得换行。

## 3. 一级表项

### 信息行

```text
英文｜繁体释义｜日期
```

- 英文优先级最高；
- 不允许 `overflow-wrap:anywhere`；
- 英文单行省略；
- 释义单行省略；
- 日期右对齐；
- 无日期时保留布局但不显示文字。

### 操作行

顺序固定：

```text
刷新｜标注｜关联｜PIN｜更多｜Oxford｜ChatGPT
```

- 每个命中区域 44×44；
- 不存在标注或关联时使用不可见占位，保持按钮位置稳定；
- 按压立即产生反馈；
- 不允许按钮回流到第三行。

## 4. 系统总表

系统总表包括全局与独立域的词汇／短语总表。

- 首页卡片使用侧向静态渐变；
- 列表页面使用更弱的侧光；
- 不使用动画光效；
- 不使用额外厚边框；
- 全局系统色强度高于域内系统色，但保持同一色相家族。

## 5. 长列表

- 每块 42 行；
- 首块立即生成；
- 其他块距视口 960px 左右开始生成；
- 未生成块提供估算高度，保持滚动范围；
- 跳转必须先物化目标块；
- 折叠字母组时移除其行 DOM；
- 日期模式只保留当前视口附近的一级行。

## 6. 滚动与位置

- 使用单一 window 滚动；
- sticky 顶栏、上下文栏和字母栏使用不透明背景；
- 位置追踪优先采样有效阅读区域；
- 支持 `scrollend` 时不在滚动中写入 IndexedDB；
- 程序化跳转期间暂停位置保存；
- 返回顶部不得覆盖上次位置。

## 7. 字体与缩放

- 禁止 Safari 自动改变页面文字比例；
- 输入控件至少 16px；
- 主要英文约 15.5–16px；
- 繁体释义约 13.5–14px；
- 卡片名称约 14.5–15px；
- 卡片复合计数约 13–14px；
- 不依赖衬线数字制造层级。

## 8. 反馈

- 复制先显示反馈，再异步保存位置；
- PIN 按钮先乐观切换视觉状态；
- 学习日期刷新按钮显示更新状态；
- 跳转目标使用整行高亮；
- 减少动态效果设置下取消非必要动画。

## 9. PWA 生命周期

- 根节点标记 standalone 状态；
- 后台恢复后检查异常 viewport 宽度；
- 仅在宽度明显偏离健康值时重置 viewport meta；
- `pageshow` 与 `visibilitychange` 都要覆盖；
- 不以桌面断点作为恢复后的合法状态。
