# Vocabulary Index 3.3.0 iPhone PWA UX 规格

## 1. 页面层级

```text
状态栏背景
固定紧凑导航
可选覆盖层：PIN / 标注审阅 / 首页标注警告
滚动大标题
固定工具区＋横向字母或日期轨道
滚动内容
常驻返回顶部
```

固定导航本身不连续改变高度。大标题属于滚动内容；离开后紧凑标题继续承担导航职能。

## 2. Safe Area

- 页面背景覆盖状态栏；
- 顶部控件从 `env(safe-area-inset-top)` 以下开始；
- 底部浮层和按钮考虑 `env(safe-area-inset-bottom)`；
- 搜索和表单同时受 Safe Area 与 Visual Viewport 限制；
- 状态栏、灵动岛和 Home Indicator 不得遮挡应用控件。

## 3. 覆盖层

### 3.1 PIN

- 固定在紧凑导航下方；
- 不进入文档流；
- 首个 PIN 和最后一个 PIN 不改变 `scrollY`；
- 仅以位移和透明度进入／离开。

### 3.2 标注审阅

- 与 PIN 互斥显示；
- 提供上一条、下一条、编辑、撤销此条、撤销当前词表全部标注和退出；
- 不推动词表内容。

### 3.3 首页黄色警告

- 只在首页且存在未处理标注时显示；
- 不进入文档流；
- 主体不可点击；
- 无取消或关闭；
- 唯一动作是“全局全部撤销”；
- 全局标注为零时自动消失。

## 4. 弹窗几何

| 类型 | 位置 | 宽度 |
|---|---|---|
| 确认 | 居中 | 视口减约 48px，最大约 322px |
| 表单 | 居中或中上 | 视口减约 40px，最大约 368px |
| 操作 | 偏下 | 视口减约 32px，最大约 390px |
| 搜索 | 中上／键盘上方 | 左右至少 16px |
| 查询菜单 | 触发按钮上方或下方 | 约两个 44px 图标宽度 |

打开含输入框的窗口时不自动聚焦输入框。

## 5. 导航和跳转

### 标题目标

字母、年份、月份、日期、未标注、词汇区和短语区标题必须停在所有当前可见顶部覆盖层之下。

### 条目目标

搜索、PIN、关联和上次位置目标位于上部阅读区，保留少量上下文，不强制居中。

### 首页

- 冷启动：顶部；
- 应用内返回：首页离开前位置。

## 6. 一级表项

### 6.1 外层

- 左侧可显示外伸序号框；
- 中部是固定几何的主体；
- 右侧预留固定关联轨道；
- 有无关联不得改变主体内部布局。

### 6.2 普通词

- 英文一行；
- 释义一行副字；
- 两者共同横滑；
- 不省略；
- 纵向滚动优先于误触横滑；
- 未明显滑动的轻点复制。

### 6.3 短语

- 常规：单行；
- 较长：最多两行、适度缩小字号、提高表项高度；
- 极限：约 1.5 倍高度，上部短语＋释义共同横滑，下部动态平均分布现有功能。

### 6.4 关联附着件

- 细长外接矩形；
- 右上右下圆角；
- 收起方向向右，展开方向向下；
- 图标为正式统一图标，不使用简单三角字符；
- 展开面板向下生成，一级表项顶部尽量保持原视觉位置。

### 6.5 标注

- 红色只作为边缘和低强度渐变；
- 文字仍保持足够对比；
- 点按主体进入处理，不复制；
- 独立控件不受主体点击行为影响。

## 7. 查询

一级行只显示一个查询入口。锚定菜单仅包含：

- Oxford 图标；
- ChatGPT 图标。

点击外部、页面滚动、路由变化或选项执行后菜单关闭。ChatGPT 继续使用 `AI查询` 快捷指令和 URL 文本输入。

## 8. 缩放

- viewport 声明 `maximum-scale=1,user-scalable=no`；
- 输入框至少 16px；
- 交互控件使用 `touch-action: manipulation`；
- standalone 模式阻止 WebKit gesture zoom；
- 目标是使日常使用几乎无法误触缩放，不承诺阻断系统辅助功能缩放。

## 9. 动效

- 覆盖层 140–200ms；
- 查询菜单 140ms 左右；
- 关联方向图标平滑旋转；
- `prefers-reduced-motion` 下取消非必要动画；
- 动效不得改变文档流或触发整页重排。
