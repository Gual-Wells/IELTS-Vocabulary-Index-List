# Vocabulary Index 3.3.1 iPhone PWA UX 规格

## 1. 顶部系统

```text
状态栏安全区
固定紧凑导航
可选更新提示
固定字母／日期导航
可选 PIN 或标注审阅覆盖层
滚动内容
```

所有顶部表面不透明。可选层不进入文档流，但必须通过统一几何计算避免互相覆盖；跳转读取实际可见表面的联合底边。

## 2. 首页

- 冷启动从顶部开始；
- 应用内返回恢复离开首页前的位置；
- 紧凑标题始终处于中间 Grid Area，即使返回按钮不可见；
- 未处理标注出现黄色覆盖条；主体不可点击，唯一操作为全局全部撤销；
- 警告条无关闭按钮且不推动卡片。

## 3. 上次位置

- 全局词汇总表、全局短语总表、域内总表和普通表分别保存；
- 字母／日期模式分别保存；
- 普通表词汇区／短语区分别保存；
- 同一 Entry 在不同上下文中的保存互不抑制；
- 目标失效后自动清理。

## 4. 一级表项

### 外层轨道

```text
[序号轨道][固定主体][关联附着轨道]
```

序号和关联均不得改变主体内部文字、日期、控件宽度或间距。

### 普通词汇

- 英文一行，繁体释义一行；
- 两者共同横滑，不换行、不省略；
- 溢出时显示轻微边缘渐隐；
- 横滑与轻点复制按单次 pointer 位移判断；
- 被标注时轻点进入处理而不复制。

### 短语

- 常规短语单行；
- 长短语最多两行；
- 极限短语约 1.5 倍高，短语和释义共同横滑；
- 下部只分布实际存在的日期与控件。

### 关联

- 附着件为窄矩形，右上右下圆角；
- 方向符号属于统一 SVG 图标系统；
- 收起向右、展开向下；
- 二级文字负责复制，右侧内部导航图标负责跳转；
- 只生成实际可达的普通词表目标。

## 5. 标注审阅

- 当前词表启动的审阅保持当前词表上下文；
- 提供上一条、下一条、编辑、撤销此条、当前词表全部撤销和退出；
- 全局总表按规范英文聚合跨域标注；
- 全部撤销立即执行，不显示确认；
- PIN 与 Review 互斥显示。

## 6. 高危操作

第一层为不可跳过的备份选择：

- 下载备份；
- 不下载。

两项都会继续。第二层是实际操作确认，可取消整个操作。增量合并不需要备份选择。

## 7. 弹窗和键盘

- 表单、搜索、操作和确认采用不同几何；
- 打开时不自动聚焦输入框；
- 用户点击输入框后才显示键盘；
- Visual Viewport 使用 height、offsetTop、offsetLeft；
- 背景位置不因弹窗或键盘改变；
- 关闭视觉图形保持低权重，命中区仍不小于 44pt。

## 8. 性能

- 滚动路径不复制整库；
- PIN、日期和标注只做局部事务／局部行更新；
- 路由事件同帧合并；
- 关系只在展开时生成；
- 分块物化继续使用 42 条一块；
- 不为每条未物化内容创建独立 Observer；
- 横滑状态只更新当前行。
