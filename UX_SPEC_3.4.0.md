# Vocabulary Index 3.4.0 UX 规范

## 1. 页面与系统总表

系统总表是具体 Entry 的投影视图。用户在普通表、域内总表或全局总表中看到同一个 Entry 时，操作的是同一个对象。

跨独立域相同规范文本是不同 Entry。全局总表分别显示；只有冲突行在正文区域右上角显示来源独立域副字。

全局标题中的数量按唯一规范文本组统计，而不是按可见行数统计。

## 2. 首页

- 大标题滚动后由固定紧凑标题接替。
- 标题严格位于屏幕水平中心。
- 首页区域不使用左侧装饰竖线。
- 全局区域和系统表卡片使用平整纯色、细边框和留白，不使用廉价阴影、光晕或方向性渐变。
- 固定词表卡片使用“进入词表”图标。

## 3. 编号

- 无序号：不保留完整序号轨道。
- 小标题内编号：字母模式按字母组，日期模式按具体日期或“未标注”组。
- 连续编号：始终继承字典序下的规范文本组编号。
- 跨域同形 Entry 在同一编号上下文中共享编号；位于不同日期小标题时可显示不同局部编号。

## 4. 动态字母标题

- 当前展开字母的真实小标题到达固定顶部下方后吸附。
- 下一字母推出当前标题。
- 点击吸附标题折叠该组。
- 折叠后小标题保持原屏幕坐标，正文在其下方消失。
- 顶部字母轨道温和跟随；只有当前按钮完全不可见时才自动滚入视野。
- 用户手动横滑字母轨道期间，系统不得争夺滚动控制。

## 5. Large Title 与位置恢复

- Large Title 完整展开时不得被固定顶栏遮挡。
- 收缩状态必须稳定，不允许停在半截标题状态。
- 恢复上次位置或程序化跳转后，重新计算 Large Title、sticky 标题和顶部遮挡底边。

## 6. 一级表项

### 普通词

- 英文单行，繁体释义单行副字；两者位于同一横滑视口。
- 日期、PIN、查询和更多保持稳定操作区。
- 不存在日期时不保留完整日期空槽。
- 真正超长文本才横滑；不得省略或挤压操作按钮。
- 横滑状态不跨 Entry、页面、模式或节点重新物化保存。

### 跨域冲突来源

- 只在全局总表的跨域同形冲突行显示。
- 放置于一级表项正文区域右上角。
- 字号和颜色低于英文主文本，不占右侧操作轨道。

### 短语

- normal 优先单行；two-line 最多两行；仍溢出时升级 extreme。
- 不允许将不可见的 line-clamp 作为最终兜底。
- 只测量已物化、当前可见或接近可见的短语。

## 7. 关系附着件与跳转

- 一级表项右侧保留稳定的关系附着轨道。
- 唯一域内目标：域内图标，直接跳转。
- 唯一域外目标：域外图标，直接跳转。
- 两个及以上目标：多目标图标，打开锚定弹窗。
- 多目标弹窗始终使用扁平列表，每行显示完整“独立域 · 词表 · 分区”。
- 只列出真实可见并可定位的目标。
- 弹窗内容过长时内部滚动，不升级为全屏 Sheet。

## 8. PIN、Annotation 与 StudyStamp

- 三者绑定具体 Entry，不绑定系统总表或额外词形组。
- 同一个 Entry 在多个投影中的状态必须一致。
- 跨域同形 Entry 相互独立。
- 全局冲突行的 PIN/标注审阅上下文显示来源域。
- 标注行点击进入审阅，不执行复制；独立按钮仍执行各自功能。

## 9. 零数量分区

保持 3.3.1 结构：标题、控制区、禁用字母和空状态均保留。0↔1 内容变化后，所有控件状态必须重新计算。

## 10. 搜索

- 打开搜索窗口不自动聚焦。
- 先限定当前范围，再评分、排序和截断。
- 全局结果按具体 Entry 显示，冲突结果显示来源域。
- 结果选择后目标位于所有顶部覆盖层之下。

## 11. 弹窗与高危操作

- 所有弹窗保留 iPhone 边缘安全距离。
- 管理窗口内部滚动，背景不滚动。
- 高危操作先询问“下载备份／不下载”；两种选择都继续到正式确认。
- 多目标菜单、查询菜单和确认窗口关闭后焦点回到合理触发点。

## 12. 高频图标

日期、PIN、查询、更多、模式、关系展开、三态跳转、进入词表、返回顶部和关闭图标使用统一：

- ViewBox；
- 线宽与圆角；
- 端点；
- 激活、禁用和按压状态；
- 视觉中心与触控中心。
