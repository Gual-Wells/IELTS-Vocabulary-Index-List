# Vocabulary Index 3.5.0 UX 规范

## 页面入口

- 首页进入普通词表：词汇视图、顶部、所有字母折叠。
- 普通词表可用底部切换按钮在词汇／短语视图间切换。
- 系统总表切换按钮禁用。
- 内部返回恢复来源页面的视图、模式、展开状态、关系状态和滚动位置。
- 搜索／关系跳转只展开目标字母并定位目标。

## 顶部与底部

顶部只保留返回、严格居中的标题和更多。字母模式下，字母轨道位于标题下方且只包含 A–Z/#。

底部工具栏从左到右固定为：上次位置、顶部、排序模式、词／短语切换、搜索。键盘出现时隐藏固定底栏。

PIN 采用底部工具栏上方的直角、完全不透明次级栏，不得覆盖标题或正文。

## 字母导航

- 当前字母立即更新选中态。
- 选中按钮不在可视区时以无动画 nearest 方式带入。
- A 强制回到最左端，# 强制到最右端。
- 用户横向拖动字母轨道期间暂停自动校正。
- 真实字母标题自身 sticky，下一标题自然推出上一标题。

## 一级表项

布局顺序：正文、可选日期、操作区。操作区顺序：关系、日期、PIN、查询、更多。

- 序号是正文前置文本，无独立框。
- 无日期不占日期宽度。
- 无释义条目为紧凑单行；有释义条目为紧凑双行。
- 来源独立域只在全局冲突行出现，位于表项边框右上。
- 不允许省略号或不可访问裁切。
- 结构性表项和关系区使用直角。

## 查询

一级表项查询总入口图标保持既定图形。弹窗内只显示重新绘制的 Oxford 和 ChatGPT 两个小图标，弹窗紧凑并锚定触发按钮。

## 动效

跨页面或视图切换仅允许 100ms 左右淡入淡出。禁止对滚动、高度、sticky 位置、字母追赶和关系展开使用平滑动画。
