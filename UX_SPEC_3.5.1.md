# Vocabulary Index 3.5.1 UX 规范

## 1. 弹窗

- 弹窗卡片在视觉视口内水平居中；
- 左右安全边距至少 16px；
- 内部表单不得使用独立 `100vw` 宽度；
- 搜索标题物理居中；编辑／管理标题可左对齐；
- 所有输入、选项和按钮以 Dialog Body 宽度为基准；
- 固定弹窗不得把触摸滚动传给背景页面。

## 2. 模式切换

- 手动切换字母／日期模式时，不读取目标模式历史锚点；
- 有明确当前 Entry 时保持该 Entry；
- 无明确当前 Entry 时从顶部开始；
- 返回页面恢复快照，搜索／关系跳转锁定目标，浏览锚点只由按钮触发。

## 3. 浏览锚点

- 靶心按钮短按跳转；
- 长按约 520ms 保存；
- 锚点按 `collectionId + viewKind + mode` 独立；
- 正常滚动、复制、PIN、搜索、关系跳转、模式切换和返回顶部均不改写锚点。

## 4. 字母栏

- 当前字母进入首部／尾部警戒位时提前移动轨道；
- A 直接归零，`#` 直接归末；
- 用户横滑期间暂停自动校正，结束后必须最终同步；
- 不使用平滑追赶或弹性过冲。

## 5. 一级表项

- 单行：无释义、无来源；
- 双行：有释义或有来源；
- 有释义和有来源时仍为同一双行高度；
- 序号位于英文前方；释义与英文左边缘对齐；
- 来源位于一级表项框右下角；
- 关系槽位始终保留，保证日期与固定按钮对齐。

## 6. 日历

日历头部顺序：上一年、上个月、年月、下个月、下一年。单箭头按月，双箭头按年。所有按钮保持 44px 命中区。
