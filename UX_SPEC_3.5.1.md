# Vocabulary Index 3.5.1 UX 规范（对齐重做版）

## Dialog

- 所有弹窗卡片在视觉视口内物理居中；
- 左右安全边距至少 16px；
- 标题相对于屏幕／卡片中心居中，不受关闭按钮挤压；
- Dialog Body 是唯一可滚动内容区；
- 输入、Select、文件输入、列表和 Footer 均不得超出内容宽度；
- 不可滚动弹窗上的滑动不得推动背景页面。

## 模式切换

- 不读取目标模式的手动浏览锚点；
- 内容区已进入阅读视口且存在明确 Entry 时，保持该 Entry；
- Large Title、日历或页面顶部状态切换后从顶部开始；
- 页面返回恢复快照；搜索／关系跳转锁定目标；手动锚点只由靶心按钮触发。

## 浏览锚点

- 短按跳转；长按约 520ms 保存；
- 锚点按 `collectionId + viewKind + mode` 独立；
- 手指移动、页面滚动或 Pointer 取消会取消长按；
- 所有普通浏览和程序跳转均不得自动覆盖锚点。

## 字母栏

- 当前字母进入首部／尾部警戒位即提前移动；
- 轨道根据浏览方向预留后续空间；
- 使用滞回避免边缘抖动；
- 首尾使用硬边界；
- 不使用平滑追赶或弹性过冲。

## 一级表项

- 单行：无释义且无来源；
- 双行：有释义或有来源；两者同时存在仍是同一双行规格；
- 主行显示序号、英文、可选日期和固定操作区；
- 第二行左侧繁体释义与英文左边缘对齐，右侧显示独立域来源；
- 来源属于一级表项框右下角，不使用绝对定位覆盖；
- 关系槽位始终保留以维持控件对齐。

## 日历

顺序为上一年、上个月、年月、下个月、下一年；单箭头按月，双箭头按年，命中区均为 44px。
