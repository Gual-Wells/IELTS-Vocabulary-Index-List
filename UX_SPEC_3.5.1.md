# Vocabulary Index 3.5.1 UX 规范

## 弹窗

- 弹窗卡片在视觉视口内物理居中，左右安全边距至少 14px。
- Header 为左右等宽 44px 控制位，中间标题不因关闭按钮偏移。
- 仅 Body 允许纵向滚动；Header、Footer、卡片空白和固定确认弹窗的滑动不移动背景。
- 卡片允许克制的小圆角；结构性词表 UI 继续使用直角。

## 字母／日期模式切换

- 用户手动切换模式时，不恢复目标模式的历史位置。
- 当前正文中存在清晰可见 Entry：目标模式定位同一个具体 Entry。
- Large Title、日历或页面顶部仍主导视图：目标模式回顶部。
- 切换不使用平滑滚动，不自动写入浏览锚点。

## 浏览锚点

- 按钮名称语义：浏览锚点。
- 短按：跳转。
- 长按 520ms：保存／覆盖当前位置。
- 无锚点短按：提示“长按此按钮保存当前位置”。
- 位移超过 10px、页面滚动或 Pointer Cancel：取消保存。
- 作用域：`collectionId + viewKind + mode`。

## 字母轨道

- 当前按钮进入第一个或最后一个完整可见警戒位时提前移动。
- 向后浏览时将当前按钮放回倒数第二个附近；向前浏览时放回第二个附近。
- A 直接归零，`#` 直接到最大横向位置。
- 不使用 Smooth，不故意制造弹性过冲。
- 用户横向拖动期间不自动争夺；结束后执行一次最终同步。

## 一级表项

### 高度

| 释义 | 来源 | 规格 |
|---|---|---|
| 无 | 无 | 单行 |
| 有 | 无 | 双行 |
| 无 | 有 | 双行 |
| 有 | 有 | 双行 |

来源不得增加第三行或额外顶部通道。

### 布局

```text
主行：序号  英文                         日期  [关系][刷新][PIN][查询][更多]
次行：      繁体释义                                      独立域来源
```

- 序号不使用独立边框。
- 释义左边缘与英文对齐。
- 来源属于一级表项框的右下区域，不属于词汇文本区。
- 无关系时保留第一个小槽位，但占位不可见、不可交互，保证其余控件对齐。

## 日历

```text
≪  <  2026 年 8 月  >  ≫
```

- 外侧双箭头：年跳转。
- 内侧单箭头：月跳转。
- 年跳转保持当前月份。
