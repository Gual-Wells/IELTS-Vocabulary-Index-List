# Vocabulary Index 3.5.2 UX 规范

## 1. 页面切换与返回

### 普通切换

以下操作进入目标页面后必须位于顶部并全部收起：

- 字母排序 ↔ 日期排序；
- 词汇视图 ↔ 短语视图。

不得映射当前 Entry，不得恢复目标作用域旧滚动或展开状态，不得自动读取手动浏览锚点。

### 明确目标跳转

搜索、关系、PIN、标注、学习日期刷新和浏览锚点跳转必须展开目标所在字母/日期组并定位具体 Entry。

### 递归返回

左上角、浏览器返回和 iOS 边缘返回统一恢复：Collection、viewKind、mode、日期月份、滚动位置、展开组、展开关系和活动分区。

## 2. 一级表项

```text
序号 ｜ 英文 + 可选繁体释义 ｜ 日期/五项控件 + 可选独立域来源
```

- 三个区域分别纵向居中；
- 左侧增加释义不会迫使右侧生成占位；
- 右侧增加来源不会迫使左侧生成占位；
- 次级信息字号与行高一致；
- 来源单行右对齐并省略；
- 序号始终垂直居中。

## 3. 顶部接缝

动态小标题的上边必须与顶部栏/字母栏的有效底边完全贴合。快速滚动时不得从接缝透出正文、控件或边框。

## 4. 字母轨道

- 横向拖动后保持用户定义位置，页面顶部 A 与页面底部 # 均适用；
- 不使用定时器自动回到 A，也不因底部边界或橡皮筋回弹自动吸回 #；
- 只有当前字母标题真实触及 sticky 边界且纵向阅读状态发生变化时，自动跟随重新接管；
- 点击字母属于显式导航，可立即接管。

## 5. 日期折叠

- 年/月为结构标题；
- 日和“未标注”为可折叠标题；
- 默认全部收起；
- 收起不能造成不可解释的页面跳动。

## 6. 底部工具栏

- 五个槽位和分隔线始终稳定；
- 禁用时只灰化图标；
- 本版试验总高 58px，不显示独立底部白带；
- 必须在 iPhone 17 standalone PWA 验证 Home 手势、误触和正文末尾可达性。

## 7. 弹窗

- 遮罩覆盖完整屏幕和底部区域；
- 卡片位于 VisualViewport 中；
- 初次出现不得先按旧尺寸闪现再归位；
- 只有明确 `.dialog-body` 可滚动；
- 背景页面保持锁定。

## 8. 长按反馈

浏览锚点达到长按阈值后，提示只在抬手后出现。Toast 不能触发 iOS 文本选择或系统触控菜单。
