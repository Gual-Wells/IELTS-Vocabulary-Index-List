# VIX 5.1.0 真机检查

- [ ] GitHub Pages PWA 显示 5.1.0，离线重开正常。
- [ ] 多层页宽度逐层内收，周边悬浮阴影清楚；没有暗色蒙版，边线不重合。
- [ ] 大词表“全部展开”后滚动流畅，虚拟化仍限制驻留节点数量。
- [ ] PIN 在普通词表与全局集合视图中都跟随 Entry 上下文显示。
- [ ] 学习日期、显示偏好、关系偏好与词表优先级投影正常。
- [ ] 页面只提供 VIX JSON 数据交换入口；increment 同域去重，不跨域误去重。
- [ ] Personal Mirror 配对、手动同步和断开正常。
- [ ] Mirror Site `/` 只展示 MirrorFS；隐藏数据库不出现在文件列表。
- [ ] VIX 内嵌 `/picker` 可搜索、浏览并选择文件，关闭/返回不破坏底层页面。
- [ ] Layer 2 候选选择提交后，Entry 增量与 Mirror 同步只在同一成功路径触发。
- [ ] 删除 Mirror 文件有明确确认并永久删除；不出现撤销或墓碑入口。
- [ ] `VIX:` 提取覆盖词汇、短语、上下文用法，Layer 2 每项有证据和具体繁体释义。
- [ ] Groq 查询模型、语音模型和 voice 独立保存；无 Google TTS 配置。
- [ ] iOS Groq 语音失败时系统 TTS 能兜底；非 iOS 不伪装云端成功。
- [ ] 页面不出现 AI 核验、annotations、撤销/重做、Bridge 或 WebMCP 入口。
