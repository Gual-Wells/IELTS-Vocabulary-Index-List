# 3.0.0 RC 人工验收清单

## 升级前

- [ ] 在 2.4.1 导出完整 JSON，并保存到浏览器之外。
- [ ] 关闭同站点其他 Safari 标签页和主屏幕 PWA。
- [ ] 确认未清除 Safari 网站数据。

## 首次升级

- [ ] 页面正常启动，只显示一次迁移提示。
- [ ] 默认词域为“通用英语”。
- [ ] A1、A2、B1、B2、C1、AWL、AVL 均存在。
- [ ] 首页总词项为 5,005。
- [ ] 完整 JSON 中 Membership 为 6,407 条且没有 `sourceText`。
- [ ] 旧词性以 `sourceLabel` 保留，不在主行显示。
- [ ] 旧 PIN、PIN 顺序、AI 标注和有效上次位置可用。
- [ ] 旧序号模式保持一致（不显示 / 分组 / 全局）。
- [ ] 再次启动不重复迁移。

## 词域与词表

- [ ] 新建专业词域后自动出现唯一“短语”系统表。
- [ ] 系统短语表不能删除或重命名。
- [ ] 同一英文词可在不同词域分别新增。
- [ ] 同域重复新增只复用 Entry 并增加 Membership。
- [ ] 调整普通词表优先级后，多来源词只显示在新最高优先级词表。
- [ ] 删除一个来源后词项回落；普通词失去全部来源后删除。

## 短语

- [ ] 新增 `thread pool` 后自动关联同域 `thread` 和 `pool`。
- [ ] 从 `thread` 添加同一短语不会重复。
- [ ] `threading` 不会因字符串包含而关联 `thread pool`。
- [ ] 未收录组成词显示“未收录”，之后补录可自动跳转。
- [ ] 系统短语表平铺按完整短语排序，无 A–Z 折叠。
- [ ] 普通词表中的短语可跳到系统短语表精确位置。

## 繁体释义

- [ ] 未启用释义的词域不显示空白释义行或输入框。
- [ ] 启用后“线程池”保存为“線程池”。
- [ ] “开发后台”保存为“開發後台”。
- [ ] CSV、JSON、AI 候选入口均只保存规范繁体。
- [ ] 搜索“线程”可命中“線程”。
- [ ] 关闭释义后隐藏但不删除已有释义；重新启用后内容恢复显示。

## 旧功能

- [ ] 单击英文只复制英文文本。
- [ ] PIN 按固定顺序前后跳转，上次位置正确。
- [ ] 编辑词形后陈旧 AI 标注删除。
- [ ] 撤销/重做可恢复词项、来源和短语索引。
- [ ] TXT/Markdown/CSV/JSON 合并与替换预览正确。
- [ ] 完整 JSON 导出、恢复、再次导出结构一致。
- [ ] 取消标注后徽标立即消失，审阅进入相邻项。
- [ ] 首页可跨词域、跨词表连续审阅全部待核查标注。

## Groq

- [ ] 只填 API Key 后可刷新动态模型目录。
- [ ] 刷新后保留历史模型和当前选择。
- [ ] AI 核查只报告拼写/格式，不生成词性建议。
- [ ] 大词表核查支持暂停、继续、取消和部分结果保留。

## PWA 与多实例

- [ ] Safari 与主屏幕 PWA 均显示 3.0.0。
- [ ] 飞行模式冷启动可加载应用壳和本地数据。
- [ ] 一个实例修改后另一实例重新载入。
- [ ] 同时写入时至少一方安全取消，而不是静默覆盖。
